// The API and Rates screen share one wire contract.
export {
  createProfileSchema, updateProfileSchema, profileRowSchema, profileRowsSchema, saveProfileSchema,
  type CreateProfileInput, type UpdateProfileInput, type RowInput, type SaveProfileInput,
} from '@breeze/shared';
