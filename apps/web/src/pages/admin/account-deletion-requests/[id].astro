---
import DashboardLayout from '../../../layouts/DashboardLayout.astro';
import AccountDeletionRequestDetail from '../../../components/admin/AccountDeletionRequestDetail';

export const prerender = false;

const { id } = Astro.params;
---

<DashboardLayout titleKey="titles.adminAccountDeletionRequestsDetail">
  <AccountDeletionRequestDetail requestId={id ?? ''} client:load />
</DashboardLayout>
