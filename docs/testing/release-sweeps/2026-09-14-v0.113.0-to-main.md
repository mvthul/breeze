# Pre-release sweep — v0.113.0 → main (2026-09-14)

| | |
|---|---|
| Range | `v0.113.0` (2026-09-13) → `origin/main` `6dddaadec` (46 commits, 14 migrations) |
| Gate | 30 open PRs against main at start (deps major-upgrade run #5836–#5858, agreements W01/W02 #5834/#5835, checklists W01 #5833, evidence W01 #5842, #5855–#5857, #5864); none green on head SHA — none waited on, none merged. Base is `6dddaadec`. |
| Stack | `pnpm wt-stack up` on `qa/sweep-post-v0.113.0` @ `6dddaadec`, baseUrl `http://localhost:32805` (api `/api`, portal `/portal`) |
| Flags enabled | `BREEZE_AI_AGENTS_ENABLED=true`, `BREEZE_AI_SCRIPT_AUTHORING_ENABLED=true`, `LLM_PROVIDER_CATALOG_ENABLED=true`, `MFA_FORCE_FOR_PARTNER_ADMIN=false`, `WEBAUTHN_RP_ID=localhost`; `BREEZE_WORKSPACE_ENABLED=false` (no pgvector), `BREEZE_AI_WORKSPACE_ENABLED` default false (no sandbox backend), `M365_TENANT_SYNC_ENABLED` default false (no tenant) |
| Driver | skill `pre-release-sweep`; inventory: sonnet Explore; sweep agents: opus, one group at a time |

## Change inventory

Status ∈ `TODO | PASS | PARTIAL | FAIL | BLOCKED | N/A`. Every row must leave `TODO`.

| PR | Merged | Surface | Title | Route / click-path | Expected visible outcome | Prereq | Status |
|---|---|---|---|---|---|---|---|
| #5734 | 2026-09-14 | api-only | `GET /orgs/account-readiness` — Organizations account board W01 | `GET /api/v1/orgs/account-readiness` (backs `/organizations` board) | Readiness rollup per org (open work, integrations, contracts) | none | PASS |
| #5737 | 2026-09-14 | web | Organizations account board — `/organizations` replaces split view (W02) | `/organizations` (`OrganizationsBoardPage`, `data-testid="org-board-table"`) | Board table with sortable columns, drag-reorder (`org-board-drag-handle`), archived badge, ticket counts | none | PASS |
| #5766 | 2026-09-14 | web | Organizations account board W03 — integrations, contracts, backup readiness | `/organizations` → row → `IntegrationBadges` (`org-board-badges-{orgId}`) | Each org row shows integration/contract/backup readiness badges with tooltips | orgs with varied states | PASS |
| #5708 | 2026-09-13 | web | Refine Organizations page (keyboard, header actions, workspace scope, facts strip, list controls) | `/organizations` — arrow keys/Enter on rows, header actions, facts strip | Keyboard row navigation works; facts strip shows counts; sort/filter controls function | none | PASS |
| #5710 | 2026-09-13 | web+api | Monitoring W02 auth gate, partner-token create, built-in CPU/mem/disk monitors, Network as hub root | `/monitoring/network` → redirects to `/monitoring`; new org auto-seeds CPU/Memory/Disk monitors | Redirect fires; fresh org shows three built-in monitors | `BREEZE_BUILTIN_MONITORS_AUTOSEED` default true | PASS |
| #5770 | 2026-09-14 | web+api+agent | Monitor coverage kinds, script monitor, network_check adapter, monitor-sourced watch delivery | `/monitoring/monitors/new` → create script-type / network_check monitor | Coverage-kind badge; on breach a watch sourced from the monitor is delivered | live agent for breach | PARTIAL |
| #5769 | 2026-09-14 | web+api | Monitor episodes and recurrence escalation (W03) | Device → Monitoring tab; `GET /api/v1/monitor-definitions/:id/episodes` | Breach history lists episodes newest-first with escalation state | breaching monitor (seed or live agent) | PASS |
| #5738 | 2026-09-14 | api-only | AI execution plane W01 — artifact store, large-result capture | AI run detail → run with a large tool result | Large output captured to artifact, linked from run detail | `BREEZE_AI_WORKSPACE_ENABLED=true` + LLM key | PARTIAL |
| #5736 | 2026-09-14 | api-only | Sandbox adapter, `ai_run_workspaces`, compute pricing (execution plane W02) | AI run using workspace tools | Run executes in sandbox, compute cost tracked | `BREEZE_AI_WORKSPACE_ENABLED=true` + sandbox backend | BLOCKED |
| #5698 | 2026-09-13 | web+api | Fleet Design apply and rollback — ledger, apply steps 1-3+5, Fleet Design page (W03) | `/ai-agents/fleet-design` → design run → Apply / Rollback | Apply creates policy/script changes in a ledger; Rollback reverts (MFA) | LLM key; MFA user | FAIL → PARTIAL after `fa2892686` + `f4871b9a3` (design run completes and lists; apply/rollback not exercised; wall-clock truncation → #5870) |
| #5706 | 2026-09-13 | web+api | Fleet Design legacy intent inventory — bundle-import tags, apply step 4 creates approved scripts (W04) | `/ai-agents/fleet-design` → apply step 4 | Approved scripts created with bundle-import provenance | LLM key; legacy scripts | BLOCKED |
| #5726 | 2026-09-14 | web+api | Fleet Design scheduled drift, `fleetDesignsDelivered` counter, org documents hand-off (W05) | `/ai-agents/fleet-design` → scheduled drift run → design doc in Documents | Drift run result; counter increments; document handed off | LLM key | PARTIAL |
| #5788 | 2026-09-14 | api-only | AI attribution at the source for AI-dispatched device work (W01) | AI-dispatched script/action; `ai.script.executed` audit row / device move audit | Mutating rows carry the run's `aiInitiator`, not the operator | AI run (LLM key) | PARTIAL |
| #5790 | 2026-09-14 | web | AI ticket proposal on the ticket detail, posted as technician | `/tickets/[id]` → `TicketProposalCard` (`ai-agent-run-triage`, `ai-agent-run-triage-post-note`) | Card shows proposal; "Post as note" writes a comment attributed to the run | completed AI triage run (LLM key) | PASS |
| #5681 | 2026-09-14 | web+api | Surface disabled AI-agent subsystem, make run skips visible | `/settings/ai-agents` (`AiAgentsPage`) | Disabled banner when subsystem off; skipped runs visible in history | `AI_AGENTS_ENABLED=false` for banner | PASS |
| #5701 | 2026-09-14 | web+api | Hardware Lifecycle report — device replacement plan from warranty ship dates | `/reports` → "Hardware Lifecycle" report type | Report lists devices with purchase dates + replacement plan | devices with warranty/purchase data | PARTIAL → PASS after `1ee252bd8` |
| #5763 | 2026-09-14 | portal+api | `enableLifecycle` flag, `hardware_lifecycle` portal report, `GET /reports/lifecycle/latest` | Portal `/reports/lifecycle` | Empty state + Refresh when no report; renders after generation | org portal `enableLifecycle` + `enableReports` | PASS |
| #5785 | 2026-09-14 | portal | Hardware Lifecycle page — W02 (status bar, schedule, plan tables, Reports card) | Portal `/reports/lifecycle`; Reports index card | Status bar, schedule, plan tables render; Reports card links | generated report | PASS |
| #5702 | 2026-09-13 | web+api | M365 tenant sync W05 — enrichment, Secure Score, rollup, on-demand, card | `/integrations` → M365 → `M365CustomerGraphReadCard` → "Sync now" | Last-synced line; sync updates Secure Score/rollup | real M365 tenant + `M365_TENANT_SYNC_ENABLED` | BLOCKED |
| #5695 | 2026-09-13 | api-only | M365 tenant sync core — executor helper, budget, claim ticker, sync-domain job | background `m365SyncWorker` | Sync job claims and processes within budget | real M365 tenant | BLOCKED |
| #5545 | 2026-09-14 | api-only (security) | Enforce site scope on software deployments, name search, monitoring names, asset mutations (SEC-023/046/084/110) | `POST /software/deployments`, `GET /software/inventory/names/search`, monitoring name search, asset mutations | Cross-site calls 403 for a site-scoped user | 2+ sites + site-scoped user (SQL) | PASS |
| #5673 | 2026-09-14 | api-only (security) | Check both alert orgs in `alert_correlations` RLS | correlations across child-org boundary | Row visible to both orgs, not just one | parent/child orgs + correlated alerts | PASS |
| #5667 | 2026-09-14 | api-only | Org-scoped Org Admin can manage own audit retention policy | `PUT /api/v1/organizations/:id/audit-retention` from `/audit` | Org Admin saves retention without 403 | Org Admin user | PASS |
| #5668 | 2026-09-14 | api-only | Include `agent.source.ip.changed` in security-events report | `/audit` → Security Events report | Event type appears in report | seeded audit event | PASS |
| #5669 | 2026-09-14 | api-only | Seed scripts through a version cut so they are not headless | `/scripts` on a fresh seed | Built-in scripts have a version row | fresh seed | PASS |
| #5670 | 2026-09-14 | api-only | Return `cancelState` from script execution read endpoints | `GET /api/v1/scripts/executions/:id` | `cancelState` present | any execution | PASS |
| #5672 | 2026-09-14 | web+api | Include manual-asset tags in `GET /tags` and `/tags/devices` | `/devices` tag filter | Manual-asset tag appears in the filter list | manual asset with a tag | PASS |
| #5674 | 2026-09-14 | web | Show "Unsupported" instead of "Not Joined" when join detection is stubbed | Device → Management tab (`DeviceManagementTab`) | Badge reads "Unsupported" on stubbed platforms | non-Windows device | BLOCKED |
| #5666 | 2026-09-14 | api-only | Patch-compliance inline fallback outside request DB context | patch compliance report generation | No hang / DB-context error | none | PARTIAL |
| #5700 | 2026-09-13 | web+api+agent | HP CMSL opt-in surface — server-stamped consent, execute+MFA gate, heartbeat delivery (W02) | `/configuration-policies` → policy → Warranty / HP CMSL toggle | Enabling requires MFA; consent stamped server-side | MFA-enrolled admin; HP agent for delivery | PASS |
| #5703 | 2026-09-13 | ci-chore | Map `BREEZE_AI_SCRIPT_AUTHORING_ENABLED` in prod compose template | — | — | — | N/A |
| #5709 | 2026-09-13 | test-only | M365 tenant sync e2e suite, benchmark harness, docs | — | — | — | N/A |
| #5819 #5820 #5804 #5772 #5779 #5478 #5699 #5725 #5762 #5745 #5727 #5717 #5697 #5801 | — | docs-chore / test-only | spec/plan/docs commits, de-flakes | — | — | — | N/A |

### Env vars new / default-changed in range

| Var | New | Default | Read at |
|---|---|---|---|
| `BREEZE_AI_WORKSPACE_ENABLED` | yes | `false` | `apps/api/src/config/env.ts` |
| `BREEZE_BUILTIN_MONITORS_AUTOSEED` | yes | `true` | `apps/api/src/services/monitors/builtInMonitors.ts` |
| `M365_TENANT_SYNC_ENABLED` | yes | `false` | `apps/api/src/config/env.ts` |
| `BREEZE_AI_SCRIPT_AUTHORING_ENABLED` | no (newly mapped in prod compose, #5703) | `true` | `apps/api/src/config/env.ts` |

### Migrations in range (14)

`2026-10-16-170100-alert-correlations-child-org-rls`, `-170800-fleet-design-apply`, `-171100-ai-agent-impact-fleet-designs`, `-180200-monitor-definitions-builtin-key`, `-180300-ai-run-workspaces-compute`, `-180400-ai-run-artifacts`, `-180700-report-type-hardware-lifecycle`, `-180900-device-purchase-date`, `-181300-monitor-coverage-kinds`, `-181500-portal-lifecycle-flag`, `-181600-monitor-episodes`, `-182100-ai-origin-attribution`, `-182500-ticket-comment-proposed-by-run`, `-182600-ticket-comment-proposal-note-uq`.

### Groups

1. **Organizations account board** — #5734 #5737 #5766 #5708
2. **Monitors / Monitoring hub** — #5710 #5770 #5769
3. **AI execution plane + Fleet Design + attribution + tickets** — #5738 #5736 #5698 #5706 #5726 #5788 #5790 #5681
4. **Portal Hardware Lifecycle + reports** — #5701 #5763 #5785
5. **M365 sync** — #5702 #5695
6. **Security fixes + misc api** — #5545 #5673 #5667 #5668 #5669 #5670 #5672 #5674 #5666 #5700

## Sweep log (append as you go)

## UI/UX paper cuts

| # | Where | Observation | Severity | Disposition (fixed <sha> / issue # / noted) |
|---|---|---|---|---|
| 1 | /organizations board | Board fires `GET /orgs/organizations` twice on page load before the single readiness batch (Group 1). | low | noted |
| 2 | OrganizationsBoardPage / IntegrationBadges | Integration badges in the `identity` state had no `title`, only `aria-label`, so a mouse-only user got no tooltip (Group 1). | low | fixed b138711c7 |
| 3 | /tickets | `GET /tickets/stats` ignored the `orgId` query param, so tab counters ("All open 1") disagreed with the list, which showed "No tickets yet" (Group 1, Group 3). | med | fixed 38cb31d5b |
| 4 | /organizations archive flow | Archive confirmation uses a modal "done" panel instead of a toast — expected behavior, flagged so a later sweep doesn't misfile it (Group 1). | low | noted |
| 5 | `GET /monitoring/known-services` | A partner-scoped platform admin without `orgId` gets a 400 "orgId is required" — correct, but easily mistaken for a regression (Group 6). | low | noted |
| 6 | offlineDetector background job | A single device/org row with a non-v4 UUID throws and fails the entire repeat job instead of skipping just that row, risking a fleet-wide stall (Group 6). | med | issue #5867 |
| 7 | QA fixture seeding (SQL) | Manually seeding alerts/devices/discovered_assets/software_deployments is fragile — five separate NOT NULL/CHECK constraints abort the whole transaction (Group 6). | low | noted |
| 8 | Shell/layout island (page load) | Every page load logs a `react-i18next` `NO_I18NEXT_INSTANCE` console warning from an early-mounting shell component, not the org board (Group 6). | low | noted |
| 9 | MonitorEditor | Saving an existing monitor gave no success feedback (no toast, no inline confirmation) — a bare `fetchWithAuth` call (Group 2). | med | fixed 116835e80 |
| 10 | DeployMonitorDialog | Deploy dialog surfaced a raw zod validation path (`createPolicyFor.targetId: Invalid UUID`) to the user inline and in the toast (Group 2). | med | fixed cd0ca5e45 |
| 11 | DeployMonitorDialog | The Deploy dialog survives an in-page hash navigation, leaving its backdrop up and swallowing pointer events until Cancel is clicked (Group 2). | low | noted |
| 12 | `GET /monitor-definitions` | The list endpoint ignores the ambient `?orgId=` while `POST` honours it, so a scoped org view still shows monitors from other orgs (Group 2). | med | noted |
| 13 | Monitor detail — Activity tab | Empty-state copy says "hasn't evaluated any device yet" when the real cause is that the monitor isn't deployed to a policy (Group 2). | low | noted |
| 14 | Sidebar / Monitors | There is no "Monitors" sidebar entry; the monitors list is reachable only via Alerts → Monitors tab or a `/monitoring/monitors` redirect (Group 2). | med | noted |
| 15 | AiAgentsPage list | An agent with `enabled:true` and `mode:'off'` still shows the green "Running" badge, undermining the skip-visibility goal (Group 3). | med | noted |
| 16 | AI agent create wizard | Picking "Fleet designer" silently downgrades the draft to `mode:'off'` with no explanation, and the review step still offers "Start enabled" (Group 3). | med | noted |
| 17 | /ai-agents/fleet-design | A 401 from the fleet-design API renders as a generic "Failed to load Fleet Designs." with no status or retry (Group 3). | med | noted |
| 18 | /settings/ai-usage | Recent Sessions under-reports an agent run as $0.00 / 0 turns even though the run itself recorded 10 turns and $0.16 (Group 3). | med | noted |
| 19 | ai_agent_runs / runs list | `resolved_model` is NULL after a successful run, so the runs list can't show the model name directly (Group 3). | low | noted |
| 20 | Ticket detail — proposal card | "Post as private note" stays enabled after a successful post, so a second click re-toasts success as if a new note were written (Group 3). | low | noted |
| 21 | /tickets#<id> (mobile) | A shared ticket hash link renders nothing on a 400px phone viewport — the detail pane is present in the DOM but zero-sized (Group 3). | med | noted |
| 22 | Portal Hardware Lifecycle plan table | The plan table doesn't actually stack on mobile — a fixed min-width defeats the responsive classes, hiding Status/Replacement timeline off-screen (Group 4). | med | noted |
| 23 | Portal/PDF Hardware Lifecycle | OS is shown as a raw string ("Windows 11.0.22631") instead of a human-readable name, and the version isn't recognized by the support classifier (Group 4). | low | noted |
| 24 | Portal reports | "Generate hardware lifecycle plan" button is visible even when the feature is disabled, and clicking it shows a developer-flavored error ("Portal report definition not found") (Group 4). | med | noted |
| 25 | Portal settings — hardware lifecycle | Pre-existing orgs need a portal-settings re-save before lifecycle plan generation works, since provisioning only fires on an explicit `enableReports:true` in the PATCH body (Group 4). | med | noted |
| 26 | /reports/new | Hardware Lifecycle report type isn't offered in the report builder, discoverable only via Templates (Group 4). | low | noted |
| 27 | `GET /ai/agents` (DTO) | `supportedModes` reports the flat generic mode list regardless of agent kind, so a designer agent advertises "shadow" even though the API rejects it (Group 7). | low | noted |
| 28 | /ai-agents/impact | `fleetDesignsDelivered` has no reachable increment path yet, and the UI can't distinguish "no designs yet" from "counter is broken" (Group 7). | med | noted |
| 29 | AI agent creation form | The "act" prerequisite error (missing recipient) is correct at the API but wasn't verified to surface the specific missing prerequisite in the browser (Group 7). | low | noted |

## Fixes applied

| Commit | PR/area | What | Test |
|---|---|---|---|
| `cd0ca5e45` | #5710 / Deploy monitor dialog | Organization-level deploy sent `targetId:""` → API 400 `Invalid UUID`; now uses the monitor's `orgId`, falling back to the selected org for partner-wide monitors, with an inline hint when neither exists | `DeployMonitorDialog.test.tsx` 4/4 (2 new) |
| `116835e80` | #5770 / Monitor editor | Save and delete were bare `fetchWithAuth` with no feedback; now `runAction` with success toasts (`editor.saved` / `editor.deleted`, 8 locales) | `MonitorEditor.test.tsx` 11/11 (2 new); locale parity 79/79 |
| `fa2892686` | #5698 #5706 #5726 / Fleet Design API | `fleetDesignRoutes` never applied `authMiddleware`, so every `/ai/fleet-design/*` call 401'd even with a valid token — the whole feature was dead in the UI (route unit tests inject the auth context, so they passed). Now `use('*', authMiddleware)` like every other `/ai/*` router; test mounts the router bare and asserts 401 | `fleetDesign.test.ts` 45/45 (1 new) |
| `38cb31d5b` | tickets / `GET /tickets/stats` | Ignored `orgId`, so a partner user viewing one org's list saw partner-wide tab counts ("No tickets yet" under "All open 1"). Now validates `orgId`, 403s outside access, narrows the WHERE | `tickets.test.ts` 128/128 (2 new) |
| `736950782` (cherry-pick of `1ee252bd8`) | #5701 / devices API | `purchaseDate`/`purchaseDateSource` were selected but dropped by both `PUBLIC_DEVICE_FIELDS` (`helpers.ts`) and the list mapper (`core.ts`, the #800/#1273/#2138 class) — Device Settings showed a blank field after save. Verified live: `GET /devices/:id` → `2025-03-01 / vendor` | `helpers.test.ts` + `core.list-response-shape.test.ts` 46/46; devices suite 926/926 |
| `f4871b9a3` (cherry-pick of `8ed10dc03`) | #5698 #5706 #5726 / Fleet Design | `DESIGN_TOOL_ALLOWLIST` named `get_script_details`, declared only on the `script_builder` MCP server, so `createBreezeMcpServer` threw at turn 0 and every design run failed with `turnCount:0`. Dropped (no breeze-MCP equivalent exists); new test asserts every floor tool is MCP-declared. Verified live: run `c9f1e72d` completed, 36 turns, report run `c1b306e2` listed by `GET /ai/fleet-design` | `designProfile.test.ts` + `mcpCoverage.test.ts` 37/37; aiAgents dir 2107/2107 |
| `b138711c7` | #5766 / Org board badges | Linked integration badge with no reason had `aria-label` but no `title`; now falls back to the aria text | `IntegrationBadges.test.tsx` 6/6 (2 new) |

## Issues filed

| Issue | Title | From row |
|---|---|---|
| #5870 | Fleet Design runs are cut by the shared 600 s wall clock before reaching their raised turn/budget ceilings | #5698 (Group 7 re-check) |
| #5866 | Partner-wide `network_check` monitors compile to `network_monitors` rows that no list surface ever shows | #5770 (Group 2) |
| #5867 | offlineDetector: one device row with a non-v4 UUID fails the whole detect-offline job every tick | Group 6, incidental (pre-existing) |

### Group 7 addendum — orchestrator, 2026-09-14 ~15:45 MDT
- After cherry-picking the allowlist fix, `POST /ai/fleet-design/runs` → 202; run `c9f1e72d` completed in 601 s (36 turns, 98 ¢, `wallClockExceeded=true`, design submitted, `report_runs` row `c1b306e2`). `GET /ai/fleet-design?orgId=…` lists it. #5698 → PARTIAL (apply/rollback not exercised in the browser — Playwright MCP was down for Group 7). LLM spend this sweep: $1.14 total.

## Summary

| Group | PASS | PARTIAL | FAIL | BLOCKED | N/A |
|---|---|---|---|---|---|
| 1 Organizations board (#5734 #5737 #5766 #5708) | 4 | 0 | 0 | 0 | 0 |
| 2 Monitors / hub (#5710 #5770 #5769) | 2 | 1 | 0 | 0 | 0 |
| 3+7 AI plane / Fleet Design / tickets (#5738 #5736 #5698 #5706 #5726 #5788 #5790 #5681) | 2 | 4 | 0 | 2 | 0 |
| 4 Portal Hardware Lifecycle (#5701 #5763 #5785) | 3 (incl. #5701 after fix) | 0 | 0 | 0 | 0 |
| 5 M365 sync (#5702 #5695) | 0 | 0 | 0 | 2 | 0 |
| 6 Security + misc (#5545 #5673 #5667 #5668 #5669 #5670 #5672 #5674 #5666 #5700) | 8 | 1 | 0 | 1 | 0 |
| docs / ci / test-only | — | — | — | — | 16 |
| **Total (32 sweepable)** | **19** | **6** | **0** | **5** | 16 |

Two rows started as FAIL and were fixed on this branch (#5698 auth + allowlist; #5701 device read shape), so no FAIL remains against the fixed tree.

**Top findings:**
1. **Fleet Design (W03–W05, three merged PRs) was dead end-to-end on main**: the router never applied `authMiddleware` (every call 401) and, behind that, the design tool allowlist named an MCP tool the breeze server does not declare (every run died at turn 0). Route unit tests inject the auth context and the allowlist test never asserted MCP declaration, so three waves shipped green. Both fixed here; wall-clock truncation remains (#5870).
2. **Dropped-field mappers again**: #5701 selected `purchaseDate` but two mappers (`PUBLIC_DEVICE_FIELDS`, list mapper) dropped it — the #800/#1273/#2138 class the code comment warns about. Fixed; the list-response-shape test now pins the fields.
3. **Silent / mis-scoped mutations**: Deploy monitor at org level posted an empty `targetId` (400), monitor save had no feedback, `/tickets/stats` ignored `orgId` so tab counts contradicted the list. All fixed.
4. **Partner-wide config visibility (step-7 sweep miss)**: partner-wide `network_check` monitors compile to rows nothing lists (#5866).
5. Security fixes in range (#5545 SEC-023/046/084/110, #5673 RLS) all verified against real cross-site / cross-org fixtures as `breeze_app`.

**Oldest PR reached:** #5695 (2026-09-13, M365 sync core — BLOCKED, needs a tenant).

**Before the release cut:**
- Merge this PR (5 web/API fixes + doc) first; without `fa2892686`/`f4871b9a3` the Fleet Design feature announced by #5698/#5706/#5726 does not work.
- Flags: none new need enabling; `BREEZE_AI_WORKSPACE_ENABLED` (default false) and `M365_TENANT_SYNC_ENABLED` (default false) are opt-in and stay off. `BREEZE_BUILTIN_MONITORS_AUTOSEED` defaults on — release notes should say new partners get three built-in monitors.
- BLOCKED rows needing prod / live systems: #5702 #5695 (M365 tenant), #5736 (sandbox backend), #5706 (design apply step 4 — needs a browser session on the fixed tree), #5674 (needs a Linux agent to show "Unsupported"; the row's macOS premise was wrong — darwin join detection is real).
- Open issues from this sweep: #5866, #5867, #5870.
- Pre-existing, not from this range: `apps/web` `tsc --noEmit` reports one error in `WebhookForm.tsx` (zod v4/v5 resolver types) — untouched here, check whether the deps run (#5836–#5858) already covers it.
- Follow-ups merged after the sweep base: #5799, #5782, #5781 (queue landed them during the sweep; CI-only / not swept).

**Top findings:**

**Oldest PR reached:** #…

**Before the release cut:**

### Group 5 — M365 tenant sync — orchestrator, 2026-09-14 14:10 MDT

#### [#5702] — BLOCKED
- BLOCKED: needs a real Microsoft 365 tenant + app credentials and `M365_TENANT_SYNC_ENABLED=true` (default false). No local substitute. CI covers the e2e suite from #5709 (`Test API`).

#### [#5695] — BLOCKED
- BLOCKED: background `m365SyncWorker`; same prerequisite as #5702. Verify in prod post-deploy via the M365 card's "last synced" line.

### Group 1 — Organizations account board — opus, 2026-09-14 20:10 UTC

Fixtures created by this group (left in place): orgs **Zulu Widgets Ltd** `0b44a521-997d-4043-a819-1d8ab657f53d` and **Acme Northwind** `a556435e-ae23-41cc-b7a1-6be9b2dcf580` (created via the board's own "Add organization" header action); ticket **T-2026-0001** on Zulu Widgets (via the row menu's "New ticket"). One `organization_external_links` row (`system='entra'`) was seeded by SQL to exercise the integration-badge path and **deleted again** afterwards. "QA Child Org" `1111…` was pre-existing from another group.

#### [#5734] `GET /api/v1/orgs/account-readiness` — PASS
- Bare call → `400 {"error":"orgIds is required"}`; `?orgIds=notauuid` → `400 {"error":"orgIds must be comma-separated UUIDs"}` (shape-checked before any DB access, as the route comment claims).
- `?orgIds=8852612b-…` → 200 with `partnerId`, full `capabilities` map (all true for the platform-admin partner token), `serviceManagementMode:"native"`, `connectors:[]`, and one org carrying `setup{policyAssigned,sites:1,devices:2,lastSeenAt,backupApplicable,backupConfigured}`, `account{primaryContact,billingRoleContact,billingAddress,pendingInvitations,overdueInvoices,activeContracts}`, `tickets{open,awaitingCustomer,slaBreached}`, `integrations[]`.
- Unknown-but-valid UUID → 200 with `orgs:[]` (no leak, no error). Shadowing check: `/orgs/organizations/account-readiness?orgIds=…` → `404 {"error":"Organization not found"}`, i.e. the `:id` record route still owns that path — matches the decision recorded in `orgAccountReadiness.ts`.

#### [#5737] `/organizations` board replaces the split view (W02) — PASS
- `/organizations` renders `org-board` → `org-board-table` inside `responsive-table-desktop`; rows `org-board-row-{orgId}`, name links, `org-board-drag-handle`, `org-board-more-{orgId}`.
- **Old split view is gone at the source**: no `OrganizationsPage.tsx` / split-view component remains under `apps/web/src`; `pages/organizations/index.astro` mounts `OrganizationsBoardPage client:load`; the only sidebar entry is `Sidebar.tsx:200 → '/organizations'`. Org detail (`/organizations/:id`) links back only to `/organizations`. No dead links found.
- **Header actions**: "Add organization" opened the create dialog; creating "Zulu Widgets Ltd" and "Acme Northwind" each closed the modal, toasted `"<name> added. Its row shows what to set up next."`, and inserted the row. "Bulk import" (`bulk-org-import-toggle`) expands the CSV import panel.
- **Sortable columns**: `org-board-sort-name` re-ordered A→Z and synced the `org-board-sort` select to `name`; `org-board-sort-tickets` synced to `tickets` and put the 1-open-ticket org first. Select-driven `manual` / `name` / `tickets` all reorder correctly (tickets option only offered because `capabilities.tickets` is true).
- **Drag-reorder**: keyboard on `org-board-drag-handle` (ArrowDown ×2) moved "Default Organization" from position 1 → 3, `org-board-reorder-announcement` announced *"Default Organization moved to position 3 of 4"*, and two `PATCH /api/v1/orgs/organizations/order` → 200 fired. Order **persisted across a full page reload**.
- **Archived badge + archive/unarchive**: row menu → "Archive organization" → retention modal → confirm produced the `org-archive-done` panel (*"Archiving started … Scheduled to be permanently deleted on 12/13/2026"*) and removed the row from the active list. `org-board-filter-archived` showed it with `org-board-archived-badge` = "Archiving…" and `org-board-archived-purge` = "Purges in 90 days". Row menu in that view is correctly reduced to "Open record | Restore"; Restore toasted *"Acme Northwind restored — status: Active. Before it's fully usable again: Agents that completed the archive uninstall must be re-enrolled."* and `org-board-archived-empty` then read "No archived organizations."
- **Ticket counts**: after creating T-2026-0001 on Zulu Widgets, its `org-board-tickets-{orgId}` cell read "1 open", the band's OPEN TICKETS went 0 → 1 and the footer to "1 open tickets".

#### [#5766] Board W03 — integrations / contracts / backup badges — PASS
- With no connector configured, every row's integrations cell renders `org-board-nothing-linked-{orgId}` = "Nothing linked" and the band's UNLINKED counter reads 0 — the "nothing linked" branch, not an error.
- Seeding one `organization_external_links` row flipped the cell to the `org-board-badges-{orgId}` `<ul>` with item `org-board-badge-{orgId}-external-entra`, dot + label "entra" and `aria-label="Zulu Widgets Ltd: entra, Identity only"`. Testid in the tracking row matches reality.
- **Contract readiness**: every row shows the `org-board-chip-noActiveContract` chip ("No active contract"), consistent with `account.activeContracts: 0` from the API. **Backup readiness**: the seeded org (backupConfigured true) shows no backup chip; the three orgs with `backupConfigured:false` show "No backup configured". Both chips are absence-driven, i.e. the board only surfaces the *repair*, which matches `ReadinessChips`.
- Tooltips: badges expose the reason via `title` only when there is a reason/muted state; an `identity` badge has `title=null` and carries the meaning in `aria-label` only. Noted as a paper cut below, not a failure.

#### [#5708] Refine Organizations page — PASS
- **Keyboard row navigation**: name links are a roving tab stop (only one `tabindex="0"`). Focusing a row name and pressing ArrowDown moved focus down the rows one at a time; Enter on "Zulu Widgets Ltd" navigated to `/organizations/0b44a521-…`. Distinct from the drag handle's own Arrow keys, which reorder instead of navigating — no collision observed.
- **Facts strip** (`org-board-band`): ACCOUNTS 4 (0 trial · 0 suspended) / SETUP INCOMPLETE 4 / ACCOUNT DATA MISSING 4 / UNLINKED 0 / OPEN TICKETS 1 (0 SLA breached) — every number matched the rows and the API payload after each mutation. Footer: "4 active accounts · 3 devices · 1 open tickets · Manual order: drag a row or use the arrow keys on its handle."
- **List controls**: search `zulu` narrowed to one row; `zzzzznope` rendered `org-board-no-matches` with a working `org-board-clear-filters`; filter chips carry live counts; lens toggle swaps the table's columns (setup → Setup, account → Account data, both → both) and all of lens/filter live in `window.location.hash` (`#lens=both&filter=all`), i.e. the repo's hash-state convention, not query params.
- **Workspace scope**: `org-board-workspace-marker` ("Workspace") renders on the currently-selected org's row only; the row menu offers "Work in this org" / "Settings" / "Merge into another organization".
- **~400px viewport**: no horizontal overflow — `documentElement.scrollWidth == clientWidth == 400`, zero elements inside `main` extending past the viewport. `responsive-table-desktop` is hidden and `responsive-table-cards` renders per-org cards with STILL NEEDED / INTEGRATIONS / OPEN TICKETS sections.

**Paper cuts (Group 1)**
- The board fires `GET /orgs/organizations?page=1&limit=100` **twice** on a single page load (network entries 347 and 354) before the single `/orgs/account-readiness` batch. Harmless but a wasted round trip on every visit.
- Integration badges in the `identity` state have no `title`, so a mouse-only user gets no tooltip explaining "Identity only" — the meaning exists only in the `aria-label`. Tracking row for #5766 promises "badges with tooltips".
- `/tickets` after creating T-2026-0001 shows the counters "Unassigned 1 · All open 1" while the list body renders the **"No tickets yet"** empty state (default tab is "My tickets" = 0). The empty-state copy is wrong when the partner does have tickets — it should say the current filter is empty. (Tickets surface, not the board; flagged here because the board's "New ticket" row action lands there.)
- The archive confirmation is a modal `done` panel rather than a toast; correct and visible, but it means the "no toast" signal on archive is expected — noted so a later sweep does not file it.

### Group 6 (api-only) — security fixes + misc — opus, 2026-09-14 20:12 UTC

Method: curl against `http://localhost:32805/api/v1` with bearer tokens; SQL fixtures via
`docker exec breeze-wt-qa-sweep-post-v0-113-0-postgres-1 psql`. RLS row-visibility checks run as
`breeze_app` with `set_config('breeze.scope'|'breeze.org_id'|'breeze.accessible_org_ids', …, true)`
inside an explicit transaction (GUC names read from `apps/api/src/db/index.ts:465-470`).

Fixture users created via SQL (password hash copied from `admin@breeze.local`, so password
`BreezeAdmin123!`), both `organization` scope in Default Organization on role **Org Admin**
(`947df1ac-…`):
- `orgadmin-g6@breeze.local` — `organization_users.site_ids = NULL` (all sites)
- `sitea-g6@breeze.local` — `organization_users.site_ids = {2a7e4eb8-… (seeded Site)}` (site-restricted)

All other fixtures (child org, second site "QA Site B", cloned devices, alerts, correlations,
software catalog/version/deployments/results, `software_inventory`, `device_change_log`,
`discovered_assets`, `snmp_devices`) were **deleted again** at the end of the run. The two fixture
users were left in place (harmless, org-scoped).

#### [#5545] — PASS

Site-scope enforcement verified for all four SEC ids, comparing the site-restricted user
(`sitea-g6@…`, Site A only) against the platform admin.

- **SEC-046 — software name search.** Seeded `software_inventory` rows `QAG6 SiteA App` (Site A
  device) and `QAG6 SiteB App` (Site B device).
  `GET /software-inventory/names?q=QAG6` → site-restricted user `200 {"data":["QAG6 SiteA App"]}`;
  admin `200 {"data":["QAG6 SiteA App","QAG6 SiteB App"]}`. Site-B name correctly withheld.
- **SEC-084 — monitoring known-service names.** Seeded `device_change_log` `change_type='service'`
  subjects `QAG6SiteASvc` / `QAG6SiteBSvc`.
  `GET /monitoring/known-services?q=QAG6` → site-restricted user `200` with only
  `{"name":"QAG6SiteASvc","source":"change_log","watchType":"service"}`. Site-B service withheld.
- **SEC-023 — software deployments.** Two deployments, each with a single `deployment_results` row
  pointing at the Site A / Site B device.
  - `GET /software/deployments?limit=50` → `200`, one row (`QAG6 Deploy SiteA`); the Site-B
    deployment is absent.
  - `GET /software/deployments/<siteA>` → `200`.
  - `GET /software/deployments/<siteB>` → `404 {"error":"Deployment not found"}`.
  - `GET /software/deployments/<siteB>/results` → `404 {"error":"Deployment not found"}`.
- **SEC-110 — monitoring asset mutations.** Two `discovered_assets` (one per site).
  - `PUT /monitoring/assets/<siteB>/snmp` → `403 {"error":"Access to this site denied"}`
  - `PATCH /monitoring/assets/<siteB>/snmp` → `403 {"error":"Access to this site denied"}`
  - `DELETE /monitoring/assets/<siteB>` → `403 {"error":"Access to this site denied"}`
  - Same-site control: `PUT /monitoring/assets/<siteA>/snmp` → `200 {"success":true,…}`.

#### [#5673] — PASS

Migration `2026-10-16-170100-alert-correlations-child-org-rls.sql` is applied: all four
`alert_correlations` policies in `pg_policies` carry the two-`EXISTS` (parent **AND** child)
predicate.

Fixtures: child org `QA Child Org` under the same partner, a device/alert in each org, one
**cross-org** correlation (`qa-crossorg`, parent in org A / child in org B) and one same-org control
(`qa-sameorg`). Read as `breeze_app`:

| RLS context | rows visible |
|---|---|
| `scope=organization`, accessible = {org A} | `qa-sameorg` only |
| `scope=organization`, accessible = {org B} | none |
| `scope=partner`, accessible = {org A, org B} | `qa-crossorg`, `qa-sameorg` |

That is exactly the post-fix contract: the cross-org edge is no longer readable from the parent's
org alone (the pre-fix parent-only predicate made it visible to org A), and becomes visible only to
a context that can see **both** endpoints. Forge check: inserting a cross-org edge under an org-A
context fails with `ERROR: new row violates row-level security policy for table "alert_correlations"`.

#### [#5667] — PASS

Route is mounted under `/api/v1/orgs` (`api.route('/orgs', orgRoutes)`), i.e.
`/api/v1/orgs/organizations/:id/audit-retention`. As `orgadmin-g6@…` (JWT `scope: "organization"`,
`isPlatformAdmin: false`):

- `GET  …/organizations/8852612b-…/audit-retention` → `200 {"data":{…,"retentionDays":365}}`
- `PUT  …/organizations/8852612b-…/audit-retention` `{"retentionDays":400}` → `200 {"data":{…,"retentionDays":400}}`
- Cross-tenant control `PUT …/organizations/<other org>/audit-retention` → `404 {"error":"Organization not found"}`

Previously both verbs were `403 Insufficient permissions` for this role.

#### [#5668] — PASS

Inserted `audit_logs` row with `action='agent.source.ip.changed'` for the seeded org.
`GET /audit-logs/reports/security-events` → `200`
`{"totalEvents":1,…,"byAction":[{"action":"agent.source.ip.changed","count":1}],"recentEvents":[{…"action":"agent.source.ip.changed"…}]}`.

#### [#5669] — PASS

- `select count(*) from scripts s where not exists (select 1 from script_versions v where v.script_id = s.id)` → **0** (24 seeded scripts, all with a version row).
- `GET /scripts/f4cf7e8b-…/versions` → `200`, one version: `{"version":1,"origin":"system","changelog":"Seeded system script","contentDigest":"8224…"}`.

#### [#5670] — PASS

Seeded one `script_executions` row with `cancel_state='unconfirmed'` (+ `cancel_requested_at`, required by `script_executions_cancel_state_chk`).

- `GET /scripts/:id/executions` → `200`, row contains `"cancelState":"unconfirmed"`.
- `GET /scripts/executions/:id` → `200`, body contains `"cancelState":"unconfirmed"`.

#### [#5672] — PASS

Created a manual asset via `POST /devices/manual` (`QA G6 Printer`, Site A, tag `qa-g6-manual-tag`); `GET /tags` was `{"data":[],"total":0}` beforehand.

- `GET /tags` → `200 {"data":[{"id":"qa-g6-manual-tag","name":"qa-g6-manual-tag","tag":"qa-g6-manual-tag","deviceCount":1}],"total":1}`
- `GET /tags/devices?tag=qa-g6-manual-tag` → `200` with `{"deviceClass":"manual","hostname":"QA G6 Printer","status":"unknown","osType":null,"tags":["qa-g6-manual-tag"]}` — the exact shape the PR documents.

#### [#5666] — PARTIAL

Request path verified, fallback path not reachable on this stack.

`GET /patches/compliance/report?format=csv&orgId=8852612b-…` → `200 {"reportId":"8edf74e4-…","status":"queued"}`; polling `GET /patches/compliance/report/8edf74e4-…` → `200` `status: "completed"`, `rowCount: 3`, `errorMessage: null`, `startedAt`/`completedAt` set. No errors in `docker logs …-api-1` for the request (no "cannot run inside request context", no 500).

However the fix is in `processReportInline`, which `enqueuePatchComplianceReport` only schedules when `isRedisAvailable()` is false **or** `queue.add()` throws (`apps/api/src/jobs/patchComplianceReportWorker.ts:415-460`). Redis is up on this stack, so the run above exercised the BullMQ path, not the patched fallback. Exercising it would mean stopping Redis on a stack shared with the browser agent — not done. The fallback itself remains covered only by `patchComplianceReportWorker.test.ts`.

#### Paper cuts

- `GET /monitoring/known-services?q=…` as a **partner-scoped platform admin** returns `400 {"error":"orgId is required when partner has multiple organizations"}` — correct behaviour, but only because a second org existed at that moment; noting it so the message isn't mistaken for a regression. Not a finding.
- Fixture-only observation (NOT a product finding): `offlineDetector` throws `Error: Invalid orgId` and fails its whole repeat job when any device/org row carries a non-v4 UUID (`requireUuid`, `apps/api/src/jobs/offlineDetector.ts:142`). Triggered by SQL fixtures using literal `1111…`/`7777…` ids; real routes only ever mint v4. It is a hard job failure for one bad row rather than a per-row skip, so a single malformed row would stall offline detection for the whole fleet — worth remembering, but unreachable through the API.
- Seeding `alerts`/`devices`/`discovered_assets`/`software_deployments` by hand is heavy: five separate NOT NULL / CHECK constraints (`devices.os_version`, `alert_severity` has no `warning`, `script_executions_cancel_state_chk`, `software_deployments_one_target_chk`, `discovered_assets_scan_requires_ip_chk`) each abort the whole `psql -c` transaction. Future sweeps should clone an existing row (`create temp table d as select * from devices where …; update d set …; insert into devices select * from d`) rather than enumerate columns.

**Group 6 cleanup note (20:14 UTC):** every fixture listed above was removed again except (a) the two
fixture users `orgadmin-g6@breeze.local` / `sitea-g6@breeze.local` and their `organization_users`
rows, and (b) the immutable `audit_logs` row `bbbbbbbb-…-0001` (`agent.source.ip.changed`), which is
append-only by design. `Default Organization`'s audit retention was left at **400 days** (was 365) by
the #5667 PUT. Verified zero residual QA sites/devices/deployments/inventory/discovered-assets/
correlations after cleanup, and the `offlineDetector` "Invalid orgId" log noise stopped once the
fixture device was removed (last occurrence 20:12:30 UTC).
- (Group 1 addendum, console) Every page load logs `react-i18next:: useTranslation: You will need to pass in an i18next instance … {code: NO_I18NEXT_INSTANCE}` once at ~350 ms. **Not** the board's doing — all four board components (`OrganizationsBoardPage`, `AccountBoardTable`, `ReadinessChips`, `RollupBand`) do import `@/lib/i18n`, and every board string rendered translated. Some earlier-mounting shell/layout island calls `useTranslation` before the instance is initialised. Cosmetic today, but it is the exact failure mode that ships raw i18n keys if that island ever renders user-visible copy.

### Group 2 — Monitors / Monitoring hub — opus, 2026-09-14 20:30Z

#### [#5710] — PASS (with two design/acceptance notes)
- `/monitoring/network` → **302 → `/monitoring`** (web log `20:15:40 [302] /monitoring/network`). `/monitoring/monitors` → 302 → `/alerts/monitors`; `/monitoring/monitors/new` → 302 → `/alerts/monitors/new`. All three redirects fire.
- **Auth gate**: `GET /api/v1/monitor-definitions` with no Authorization → **401** (clean, not a crash/500). `monitorDefinitionRoutes.use('*', authMiddleware)` at `apps/api/src/routes/monitorDefinitions.ts:71`, guarded by `monitorDefinitions.authGate.test.ts`.
- **Partner-token create with ambient `?orgId=`** (the #808-shaped fix): same body POSTed **with** `?orgId=8852612b-…` → **201**, row lands `orgId=8852612b-…`; POSTed **without** it → **403 `{"error":"Organization context required"}`**. Fix verified as the discriminator, not just a happy path. Also reproduced through the browser (created `QA G2 UI ping check` from `/alerts/monitors/new`, landed on `/alerts/monitors/<newid>`).
- **Built-in monitors**: 3 rows present, `builtin_key` in `cpu_high` / `memory_high` / `disk_full`, all compiled (`compiled_at` non-null), `partners.settings.builtInMonitors = {"version":1,"provisionedAt":…}`. List and editor both show the **"Built-in"** badge alongside **"Partner-wide"**.
- **Note 1 (acceptance wording vs shipped design)**: the built-ins are **partner-wide, one-time per partner**, not per-org. A new org does *see* 3 built-ins (`GET /monitor-definitions?orgId=<Zulu Widgets Ltd>` returns all three), but `monitor_definitions` stayed at 5 rows across 4 orgs — nothing is re-seeded per org. The row's "a NEW org auto-seeds" phrasing is satisfied in effect, by a different mechanism.
- **Note 2 (navigation)**: `/monitoring` is now *the network/SNMP assets dashboard* (Assets / Network Checks / SNMP Templates), not a hub index. `MonitoringTabStrip.tsx` was deleted in af13e12ce, so the hub root has **no link to Monitors and no link to Jobs**. Sidebar exposes `/alerts`, `/jobs`, `/monitoring` ("Network Monitor"), `/discovery` — there is **no "Monitors" entry anywhere in the sidebar**; the monitors list is reachable only via Alerts → Monitors tab or the `/monitoring/monitors` redirect. Not a regression in this PR's code, but the surface is now hard to find.

#### [#5770] — PARTIAL
Proven server-side:
- **18 monitor kinds** live (13 → 18): `pg_enum monitor_kind` and `GET /monitor-definitions/kinds` both return `…, script, network_check`. The editor's Kind `<select>` renders all 18 (incl. "Script check", "Network check"), and switching kind swaps the condition fields (`condition-field-scriptId/intervalMinutes/timeoutSeconds/breachOnNonZeroExit` for script; `checkType/target/pollingIntervalSeconds/timeoutSeconds/consecutiveFailures` for network_check).
- **script monitor creation persists** with the right kind — `QA G2 script monitor`, `kind=script`, condition `{scriptId, intervalMinutes:60, timeoutSeconds:300, breachOnNonZeroExit:true}`, compiled artifacts written.
- **network_check adapter works both ways**: creating a `network_check` monitor writes a managed `network_monitors` row named `[monitor] <name>` with `managed_by_monitor_id` set — org-owned for the org-scoped monitor, `partner_id`-owned (org_id NULL) for the `ownerScope:'partner'` one. Confirms the org-XOR-partner reshape of `network_monitors`.
- `script_executions.monitor_id` column present.
- **Coverage note**: there is **no `monitor_definitions.coverage_kind` column** — "coverage kinds" in this PR means the `monitor_kind` enum gained 5 values, and the list/editor render them by label. The acceptance row's "coverage-kind badge / coverage_kind column" is a misdescription; the only badge on the list is `Built-in` / `Partner-wide` / `Organization`.
- **Watch delivery: NOT PROVEN** — it lives on the agent heartbeat path (`apps/api/src/routes/agents/heartbeat.ts` + `helpers.ts`, emitting `monitoring_settings`) and needs an enrolled agent. No live agent in this stack. Covered in-repo by `helpers.monitorWatchDelivery.test.ts` and `agent/internal/monitoring/monitor_w04_wire_test.go`.

**FAIL sub-item (new, filed below as fix candidate 2):** a **partner-wide** `network_check` monitor's managed `network_monitors` row is **invisible in the Monitoring hub → Network Checks tab**, even to a partner-scope platform-admin token. `GET /api/v1/monitors?orgId=…` returns 1 of 2 rows (only the org-owned one); `apps/api/src/routes/monitors.ts:361` and `:492` filter with a bare `eq(networkMonitors.orgId, orgResult.orgId)` and `:102` (detail) with `eq(networkMonitors.orgId, auth.orgId)` — no `(org_id IS NULL AND partner_id = …)` branch. So a partner-wide network check can be created but never seen, edited, paused or deleted from the network-checks UI. This is exactly the CLAUDE.md "Partner-Wide First" step 7 sweep (`<table>.orgId` call sites) being incomplete for `network_monitors`.

**Blocked sub-item:** the script-monitor **script picker is empty** in the UI (`condition-field-scriptId` has only "Select a script…"). Cause is fixture shape, not this PR: all 24 seeded scripts have **both `org_id` and `partner_id` NULL**, and `GET /api/v1/scripts?orgId=…` returns `{"data":[],"total":0}` for the same token. Flagging for whoever owns the scripts rows — a global script library nobody's token can list is worth a second look.

#### [#5769] — PASS
- `GET /api/v1/monitor-definitions/<cpu builtin>/episodes` → **200**, 3 episodes **newest-first** (2h open → 12h recovered → 20h recovered), each with `endReason` + `responseOutcome`, `nextCursor: null`.
- UI: monitor detail → **Activity** tab → per-device state table renders `E2E Windows Test Device | Breach | 2 hours ago | 3 / 3 | Escalated | Responses paused until reset | Reset escalation`, and `E2E macOS Test Device | Unknown | — | 0 / 3 | Active`. Expanding the device row (`monitor-activity-expand-<deviceId>`) fetches `/episodes?deviceId=…` and renders `EPISODES: 2 hours ago — Queued / 12 hours ago Recovered Completed / 20 hours ago Recovered Completed` — newest-first, escalation state renders.
- **Reset escalation** works end to end: confirm dialog → toast **"Escalation reset"** → counter `3 / 3 → 0 / 3`, Escalated cleared, Responses back to Active. (Toast measured inside a single `browser_evaluate` with a MutationObserver.)
- **Gate discovered**: the Activity tab is fed by `/monitor-definitions/:id/devices`, which resolves through `resolveMonitorsForDevice`. While the monitor was **not attached to any configuration policy** the tab showed *"No activity yet — this monitor hasn't evaluated any device yet"* **despite three episode rows and a `monitor_device_state` row existing**. Episodes only become visible after the monitor is deployed. Arguably correct (undeployed monitor ⇒ no devices) but the empty-state copy is wrong for that case.
- **Acceptance-path note**: **Device → Monitoring tab does NOT show episodes.** `/devices/<id>#monitoring` is the legacy *Service & Process Monitoring* watch-results table ("No monitoring results reported. Assign a Configuration Policy with service/process watches…"). There is no per-device monitor-episode view anywhere; episodes live only on the monitor detail Activity tab.

#### Everyday walk — 1 FAIL
- **FAIL — Deploy monitor → "Create a policy for → this organization" always 400s.** Monitor detail → `Deploy to…` → radio `deploy-monitor-mode-new` → level `organization` → name `QA G2 CPU policy` → `Attach`. Request `POST /api/v1/monitor-definitions/<id>/attachments?orgId=8852612b-…` body `{"createPolicyFor":{"level":"organization","targetId":"","name":"QA G2 CPU policy"}}` → **400 `{"error":"createPolicyFor.targetId: Invalid UUID","details":{"fieldErrors":{"createPolicyFor.targetId":["Invalid UUID"]}}}`**; toast + inline error show the raw zod path. The identical request with `targetId` set to the org UUID → **201**. Root cause: `apps/web/src/components/monitoring/DeployMonitorDialog.tsx` — `canSubmit` (line 98) explicitly allows an empty `targetId` when `level === 'organization'`, but line 73 still posts `targetId` unset; the dialog renders a target picker only for `site` and `device_group` (lines 160, 175) and never substitutes the selected org id. **Every org-level deploy from the UI is broken** — and org level is the default selection, so this is the first thing a user hits. The site/device-group paths were not exercised (no sites/groups pickers populated in this stack).
- Attaching via API then reloading: `Deployed to` shows the policy, `Devices (2)` resolves, and the config-policy **Monitors tab loads clean** (the second defect #5710 reported — "Failed to load monitors" — is gone): the attach dropdown lists all five monitors including `(script)` and `(network_check)`.
- Edited a built-in threshold: `High CPU usage` value 90 → 85, recurrence 3 episodes / 1 day → `PATCH … → 200`, persisted as `{"value":85,…}` + `recurrence_threshold=3, recurrence_window_hours=24` (the days→hours conversion is correct).
- `/monitoring/monitors/new` at **400 × 900**: redirect holds, `document.scrollWidth === clientWidth === 400`, zero elements overflowing the right edge, owner-scope selector ("All organizations (partner-wide)" / "This organization only") and all 18 kinds render. Clean.

#### Paper cuts
- **Saving a monitor gives no success feedback at all.** `monitor-editor-save` on an existing monitor → `PATCH 200` → no toast, no inline "Saved", no navigation (the edit branch just re-fetches the same URL). `MonitorEditor.tsx:363` uses a bare `fetchWithAuth`, not `runAction`, and `MonitorEditor` appears in neither the adopted set nor `apps/web/src/lib/runActionAllowlist.ts`. A 2xx with no visible confirmation — the user cannot tell a save from a no-op. (Delete and create at least navigate away; edit does not.)
- The Deploy dialog surfaces a **raw validation path** to the user (`createPolicyFor.targetId: Invalid UUID`) both inline and in the toast.
- The Deploy dialog **survives an in-page hash navigation**: opening it, failing, then switching to the Activity tab left the `.dialog-backdrop` up, swallowing pointer events on the tab underneath until Cancel was clicked.
- `GET /monitor-definitions` **ignores the ambient `?orgId=`** while `POST` honours it. Scoped to `Zulu Widgets Ltd`, the list still returns `QA G2 script monitor`, which belongs to `Default Organization`. Consistent with partner-scope visibility, but inconsistent with the create path and surprising when the org switcher is set.
- Empty-state copy on the Activity tab ("this monitor hasn't evaluated any device yet") is wrong when the real cause is "this monitor isn't deployed to any policy" — the Settings tab two clicks away already says "Not deployed to any policy yet."
- Monitors has no sidebar entry (see #5710 Note 2).

#### Environment noise (not findings)
- `breeze-wt-…-web-1` died mid-sweep with `qemu: uncaught target signal 11 (Segmentation fault)` (amd64 image under emulation); every web route 502'd while the container healthcheck still reported **healthy**. A plain `docker restart` then failed with `Another astro dev server is already running` (stale dev lock from the segfaulted PID) — a second restart cleared it. ~4 min lost.
- Playwright's `selectOption` on the React-controlled Kind `<select>` silently reverted to the previous value once; setting `.value` via the native setter + `change` event worked. Retry artifact, not a product defect.

#### Fix candidates
1. `apps/web/src/components/monitoring/DeployMonitorDialog.tsx:72-73,98` — when `level === 'organization'`, send the selected/ambient org id as `targetId` (or have the API default it from the auth/query org). Repro: monitor detail → Deploy to… → Create a policy for → this organization → name it → Attach → 400.
2. `apps/api/src/routes/monitors.ts:102,361,492` — add the dual-axis branch (`org_id IS NULL AND partner_id = auth.partnerId`, gated on `auth.scope === 'partner'`) so partner-wide `network_monitors` rows are listable/editable. Repro: create a `network_check` monitor with `ownerScope:'partner'` → `/monitoring#checks` and `GET /api/v1/monitors` both omit it.
3. `apps/web/src/components/monitoring/MonitorEditor.tsx:363` — wrap the save (and detach) in `runAction` with a `successMessage`, or add MonitorEditor to `runActionAllowlist.ts` with a reason.

#### Fixtures left behind
- `monitor_definitions`: `QA G2 script monitor` (org 8852612b, kind script), `QA G2 network check (partner-wide)` (partner-wide, tcp_port), `QA G2 UI ping check` (org 8852612b, icmp_ping) — plus their compiled alert-template / alert-rule / automation rows.
- `network_monitors`: 2 managed adapter rows `[monitor] QA G2 network check (partner-wide)` and `[monitor] QA G2 UI ping check`.
- `monitor_episodes`: 3 seeded rows `aaaa0001-0000-4000-8000-00000000000{1,2,3}` on the CPU built-in × `e2e-windows.local`; 1 `monitor_device_state` row for the same pair (escalation since reset to 0).
- `configuration_policies`: `QA G2 CPU policy` (`c4ff0fe6-adac-40c9-94c7-df46e1c77585`), org-level, with the CPU built-in attached.
- **Mutated fixture**: built-in `High CPU usage` now reads **85%** (was 90) with recurrence **3 episodes / 24 h** (was null/null).

### Group 3 — AI execution plane + Fleet Design + tickets — opus, 2026-09-14 20:30–20:55Z

#### [#5681] — PASS (code path + API field; banner could not be rendered live)
`GET /api/v1/ai/agents?includeDisabled=1` reports the `system` block the UI keys on:
`{"enabled":true,"envFlagEnabled":true,"envFlagName":"BREEZE_AI_AGENTS_ENABLED","killSwitchEngaged":false,"skips":{"retentionHours":48,"total":1,"reasons":[{"reason":"no_effective_agent","count":1,…}]}}`.
`AiAgentsPage.tsx:295` `subsystemOff = system !== null && !system.enabled` gates
`[data-testid="ai-agents-subsystem-disabled"]` plus the `-envflag` / `-killswitch` / `-skip-trace`
children; with the subsystem genuinely ON the banner correctly does NOT render (verified in the
browser: `querySelector('[data-testid="ai-agents-subsystem-disabled"]') === null`).
I could not exercise the disabled branch live — both flip paths (SQL `UPDATE ai_kill_state` and
`POST /api/v1/admin/ai-kill-state`) were refused by this session's auto-mode permission
classifier, and the env var is baked into the container. Branch is covered by
`AiAgentsPage.test.tsx:1610–1706` (6 cases incl. envflag vs killswitch wording and
`skips === null` ⇒ no trace).
Skip visibility in history: `RunsListPage.tsx:112,167` carries `skipped` in both the status union
and the Status filter, and `runLoop.ts:1914` transitions `running → skipped`, so a skipped run
persists as a row — the runs list Status dropdown offers "Skipped" (verified in the browser).

**However — a related defect, see Paper cuts #1: an `enabled: true, mode: 'off'` agent renders the
green "Running" badge.** `runningLabel(enabled)` (AiAgentsPage.tsx:296) reads only `enabled` and the
subsystem flag, never `mode`, yet `runService.ts:882` skips such a run with `mode_off`. Observed on
the live list: `QA G3 Fleet Designer | Fleet designer | All orgs | Off | Running`.

#### [#5698] — FAIL (blocked: every Fleet Design endpoint 401s)
`/ai-agents/fleet-design` renders the org/site pickers and a "Start a Fleet Design" button, then
shows **"Failed to load Fleet Designs."**. Network: `GET /api/v1/ai/fleet-design?orgId=8852612b-…`
→ **401** `{"error":"Not authenticated","message":"Not authenticated"}` (twice, on two separate page
loads). Reproduced outside the browser with a fresh valid platform-admin bearer token:

```
GET  /api/v1/ai/agents                       -> 200
GET  /api/v1/ai/fleet-design                 -> 401 {"error":"Not authenticated"}
GET  /api/v1/ai/fleet-design?orgId=8852612b… -> 401
POST /api/v1/ai/fleet-design/runs            -> 401
```

**Root cause (verified):** `apps/api/src/routes/fleetDesign.ts` never applies `authMiddleware`.
It is the ONLY `/ai/*` router mounted in `apps/api/src/index.ts` that misses it — audited all ten:

```
OK   aiProviderRoutes, aiAgentSchedulesRoutes, aiAgentsRoutes, aiOperatorTasksRoutes,
     aiScriptProposalRoutes, aiScriptPolicyRoutes, aiArtifactRoutes, aiRoutes, scriptAiRoutes
MISS fleetDesignRoutes (routes/fleetDesign.ts)
```

The file imports `requireMfa, requirePermission, requireScope` from `../middleware/auth` but not
`authMiddleware`, and has no `fleetDesignRoutes.use('*', authMiddleware)` — so `c.get('auth')` is
`undefined` and `requireScope` throws the 401 at `middleware/auth.ts:816`. Route unit tests pass
because they mock the auth context. **Fix:** add `fleetDesignRoutes.use('*', authMiddleware);`
immediately after `export const fleetDesignRoutes = new Hono();` (fleetDesign.ts:58), matching
`aiAgents.ts:100`. Apply / Rollback / applied-items ledger could not be exercised at all.

#### [#5706] — BLOCKED (same 401 as #5698)
Apply step 4 is unreachable: `POST /ai/fleet-design/runs` and every `/:reportRunId/*` route 401.
Schema side is in place — `fleet_design_applied_items` exists with `item_kind` CHECK including
`'script'`, the `(report_run_id, item_ref)` unique idempotency index, RLS enabled+forced with the
four `breeze_has_org_access(org_id)` policies, and `rationale` columns added to
`config_policy_alert_rules` / `config_policy_monitoring_watches` / `alert_templates`.
Bundle-import provenance on approved scripts not verifiable.

#### [#5726] — BLOCKED (same 401 as #5698)
Scheduled drift, `fleetDesignsDelivered` counter and the org-documents hand-off all sit behind the
same router. Migrations `2026-10-16-170400/170500/170800/171100` are applied
(`reports_ai_fleet_design_org_uniq`, `ai_agent_schedules.kind IN ('sweep','narrative','design')`,
`ai_agent_runs.profile … 'design'`, `ai_agents.kind … 'designer'` all present in `pg_constraint`).
A `designer` agent was creatable through the UI wizard, so the W01 surface is alive; only the
fleet-design HTTP surface is dead.

#### [#5788] — PARTIAL
Migration `2026-10-16-182100-ai-origin-attribution.sql` is applied and complete against the live DB:

```
enum ai_initiator_kind = {ai_assistant, ai_agent}
script_executions.ai_initiator_kind | .ai_session_id | .ai_agent_run_id
device_commands  .ai_initiator_kind | .ai_session_id | .ai_agent_run_id
action_intents   .ai_origin_kind    | .ai_origin_session_id | .ai_origin_agent_run_id
indexes: script_executions_ai_device_created_idx, script_executions_ai_org_created_idx,
         device_commands_ai_device_created_idx
```

**Not client-forgeable (tested):** `POST /api/v1/scripts/b9687a21…/execute` with a body carrying
`"aiInitiatorKind":"ai_agent","aiAgentRunId":"1111…"` returned 201 and the resulting rows kept all
three columns NULL (`script_executions` `57260b29…` → `|||manual`; `device_commands` `43373fd1…` →
`ai_initiator_kind` NULL, `created_by` = the human admin). Attribution is server-derived only, from
`AuthContext.aiOrigin` built in `services/aiAgents/agentAuthContext.ts:87` and stamped through
`services/aiOriginColumns.ts` at the two insert chokepoints (`commandQueue.ts:537`,
`scriptDispatch.ts:571`); `writeAiCommandAudit` (commandQueue.ts:131) emits `ai.command.executed`
with `initiatedBy:'ai'` for every AI-origin dispatch.
PARTIAL because no AI-initiated *mutation* could be produced: the one real LLM run I triggered ran in
shadow mode and proposed no device action, so no `ai.script.executed` audit row or `action_intents`
row with `ai_origin_*` populated exists to inspect.
Observation: the execute route silently ignores the unknown `aiInitiatorKind`/`aiAgentRunId` keys
rather than 400-ing (non-strict schema) — harmless here since they are never read, but worth noting.

#### [#5790] — PASS
Fixture: created ticket **T-2026-0002** (`aa8d2f23-74f1-4ba7-a9f5-1b5249550b3e`, Default
Organization) through `/tickets/new`, then seeded one `ai_agent_runs` row
(`11111111-2222-4333-8444-555555555555`, `profile='triage'`, `status='completed'`, `finished_at`
set, `ticket_id` = that ticket, `outcome.ticketProposal` per `TicketTriageProposal` + one
`ticketTriageSkipped` entry).

- `GET /api/v1/tickets/<id>/ai-proposal` → 200 with the mapped DTO (summary, fields.priority
  `{value:"high",confidence:0.91}`, draftReply, notes, skipped).
- Ticket detail renders `[data-testid="ai-agent-run-triage"]`: "Ticket triage / PROPOSED FIELDS
  Priority: high 91% confidence / DRAFT REPLY / NOTES / NOT APPLIED — Device link: No device was
  proposed."
- Clicked `[data-testid="ai-agent-run-triage-post-note"]` → toast **"Posted as a private note"**
  (captured with a MutationObserver on `[data-testid="toast"]`).
- DB: `ticket_comments` `b598ada3-…` → `origin_principal_kind='user'`,
  `user_id=29054da9-…` (Breeze Admin), `proposed_by_run_id=1111…`, **`agent_run_id` NULL** — the
  helpdesk loop guard's meaning is preserved exactly as `-182500`'s header describes.
- Thread shows it as "Breeze Admin · Internal", i.e. posted as the technician.
- **Unique constraint from `-182600` works:** a second
  `POST /tickets/<id>/ai-proposal/post-note` with the SAME `runId` returned **201** with the
  *existing* `commentId` `b598ada3-…` and `select count(*) from ticket_comments` stayed at **1** —
  `postProposalNote()` catches the `ticket_comments_one_proposal_note_per_run_uq` violation and
  returns the existing row, exactly the documented recovery shape.
- RunDetailPage (refactored in this PR) renders the real run fine: `run-detail-page`,
  `-header`, `-summary`, `-meta`, `-trace` with 9 trace entries, `-ledger-cards` with 4 cards.

#### [#5738] — PARTIAL (artifact plane wired; capture is dark on this stack)
`ai_run_artifacts` exists with the full column set (`id, org_id, run_id, session_id, kind, name,
content_type, bytes, sha256, blob_key, head_preview, tail_preview, source_device_id, created_by_tool,
expires_at, created_at`), RLS **enabled + forced**, and the `ai_artifact_kind` enum
(`input_capture, step_script, step_stdout, output, report`) is present.
Both endpoints are alive and correct:

```
GET /api/v1/ai/agents/runs/7eba9b24…/artifacts        -> 200 {"data":[]}
GET /api/v1/ai/artifacts/11111111-1111-4111-8111-…    -> 404 {"error":"Artifact not found","code":"ARTIFACT_NOT_FOUND"}
```

No artifact could be captured: `services/artifacts/toolResultCapture.ts:122` short-circuits
(`if (!aiWorkspaceEnabled()) return raw;`) and `aiWorkspaceEnabled()` is false on this stack
(`BREEZE_AI_WORKSPACE_ENABLED=false`, and `ARTIFACT_BLOB_BACKEND` / all `ARTIFACT_S3_*` are empty).
The real 10-turn run produced 0 artifact rows, as designed for a flag-off deployment.
Large-result capture / run-detail linkage therefore not provable here.

#### [#5736] — BLOCKED (expected: no sandbox backend)
`BREEZE_AI_WORKSPACE_ENABLED=false` in the API container, so `aiWorkspaceEnabled()`
(`config/env.ts:194`, the AND of `isHosted()` + both flags) is false and no workspace tool runs.
Server-side evidence that W02 landed:
- `ai_run_workspaces` exists with the full shape (`backend, provider_ref, region, bootstrap_hash,
  status, ready_at, destroying_since, destroyed_at, deadline_at, cpu_ms, wall_ms, mem_allocated_mb,
  compute_cents, staged_bytes, artifact_bytes, step_count, steps, destroy_attempts, last_error`),
  RLS enabled + forced, `backend` CHECK `IN ('vercel','gvisor_pool','agentcore','fake')`.
- Compute columns present: `ai_agent_runs.compute_cpu_ms/.compute_wall_ms/.compute_cents/
  .compute_reserved_cents/.workspace_id`, `ai_cost_usage.compute_cents`,
  `ai_sessions.total_compute_cents`.
- Pricing + reaper code present: `services/aiComputePricing.ts` (`COMPUTE_PRICING` per backend,
  `AI_COMPUTE_PRICE_MULTIPLIER` env), `services/workspace/{sandboxBackend,vercelSandboxBackend,
  fakeSandboxBackend}.ts`, `jobs/workspaceReaper.ts`.
- `config/validate.ts:1349–1979` refuses `BREEZE_AI_WORKSPACE_ENABLED=true` without
  `IS_HOSTED=true`, a `vercel` backend, Vercel credentials and a regional artifact bucket — i.e.
  the flag fails at boot rather than at the first run. Clear gating, as specified.
The completed run recorded 0 workspace rows and NULL compute columns.

#### [Everyday] Tickets list — FAIL (cross-org count vs. list)
`/tickets` opens on the **All open** tab (`border-primary`, confirmed by className; "My tickets" is
NOT the active tab) showing **"All open 1"** / **"Unassigned 1"** while the body renders the
zero-data empty state **"No tickets yet — Tickets arrive from the customer portal…"**.
Root cause found and reproduced at the API, with the SAME orgId on both calls:

```
GET /api/v1/tickets?statusGroup=open&limit=100&orgId=8852612b-…  -> {"data":[],"pagination":{…,"total":0}}
GET /api/v1/tickets/stats?orgId=8852612b-…                       -> {"data":{"open":1,"unassigned":1,"mine":0,…}}
```

`apps/api/src/routes/tickets/tickets.ts:189–238` (`GET /stats`) never reads the `orgId` query param
— it builds its predicate purely from `buildScopeConditions(auth)`, which for a **partner**-scope
token filters on `partnerId` + `accessibleOrgIds` (ALL orgs). The list route honours `orgId`. The
only ticket at the time was Group 1's `QA sweep ticket count check` in **Zulu Widgets Ltd** while
the selected org was **Default Organization**. After I created T-2026-0002 in Default Organization
the tabs read "All open **2**" with **1** row listed — same mismatch, now visible with data.
Answer to Group 1's question: it is the **All open** tab (the default), and the discrepancy is the
org filter, not the tab. Secondary issue: the empty state uses the zero-data copy ("No tickets yet")
where a filtered-empty state copy belongs.
**Fix candidate:** `apps/api/src/routes/tickets/tickets.ts` `GET /stats` — accept and apply the
`orgId` query param (validated through `auth.canAccessOrg`) the way `/triage-evaluation`
(same file, line ~257) already does.

#### [Everyday] Ticket comment, run detail, AI usage — PASS
- Posted an ordinary public reply on T-2026-0002 via `[data-testid="ticket-composer-send"]`; it
  appears in the thread immediately as "Breeze Admin" (no toast — the composer is not a `runAction`
  caller, but the rendered comment is the confirmation).
- `/ai-agents` runs list: both runs render with Agent / Status / Trigger / Verdict / Duration
  columns and a Status filter that includes **Skipped**.
- `/ai-agents/runs/<id>`: full render, no console errors.
- `/settings/ai-usage`: Today's Cost **$0.16**, Monthly **$0.16**, Messages Today **10**, Tokens
  **111.9K** (110.2K in / 1.7K out) — matches the run I triggered.
- Ticket detail at 400 px: `/tickets/<uuid>` renders the proposal card (334 px) and composer
  (366 px) with **zero** horizontally-overflowing elements and `scrollWidth === clientWidth === 400`.

#### Paper cuts
1. **`enabled` + `mode:'off'` agent shows the green "Running" badge.** `AiAgentsPage.tsx:296`
   `runningLabel(enabled)` never consults `mode`, so the list says "Off · Running" for a row that
   `runService.ts:882` will skip with `mode_off`. Directly against #5681's "make skips visible" aim.
2. **The create wizard silently downgrades a Fleet designer to `mode:'off'`.** Step 1 defaults to
   Shadow; `allowedModesForKind('designer')` is `['off','act']` (shared/types/aiAgents.ts:13), so
   picking "Fleet designer" drops the draft to Off with no explanation, and the review step's
   "Start enabled" then creates an enabled-but-inert agent. The DTO's `supportedModes` still
   reports the generic `['off','shadow','act']`, so the client can't even render the reason.
3. **Fleet Design's error copy hides an auth failure.** A 401 renders as the generic
   "Failed to load Fleet Designs." with no status, no retry and no sign-in prompt.
4. **`/settings/ai-usage` → Recent Sessions under-reports an agent run to $0.00 / 0 turns.**
   The `ai_sessions` row created for run `7eba9b24…` has `turn_count=0`,
   `total_cost_cents=0.000000`, `title` NULL ("Untitled"), while the run row itself carries 10 turns
   / 16 cents and the page header (fed by `ai_cost_usage`) correctly shows $0.16 / 10 messages.
5. **`ai_agent_runs.resolved_model` is NULL after a successful 10-turn run** (also
   `compute_cpu_ms`/`compute_wall_ms` NULL, expected with the workspace off). The runs list has to
   fall back elsewhere for the model name; `/settings/ai-usage` shows "claude sonnet" from the
   session row instead.
6. **"Post as private note" stays enabled after a successful post** with no posted/disabled state,
   so a second click re-toasts success (harmless — the server is idempotent per run — but it reads
   as a second note having been written).
7. **A shared `/tickets#T-2026-0002` link shows nothing on a phone.** At 400 px the split-view
   detail pane is present in the DOM but zero-sized; only the queue renders, with no indication a
   ticket was selected. Clicking the row navigates to `/tickets/<uuid>`, which renders correctly —
   so the hash form just doesn't have a mobile branch.

#### Fixtures left behind (Group 3)
- Ticket **T-2026-0002** `aa8d2f23-74f1-4ba7-a9f5-1b5249550b3e` (Default Organization) with 2
  comments: the proposal note `b598ada3-…` (`proposed_by_run_id` set) and one ordinary public reply.
- `ai_agents`: **QA G3 Fleet Designer** `8c774c90-c5e6-44af-a612-a1f72671f746` (partner-wide,
  kind `designer`, enabled, mode `off`) and **QA G3 Triage**
  `99feae33-e0cd-44e6-9c4c-08aee29075ee` (partner-wide, kind `triage`, enabled, mode `shadow`).
- `ai_agent_runs`: seeded row `11111111-2222-4333-8444-555555555555` (hand-written triage proposal,
  agent_id = the designer agent) and the REAL run `7eba9b24-4aec-47be-929d-de55ae7e8560`
  (QA G3 Triage, completed, 10 turns).
- `script_executions` `57260b29-3ae0-4648-8bd8-2e674afe6646` + `device_commands`
  `43373fd1-92dc-478e-9e74-1c4e5858429d` — a queued "Flush DNS Cache" on `e2e-windows.local`
  (offline, will never run) used for the #5788 forgery test.
- `ai_sessions` `77bdced0-a77e-4897-8651-dbaef14c607c` (closed) from the real run.

#### LLM spend caused
One real Anthropic call chain: agent run `7eba9b24…`, 10 turns, 111.9K tokens
(110.2K in / 1.7K out), **$0.16** — visible on `/settings/ai-usage`.

### Group 4 — Portal Hardware Lifecycle + reports — opus, 2026-09-14 21:10 UTC

Logins used: web `admin@breeze.local` (browser, 1×; plus 2 curl logins for API rows), portal `portal@breeze.local` / `PortalTest123!` (browser, 2× — the MCP browser context reset mid-run). Portal credentials come from `e2e-tests/seed-fixtures.sql` (argon2 hash seeded, plaintext documented in the comment at line ~68); no portal user had to be created.

#### [#5701] — PARTIAL

Report itself is excellent; the operator-facing purchase-date field is read-broken.

- Created the report from **Reports → Templates → "Hardware Lifecycle Report" → Use template** (options modal: replace-after years, server years, include hand-entered assets, list other equipment). Toast: `Created "Hardware Lifecycle Report". Generate it from the Reports list.` — note the row brief's path (`/reports/new` → type "Hardware Lifecycle") does **not** work: `/reports/new` only offers Devices/Alerts/Patches/Compliance/Activity. Templates is the only entry point.
- **Generate now** → `POST /api/v1/reports/:id/generate` 200, run `completed`, `rowCount=2`.
- Snapshot arithmetic verified against the seeded fixtures: macOS (purchase 2020-01-15, manual, no warranty) → `ageYears 6.7`, `replaceBy 2024-01-15`, `replacement: replace`, `lifeUsed 1`; Windows (purchase 2025-03-01 **vendor**, warranty to 2029-03-01) → `replacement: supported`. Recommendation prose generated.
- **PDF export PASSES.** Downloaded `hardware_lifecycle-report-2026-09-14.pdf` (22 KB, 2 pages, client-rendered from the stored snapshot). Extracted text confirms: partner header band, "At a glance" counts, replacement schedule (Now / After Sep 2027), plan table with quarter timeline, the `*` footnote "Purchase date taken from the manufacturer's ship record.", and the "What we recommend" page.
- **FAIL within the row — purchase date does not round-trip to the UI.** `Device Settings` (device actions menu → Device Settings) has the field (`data-testid="device-purchase-date"`, help text "Used by the Hardware Lifecycle report to plan replacement."). Writing works: typed `2019-06-01`, saved, DB shows `purchase_date=2019-06-01, purchase_date_source='manual'` (source mirror correct). **Reading does not**: reload + reopen → field is blank. Root cause verified at the API: `GET /api/v1/devices/:id` and `GET /api/v1/devices` both return **no `purchaseDate` / `purchaseDateSource` key**. The list query *selects* them (`apps/api/src/routes/devices/core.ts:964-965`) but the response mapper at `core.ts:1154-1210` never emits them — the exact "list fields get silently dropped (#800/#1273/#2138)" class the comment at `core.ts:1199` warns about. Consequences: an operator can never see or correct a stored purchase date, and the vendor-provenance hint in `DeviceSettingsModal.tsx:159` (`device.purchaseDateSource === 'vendor'`) is dead code in practice.
  - Repro: set `devices.purchase_date` for any device (SQL or via the modal), reopen Device Settings → blank. `curl -H "authorization: Bearer $TOK" /api/v1/devices/<id>?orgId=<org> | grep -i purchase` → nothing.
- No toast on **Generate now** and the row's "Last generated" stays `Never` until a manual page reload (`handleGenerate` in `ReportsList.tsx:160` is raw `fetchWithAuth`, no `runAction`, no success message, and only `fetchRecentRuns` is refreshed — not the reports list). Pre-existing for all report types, not introduced here.

#### [#5763] — PASS

- **Flag OFF (seeded default)**: `/portal/reports` renders without the Hardware lifecycle card (`[data-testid="reports-lifecycle-card"]` absent, gated in `apps/portal/src/pages/reports/index.astro`), and `/portal/reports/lifecycle` redirects to the portal home (`/portal/quotes`). No dead page, no 500.
- **Toggling ON**: Settings → Organizations → Default Organization → **Customer Portal** → Visibility → "Hardware lifecycle" (`data-testid="org-portal-toggle-enableLifecycle"`) → Save portal settings → toast `Portal settings saved`; DB `portal_branding.enable_lifecycle=true`. The save also provisioned the missing `Customer portal — Hardware Lifecycle` report definition (`portal_self_service=t`) via `onPortalFlagsChanged`.
- **Flag ON, not yet generated**: `/portal/reports/lifecycle` renders the empty state "No hardware lifecycle plan yet / We have not generated your hardware lifecycle plan yet." + a `lifecycle-refresh` button. Clicking Refresh generated the run and re-rendered the page in ~5 s.
- **Toggling OFF again**: card disappears, `/reports/lifecycle` redirects again, and the already-generated run disappears from the portal ledger ("No reports yet") — matching the deliberate `ne(reports.type,'hardware_lifecycle')` visibility filter.
- Portal PDF download from the run ledger works (27.9 KB `%PDF-`); CSV link also offered.

#### [#5785] — PASS

`/portal/reports` → Hardware lifecycle card → `/portal/reports/lifecycle` renders, from the latest run: "As of Sep 14, 2026, 9:06 PM" + Refresh, the status bar (1 Replace now / 1 On track with the proportional bar), the sentence "The oldest computer due for replacement is 6 years old.", the upcoming schedule (Now → E2E macOS Test Device; After Sep 2027 → 1 computer), the "Device replacement plan" table (computer / OS / age / purchased / warranty / status / timeline, with the ship-record footnote) and the recommendation bullet. Card href `/portal/reports/lifecycle` correct.

#### Paper cuts

- **Mobile (400px) plan table does not actually stack.** `apps/portal/src/components/lifecycle/LifecyclePlanTable.tsx:81` sets `min-w-[52rem]` on the table, which defeats the `block sm:table-cell` mobile-stacking classes on every cell: at 400px the table is 832px inside an `overflow-x-auto` parent, so Purchased / Warranty / **Status** / Replacement timeline are off-screen and only reachable by horizontally scrolling the table with no visible affordance. The page itself does not scroll horizontally (`documentElement.scrollWidth == clientWidth == 400`), so it is a legibility cut, not a layout break — but "Replace now" is the one thing a customer opens this page for.
- **Customer-facing raw OS string**: both the portal page and the PDF print `Windows 11.0.22631` verbatim. macOS renders as `macOS 14.5`. A customer-ready replacement plan should say "Windows 11 (23H2)". Same build also classifies as `osSupport: unclassified` (the conservative-by-design classifier in `packages/shared/src/utils/hardwareLifecycle.ts` does not recognise `11.0.22631`), so a mainstream, fully-supported Win11 fleet shows no OS-support signal at all.
- **"Generate hardware lifecycle plan" button is always visible in the portal** even with `enableLifecycle` off (documented as deliberate in `ReportRunList.tsx`), and clicking it surfaces the developer-flavoured error **"Portal report definition not found."** in a customer portal. Copy should be customer-language.
- **Pre-existing orgs need a portal-settings re-save** before the portal can generate a lifecycle plan: `onPortalFlagsChanged` only provisions definitions when `requested.enableReports === true` is in the PATCH body, so an org that already had Reports on before this release has no `hardware_lifecycle` definition until someone touches and saves the portal settings page. Turning the lifecycle flag on through the UI happens to fix it (the editor PATCHes the whole flag set), so this only bites API-only/scripted flag flips.
- `/reports/new` has no Hardware Lifecycle type — discoverable only via Templates. Worth a cross-link if the builder is meant to be the primary entry point.

#### Fixtures left behind

- `devices.purchase_date`: `e2e-macos.local` = `2020-01-15` (`manual`), `e2e-windows.local` = `2025-03-01` (`vendor`).
- `device_hardware` rows for both e2e devices (Apple MacBook Pro 16 / `QASWEEP2`; Dell Latitude 7420 / `QASWEEP1`) — the table was empty before.
- One `device_warranty` row for `e2e-windows.local` (Dell, active, 2025-03-01 → 2029-03-01, not a subscription).
- Reports: `Hardware Lifecycle Report` (hardware_lifecycle, 1 completed run), `QA Sweep Control Devices` (device_inventory, 1 completed run — created only as a control to prove a PDF-download browser crash was MCP flakiness, not a lifecycle bug), and the auto-provisioned `Customer portal — Hardware Lifecycle` definition with one completed portal run.
- `portal_branding.enable_lifecycle` left **false** (restored to the seeded default after the off-again check); `enable_reports` untouched (true).

### Group 7 — Fleet Design re-check + HP CMSL + join badge — opus, 2026-09-14 21:25 UTC

**Tooling note:** the Playwright MCP server (`plugin:playwright:playwright`) failed to
connect for this session — no browser was available. Everything below is
curl-against-the-live-API plus source verification; rows whose only remaining
evidence would have been a rendered page are marked accordingly. Login was
performed once (428 on first POST /auth/login, retry 200 — known noise).

#### [#5698] — FAIL

The `authMiddleware` fix is real and confirmed: `GET /api/v1/ai/fleet-design` → 200
`{"items":[]}` (previously 401). The row nevertheless fails on a **new, deeper
defect one layer in — every designer run dies at turn 0 before any LLM call.**

Repro (exactly what was done):
1. `PATCH /ai/agents/8c774c90-c5e6-44af-a612-a1f72671f746 {"mode":"act","recipients":{"userIds":["<admin>"],"roleIds":[]}}` → 200.
   (`{"mode":"shadow"}` → 400 `mode_not_allowed_for_kind`; `{"mode":"act"}` alone →
   422 `act_prerequisites_not_met: recipient`. Both are correct, well-worded refusals.)
2. `POST /ai/fleet-design/runs {"orgId":"8852612b-…"}` → **202** `{"runId":"d6a3975f-01b8-4be4-b8a3-f53bd705d64b"}`.
3. `GET /ai/agents/runs/d6a3975f-…` → `status:"failed"`, `errorCode:"run_failed"`,
   `turnCount:0`, `costCents:0`, `trace:[]`, `fleetDesign:null`, `reportRunId:null`.
   queuedAt→finishedAt = 0.5 s.

API log (`breeze-wt-qa-sweep-post-v0-113-0-api-1`):

```
[aiAgentRunLoop] agent run failed {
  runId: 'd6a3975f-01b8-4be4-b8a3-f53bd705d64b',
  errorCode: 'run_failed',
  error: Error: [createBreezeMcpServer] onlyTools referenced unknown tool name(s):
    "get_script_details" (nearest: get_device_details, get_script_execution, get_script_proposal)
      at createBreezeMcpServer (apps/api/src/services/aiAgentSdkTools.ts:3045:15)
      at driveSdkLoop (apps/api/src/services/aiAgents/runLoop.ts:1678:21)
      at executeAgentRun (apps/api/src/services/aiAgents/runLoop.ts:1932:20)
```

Root cause (verified, not inferred): `DESIGN_TOOL_ALLOWLIST`
(`apps/api/src/services/aiAgents/designProfile.ts:12`) lists `get_script_details`.
That name is a key in `TOOL_TIERS` (`aiAgentSdkTools.ts:190`) but has **no `tool()`
declaration in `createBreezeMcpServer`** — it is reachable only from the separate
`script_builder` MCP server, which `aiAgentSdkTools.mcpCoverage.test.ts`'s
`NOT_IN_BREEZE_MCP_SERVER` map already documents by name. So the design profile's
`onlyTools` hands `createBreezeMcpServer` a name it will never accept, and the
constructor throws before the first model turn. The other five floor tools
(`get_device_details`, `get_device_context`, `search_logs`,
`get_configuration_policy`, `get_playbook_history`) are all properly declared.

Why no test caught it: `designProfile.test.ts`'s "every floor tool is a read-only
catalog tool" asserts catalog membership + `readOnly`, never MCP-server
declaration — which is the property `onlyTools` actually validates at runtime.

Consequence: Fleet Design is non-functional end-to-end on this build. Apply
steps 1–3+5, the applied-items ledger and Rollback could not be reached at all,
because no design run can ever produce a `report_run` to apply. Attempting to
seed a synthetic `report_runs` row to exercise apply/ledger/rollback anyway was
blocked by the session's auto-mode classifier (DB writes denied), so those
sub-steps are untested rather than failing.

What was verified for those sub-steps without a report run:
- All five id-addressed endpoints exist and 404 uniformly on an unknown id (no
  distinct "malformed"/"forbidden" signal): `GET /:id`, `GET /:id/applied`,
  `POST /:id/apply/preview`, `POST /:id/rollback`, `POST /:id/document`.
- Web surfaces all present and wired through `runAction` with a `successMessage`
  (`FleetDesignPage.tsx`): `fleet-design-start-button`, `fleet-design-review-apply`,
  `fleet-design-header-rollback`, `fleet-design-file-document`,
  `fleet-design-download`, `fleet-design-rollback-refused`, plus per-item
  `fleet-design-item-<itemRef>` rows with an "applied" badge in place of the
  checkbox. Not rendered (no browser).

**MFA gate on Rollback — it does not block here, and that is worth recording.**
`POST /:id/rollback` carries `requireMfa()`. The container has `ENABLE_2FA=true`,
but `admin@breeze.local` has no enrolled factor and is inside the enrollment
grace window (`mfaGraceEndsAt` on the login response), so the minted access token
carries `"mfa": true` (decoded from the JWT) and `hasSatisfiedMfa()` passes.
Every `requireMfa()` route was reached successfully this session (agent PATCH,
fleet-design run trigger, schedule create). So on this stack the MFA step-up is a
**no-op, not a hard block** — the gate's denial path could not be exercised with
the available credentials.

#### [#5706] — BLOCKED

Apply step 4 (legacy intent inventory → approved scripts tagged `bundle-import`)
is unreachable for the same reason as #5698: no design run completes, so there is
no stored outcome carrying a `legacy` section to reconcile, and seeding one was
blocked. Static verification only:
- The step exists and is ordered: `apply.ts:123-128` runs `[1 stepFunctions,
  2 stepRetire, 3 stepMonitoring, 4 stepScripts, 5 stepRoleCorrections]`, each in
  its own savepoint, with `partial: { failedStep }` reported to the caller.
- Ledger schema is real: table `fleet_design_applied_items`, item kinds
  `function | retired | policy | script | role_correction` (`stepKind()`, apply.ts:83).
- Outcome item-ref scheme is deterministic (`fleetDesignOutcomeFromSubmission`,
  `packages/shared/src/validators/fleetDesign.ts:140`): `legacy:<scriptId>`,
  `automation:<functionKey>:script:<n>`, `monitoring:<functionKey>:{watch,rule}:<n>`,
  `functions:<functionKey>`, `retired:<n>`, `roleCorrections:<deviceId>`.
- Web renders it: `fleet-design-legacy-table` / `fleet-design-legacy-row-<scriptId>`
  and `fleet-design-script-content-<itemRef>` in `FleetDesignViewer.tsx`.

Not PARTIAL: the brief's PARTIAL condition was "the design run yields no legacy
scripts", but no design run yields anything at all.

#### [#5726] — PARTIAL

Schedule and counter verified live; the document hand-off could not be reached.

- **Scheduled drift — schedule persists, PASS.** `POST /ai/agents/schedules`
  `{"ownerScope":"partner","agentId":"8c774c90-…","kind":"design","cron":"0 3 1 * *","timezone":"UTC","sweepKinds":[],"enabled":true}`
  → **201**, id `91779e0b-3e7b-421c-9d8a-0fa61a6996b9`. Re-listing
  `GET /ai/agents/schedules?agentId=…` returns it (1 row, kind `design`, cron
  `0 3 1 * *`, enabled). The monthly-or-rarer rule is enforced with a readable
  message: a weekly cron `0 3 * * 1` → **400** "a design schedule fires at most
  once a month — literal minute, hour and day-of-month 1-28, month `*`, `*/N`
  or a list of months, `*` day-of-week". No on-demand drift trigger is offered,
  and a scheduled tick was not waited for (and would fail anyway, per #5698).
  UI side is wired: `AiAgentSchedulesSection.tsx` gates the kind chooser on the
  target agent's kind (`SCHEDULE_KINDS_FOR_AGENT_KIND`: `designer → ['design']`,
  everything else → sweep/narrative), with a coded `agent_kind_not_designer` error.
- **`fleetDesignsDelivered` counter — present, PASS (reads 0).**
  `GET /ai/agents/impact?orgId=8852612b-…` → 200; the field is in `totals` and in
  every day of the 30-day `series`. Backed by a real column
  (`ai_agent_impact_daily.fleet_designs_delivered`, with a `>= 0` check constraint)
  and summed in `impactQuery.ts:146`. Localized in all 8 locales
  (en: "Fleet Designs delivered"). It reads 0, which is correct — no design was
  ever delivered — so the increment path itself is unproven.
- **Org documents hand-off — not reached.** `POST /ai/fleet-design/:id/document`
  exists, is gated on `documents:write` (`requireDocumentsWrite`, matching
  `routes/orgDocuments.ts`), and the web button
  (`fleet-design-file-document`) goes through `runAction` with
  `successMessage: t("page.fileAsDocumentSuccess")`. With no report run it 404s,
  and nothing lands in Org → Documents.

#### [#5700] — PASS

Exercised against the live API on configuration policy `QA G2 CPU policy`
(`c4ff0fe6-adac-40c9-94c7-df46e1c77585`). All four D3 consent behaviours hold:

1. **Client-supplied consent is refused, not stripped.** `POST /:id/features` with
   `inlineSettings.hpCmsl.consent` (forged user id, back-dated `acceptedAt`) →
   **400** `{"code":"WARRANTY_CONSENT_NOT_CLIENT_SETTABLE","error":"HP CMSL consent
   is recorded by the server from your authenticated session. Remove
   hpCmsl.consent from the request and send it again."}`
2. **Server stamps consent on enable.** Same POST without `consent` → **201**,
   link `3c1f9b32-af94-4b83-9707-345f1f9efc6a`, stored
   `hpCmsl.consent = {eulaId:"hp-cmsl-eula-2026-04-01", acceptedAt:"2026-09-14T21:22:44.955Z",
   acceptedByUserId:"29054da9-8f9e-4359-a9b1-3352311e654f"}` — the real
   authenticated user id and the server clock, against the current
   `HP_CMSL_EULA_ID`. Consent lives as jsonb on `config_policy_feature_links`
   (no new table, so no cascade/export-policy registration is owed).
3. **An unrelated edit keeps the acceptance.** `PATCH` setting `warnDays: 45` with
   `hpCmsl.enabled` still true → 200, consent object byte-identical (same
   `acceptedAt`).
4. **Disabling drops the consent.** `PATCH` with `hpCmsl.enabled:false` → 200,
   `inlineSettings.hpCmsl` becomes `{"enabled":false}` — consent gone.

Web surfaces present in `WarrantyTab.tsx`: `warranty-tab-hp-cmsl-toggle`,
`warranty-tab-hp-cmsl-consent` (the explainer), `warranty-tab-hp-cmsl-acceptance`,
`warranty-tab-hp-cmsl-no-acceptance`, `warranty-tab-hp-cmsl-superseded` (fires when
the recorded `eulaId` differs from the current one), and
`warranty-tab-inheritance-warning`. Not rendered (no browser).

**Gate caveat (the brief's "without MFA the UI must explain").** The denial path
could not be exercised: the admin holds `devices:execute` and, per the #5698 note,
a token with `mfa: true`. Verified by reading `hpCmslGate.ts` instead — both
refusals return a coded 403 with a human-readable `error` string
(`HP_CMSL_EXECUTE_REQUIRED`: "Enabling HP warranty collection installs HP software
on the devices this policy reaches, so it requires the devices:execute permission
— the same permission a software deployment requires."; and `MFA_REQUIRED`), and
the web save goes through `runAction`, which toasts a non-2xx body. So it explains
rather than silently no-ops **by construction** — inferred, not observed.

#### [#5674] — BLOCKED (and the row's premise is wrong)

Could not be exercised: `GET /devices/<id>/management-posture` returns
`{"posture":null,"collected":false}` for **both** seeded devices (macOS
`42fc7de0-…` and Windows `e65460f3-…`). No agent has ever reported posture on this
stack, so the tab renders its not-collected empty state and no identity badge of
either kind is reachable. Seeding a posture row was blocked by the auto-mode
classifier.

Separately, the row's expectation does not match the code. **macOS is not a
stubbed platform.** `agent/internal/mgmtdetect/deep_identity_darwin.go` is a real
implementation (shells `dsconfigad -show`, reports `Source: "darwin"`). The
`unsupported` stub is `deep_identity_other.go`, built under
`//go:build !windows && !darwin` — i.e. Linux/BSD only. So on a live macOS device
the tab would show a real join type, never the unsupported badge; that is correct
behaviour, not the bug the row describes.

The #5626 fix itself is present and correct in the web layer
(`DeviceManagementTab.tsx`): `identityDetectionUnsupported = identity.source === "unsupported"`
swaps the join-type label for `t("deviceManagementTab.identityDetectionUnsupported")`
("Join Detection Not Supported"), renders the hint paragraph
(`data-testid="identity-detection-unsupported"`: "…so its join status is unknown —
it has not been confirmed as unjoined."), and passes `unknown` to all three
`BoolFlag`s (`identity-flag-azureAdJoined`, `-domainJoined`, `-workplaceJoined`) so
they render unknown rather than a false negative. A Linux device is what would
exercise it; the seeded fleet has none.

**Paper cuts**
- `GET /ai/agents` reports `supportedModes: ["off","shadow","act"]` for a
  `designer` agent, because `mapRow` (`routes/aiAgents.ts:161`) returns the flat
  `SUPPORTED_AGENT_MODES` constant regardless of kind, while the server actually
  rejects `shadow` for that kind (400 `mode_not_allowed_for_kind`). Not
  user-visible today — `ModeChoice.tsx` disables the card off
  `allowedModesForKind(kind)` rather than off the DTO — but the DTO field is
  advertising a mode the API refuses, so any other consumer that trusts it
  (an SDK, a script, a future screen) gets a 400.
- `fleet_designs_delivered` has a dedicated column, a check constraint, an API
  field and 8 locales, but no reachable path increments it on this build. Nothing
  distinguishes "no designs yet" from "designs run but the counter is broken" in
  the UI — the impact page will read a confident 0 either way.
- The designer agent's `act` prerequisite error (`act_prerequisites_not_met:
  recipient`) is exactly right in the API. Worth checking in the browser that the
  agent form surfaces *which* prerequisite is missing rather than a generic
  refusal — not verifiable here.
