# Pre-release sweep — v0.114.0 → main (2026-09-20, pass 3)

| | |
|---|---|
| Range | `v0.114.0` (`59a94162ca`, 2026-09-17 16:02Z) → `origin/main` `58f76b854` (2026-09-20 23:05Z) — 221 commits. **This pass inventories pass-2 base `b4f589a6b3` → `58f76b854` (69 PRs).** |
| Gate | Merge queue drained to 0 before the sweep base was fixed. Merged during the gate: #6469, #6418, #6420, #6421, #6424, #6425, #6427, #6428, #6429, #6430, #6431, #6432, #6433, #6434, #6426, #6439(no — see below), #6440, #6442, #6448, #6450, #6451, #6453, #6454, #6455, #6458, #6459, #6423, #6481. Waiting (rebased during the gate, CI pending, NOT in the sweep base): #6439, #6468 → follow-up rows. |
| Stack | `pnpm wt-stack up` on `qa/sweep-v0.114.0-pass3` @ `58f76b854` |
| Flags enabled | `MCP_BOOTSTRAP_ENABLED` `MCP_OAUTH_ENABLED` `BREEZE_AI_AGENTS_ENABLED` `LLM_PROVIDER_CATALOG_ENABLED` `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED` `BREEZE_AI_SCRIPT_AUTHORING_ENABLED` `BREEZE_AI_AGENTS_SWEEP_ACT_ENABLED` `TOOL_SOURCES_ENABLED` = true; `BREEZE_WORKSPACE_ENABLED=false`; `WEBAUTHN_RP_ID=localhost`; `MFA_FORCE_FOR_PARTNER_ADMIN=false`. Stack `breeze-wt-qa-sweep-v0-114-0-pass3`, baseUrl `http://localhost:33032`, admin@breeze.local (platform admin), 3 orgs / 2 devices seeded. |
| Driver | skill `pre-release-sweep` |

Prior passes in this release window: `2026-09-19-v0.114.0-to-main.md` (pass 1),
`2026-09-19-v0.114.0-to-main-pass2.md` (pass 2). This pass covers the ~120
commits that landed after pass 2, plus the 09-20 issue-fixer fan-out.

## Phase 0 findings (before the stack)

### Portal Dev E2E (hydration) — standing FAILURE on main, not flake

The job is `continue-on-error` on `pull_request` **and** was failing on the
`merge_group` runs of #6442, #6434, #6426 and #6451 — i.e. every entry that
landed, landed with this job red. It is only green where it is `skipped`
(#6481, docs-only). So it is a real break on `main` that the non-blocking flag
has been absorbing, not per-PR noise.

Two specs, both timing out at 30 s:

1. `tests/partner-sending-domains.spec.ts:48` — "The tab is offered and opens
   on its own hash". `page.getByTestId(rootTestId).waitFor()` never resolves;
   the run then reports `Target page, context or browser has been closed`.
   Partner sending domains is feature #6180, whose waves are specced but
   reportedly not implemented — so this spec may be asserting a surface that
   does not exist yet, which would make the spec the defect rather than the app.
2. `tests/multi-currency.spec.ts:97` — "org currency selection, manual invoice,
   and quote acceptance in a EUR org". Fails on
   `expect(an authenticated /api/v1 request from the app).toBeTruthy()` — the
   portal never issues an authenticated API call within 30 s, which reads as a
   hydration or session failure rather than a currency bug.

**RESOLVED — both are E2E test defects, not application defects.** Reproduced
locally against the sweep stack, then diagnosed to root cause.

**Finding 1 — `partner-sending-domains.spec.ts` asserted a testid that does not
exist.** `PartnerSendingDomainsPage.navTab()` looked for
`settings-nav-tab-sending-domains`. That string appears **nowhere** in
`apps/web/src` — the only occurrence in the repo is the page object itself.
`SettingsSectionNav` emits `<testIdPrefix>-tab-<key>`, and `PartnerSettingsPage`
passes `testIdPrefix="partner-settings"` with the camelCase `TabKey`, so the
real id is `partner-settings-tab-sendingDomains`. Verified in a live browser:
15 `partner-settings-tab-*` ids render and the sending-domains one is present
and visible. Step 1 could never have passed. **Fixed** (page object).

**Finding 2 — the same spec waited for a native `confirm()` that the app never
shows.** Step 7 registered `authedPage.once('dialog', …)`, but the tab confirms
removal through its own `ConfirmDialog` (`confirmTestId=
"sending-domains-remove-confirm"`). No native dialog event ever fires, the modal
stays open, and **no DELETE is ever issued** — confirmed against the API log,
which shows create `201`, check `202`, identity `200`, test-send `202` and no
DELETE at all. This is the repo's own documented trap (CLAUDE.md: modals are
`fixed inset-0` divs, assert on testid, not a dialog container). **Fixed**
(click the confirm testid instead of handling a dialog).

Net: steps 2–6 of this spec were already exercising the feature correctly
against a real API. The app is fine; the spec's first and last steps were wrong.

**Still open on this spec:** not yet proven green end to end. Each failed run
leaves a `partner_sending_domains` row behind, and the recommendation card only
renders in the empty state, so a re-run then fails at step 1 for a *third*,
unrelated reason. The spec is not idempotent against its own leftovers — worth
a follow-up issue. A raw `DELETE` is correctly refused by
`breeze_sending_domain_release_guard()` (provider-managed domains must be
released first), so cleanup needs the release path, not SQL.

**Finding 3 — `multi-currency.spec.ts` was a false alarm from my own session.**
Its `readAccessToken()` failures (landing on `/login`, no Bearer request) were
caused by my concurrently signed-in browser session and a tripped login rate
limiter, not by the app. Verified separately that the app does send
`authorization: Bearer …` on every `/api/v1/` call. **Re-measure this one on a
clean stack before drawing any conclusion.**

**Operational note for future sweeps:** the login limiter keys on the *caddy*
IP (`login:ip:::ffff:172.31.2.10`, `login:socket:…:<email>`), so every browser
login on the stack shares one bucket — an agent logging in repeatedly will
rate-limit `globalSetup`. Clear with
`redis-cli --scan --pattern 'login:*' | xargs redis-cli del` using
`REDIS_PASSWORD` from `.env`.

## Change inventory

Status ∈ `TODO | PASS | PARTIAL | FAIL | BLOCKED | N/A`. Every row must leave `TODO`.

| PR | Merged | Surface | Title | Route / click-path | Expected visible outcome | Prereq | Status |
|---|---|---|---|---|---|---|---|
| #6481 | Y | docs-chore | W09 — token-mode recovery follows cross-snapshot object references | N/A — plan doc only | No UI change | none | N/A |
| #6451 | Y | api-only | close the DR reconcile and bare-metal in-flight races | `POST /dr/plans/:id/execute` and BMR recovery start race | Concurrent execute/recover calls rejected cleanly; `bareMetalRecoveryInFlightIndex` unique index | live agent for a full BMR run — BLOCKED-candidate | BLOCKED |
| #6426 | Y | api-only | enqueue the SNMP immediate poll after the request transaction commits | Devices → Network → SNMP asset "Poll now" (`POST /monitoring/assets/:id/poll`) | Immediate poll no longer races the DB commit | live SNMP device — BLOCKED-candidate | BLOCKED |
| #6434 | Y | api-only | give whole-machine restores a 24h ceiling instead of a 60-minute reap | stale command reaper on a whole-machine restore command | Restore in progress past 60 min is not reaped; 24h ceiling | live agent BMR restore — BLOCKED-candidate | BLOCKED |
| #6442 | Y | web | Disk Cleanup v2 W05 — system_cleanup AI/MCP tool, docs parity, release notes | `/devices/[id]` → AI chat → ask assistant to clean disk; `/ai-risk` Approval History Feed | Chat shows a "System Cleanup" tool-call card with a labeled outcome; feed lists system_cleanup | AI chat enabled | PARTIAL |
| #6423 | Y | web | preview the inherited tax rate on draft invoices | `/billing/invoices/[id]` (draft) → summary panel | Muted hint "Tax rate inherited: X% — $Y tax, $Z total" (`invoice-tax-inherited-hint`) when a taxable line exists and no explicit rate | partner Billing tax rate configured | PASS |
| #6459 | Y | web | page site and org pickers to exhaustion instead of one server page | `/billing/invoices` org filter, `/billing/quotes`, Move to Organization org picker, New Ticket org/site select | Org/site dropdowns list all orgs/sites beyond the first server page | >1 page of orgs/sites | PASS |
| #6458 | Y | web | atomic budget reservations for /client-ai and warm technician sessions | AI chat sidebar; rapid messages at the AI budget cap | Budget-exceeded toast consistent, no double-spend | concurrent requests — BLOCKED-candidate | PASS |
| #6455 | Y | agent-only | refuse tier-2 fleet users-FK writes for agent principals | AI agent tool call to aiToolsFleet write actions by an agent principal | Rejected with guardrail error; visible in `/ai-risk` as denied | tier-2 fleet automation running — BLOCKED-candidate | BLOCKED |
| #6454 | Y | web | surface reclaimable size and scan confirmation in disk cleanup | `/devices/[id]` → Filesystem tab → Analyze Now / Cleanup Preview → confirm (`cleanup-confirm-dialog`) | Confirm dialog lists target paths; "Reclaimed: X" banner after cleanup | live agent for real bytes — BLOCKED-candidate; UI renders regardless | PARTIAL |
| #6453 | Y | web | mount the file Restore Wizard so restores are not API-only | `/backup` → snapshot row → "Restore" | Multi-step Restore Wizard modal opens (picker → destination → confirm) | live agent + snapshot — BLOCKED-candidate for full restore; wizard renders without | PARTIAL |
| #6450 | Y | docs-chore | keep noeviction, correct the allkeys-lru claim, document recovery | Docs `/deploy/environment`, `/reference/troubleshooting` | Docs state Redis uses `noeviction` with recovery procedure | none | N/A |
| #6448 | Y | agent-only | anchor fully-literal cleanup rules above the target | Agent Disk Cleanup rule engine | Literal-path rule anchors correctly | live agent — BLOCKED-candidate | BLOCKED |
| #6440 | Y | web | name the missing prerequisite on execute; stop half-applying plan saves | `/dr` → plan card → Execute → rehearsal/failover/failback | Execute error names the missing prerequisite; invalid step save does not partially persist | DR plan missing a prerequisite | PASS |
| #6433 | Y | api-only | hold an active chain's full snapshot against retention | backup retention job; `/devices/[id]` → Backup tab snapshot list | Full snapshot anchoring an active chain is retained | retention run on active chain — BLOCKED-candidate | BLOCKED |
| #6432 | Y | api-only | give the bundled coturn a certificate so TURNS on 5349 works | remote session over TURN; compose/.env.example TURN TLS vars | Remote session connects via `turns:` 5349 | real network topology + cert — BLOCKED-candidate | BLOCKED |
| #6431 | Y | web | persist a terminal BMR failure reason; filter template/oneshot units in the service probe | `/devices/[id]` → Backup tab → Bare-Metal Recovery bootstrap section | Failed BMR bootstrap shows persistent specific reason after refresh | live agent BMR failure — BLOCKED-candidate | PASS |
| #6430 | Y | agent-only | preserve and restore Windows Hidden/System/Sparse file attributes | agent backup/restore engine | Attributes preserved | Windows agent — BLOCKED-candidate | BLOCKED |
| #6429 | Y | agent-only | skip the doomed HTTP result submission for WS-direct backup_run | agent heartbeat result reporting | No spurious HTTP POST | live agent — BLOCKED-candidate | BLOCKED |
| #6428 | Y | web | schema-validate `maintenance` inlineSettings on the feature-link route | `/configuration-policies/[id]` → Maintenance tab → save invalid inline settings | Clear 400 validation error instead of silent persist | admin editing a policy | PASS |
| #6427 | Y | agent-only | 16 MiB+ uploads to MinIO over HTTPS, and verification of an incomplete snapshot | agent S3 provider | Large upload succeeds; incomplete snapshot flagged | MinIO HTTPS + live agent — BLOCKED-candidate | BLOCKED |
| #6425 | Y | agent-only | stop duplicating the system-state failure reason in errorLog | agent backup errorLog | Reason appears once | live agent — BLOCKED-candidate | BLOCKED |
| #6424 | Y | api-only | fail a backup job loudly when the strict queue schema rejects its result | agent WS submits malformed backup_run result | Job marked failed with clear error on Backup tab | live agent — BLOCKED-candidate | BLOCKED |
| #6421 | Y | web | stop the MonitorEditor owner-scope hydration mismatch | `/alerts/monitors/[id]` — hard refresh | No hydration-mismatch console warning / flash of wrong owner-scope UI | none | PASS |
| #6420 | Y | api-only | flag pure returned tool errors as isError and ledger them as failures | `POST /mcp` — tool returning an error payload | `isError: true`; ledgered as failure in `/ai-risk` | MCP client session | PARTIAL |
| #6418 | Y | agent-only | resolve the trash directory without depending on $HOME | agent delete-to-trash | Works without $HOME | service-account agent — BLOCKED-candidate | BLOCKED |
| #6469 | Y | api-only | refuse recovery for snapshots that reference older snapshots' objects | `POST /backup/bmr/:id/recover` on a referencing snapshot | Clear rejection | crafted storage state — BLOCKED-candidate | BLOCKED |
| #6422 | Y | web | Billing Profiles W03: minimums and rounding land on time entries | `/tickets/[id]` → Time & Billing (log time), `/timesheet` | Entries round/apply configured billable minimum | org Billing Profile with minimums/rounding | PASS |
| #6032 | Y | agent-only | derive status path from session config and add agent fallback | Breeze Helper desktop app | Tray status correct | Helper installed — not browser | BLOCKED |
| #6416 | Y | api-only | the converter: preview, equivalence, ledger lifecycle, routes, onboarding | `POST /monitor-definitions/:id/conversion/preview` and `/apply`; no web UI | Equivalence report; ledger entry on apply | API only this wave | PASS |
| #5841 | Y | docs-chore | bump @sentry/react-native 7 to 8 | apps/mobile | none web | mobile build | N/A |
| #5847 | Y | web | bump @simplewebauthn/server + /browser 13 → 14 | `/settings/profile` → passkey registration; login passkey step | Passkey register/use succeeds | platform authenticator (CDP virtual) | PASS |
| #6114 | Y | web | show device names instead of {{device}} in alert titles | `/alerts` list, `/alerts/[id]`, dashboard AlertsFeed, Org Overview | Real device name in titles, no literal `{{device}}` | an active alert | PASS |
| #6419 | Y | docs-chore | bump the marketing version to 1.0.1 | apps/mobile | none | none | N/A |
| #6404 | Y | agent-only | W05 lab-proof defects — mounted-image convert, unsized VHDX target, refused status | agent bare-metal rebuild | Conversion completes; refused status accurate | live agent VHDX — BLOCKED-candidate | BLOCKED |
| #6417 | Y | docs-chore | link the iOS App Store listing | Docs `/features/mobile` | Working App Store link | none | N/A |
| #6414 | Y | agent-only | A-W06 agent-first coverage burn-down — 13 Tier-1 reads + MCP_COVERAGE contract | AI chat / MCP client Tier-1 read tools | Tier-1 read questions succeed via dedicated tool | AI chat enabled | PASS |
| #5844 | Y | docs-chore | bump @sentry/cli 2.58.4 → 3.7.0 (apps/mobile) | apps/mobile | none | none | N/A |
| #6399 | Y | web | dedicated ai_sessions:use permission so org roles can open AI chat | Header AI chat entry; Settings → Roles → Permissions | Custom org role with `ai_sessions:use` opens AI chat; without it entry disabled/hidden, no confusing 403 | admin creating a custom role | PASS |
| #6406 | Y | api-only | B-W01 additive spec catch-up — version negotiation, title/annotations/_meta, structuredContent | `POST /mcp` initialize | Protocol version negotiated; titles/annotations returned | external MCP client | PASS |
| #6393 | Y | web | W02 — billing profiles cut-over: Rates screen, resolver, stamping, conversion, override gate | `/settings/billing` → Rates tab (`billing-settings-tab-rates`) | New Rates tab with billing profiles, editable and saved | partner admin | PASS |
| #6411 | Y | web | move-org site picker asks for the route's full page of sites | `/devices/[id]` → Move to Organization → destination site dropdown | Full page of sites listed for the target org | org with many sites | PASS |
| #6405 | Y | api-only | tool capture/eval harness sends each surface's production system prompt | internal dev scripts | n/a | none | N/A |
| #6410 | Y | api-only | network_check raises one alert per check on its alert device, online or not | alert worker for network_check; `/alerts` list | Exactly one alert per failing check | live network fault — BLOCKED-candidate | BLOCKED |
| #6409 | Y | api-only | network_check compiler writes expectedStatus, the key the agent reads | monitor compiler | Agent evaluates status correctly | live agent — BLOCKED-candidate | BLOCKED |
| #6119 | Y | e2e-only | run the production-browser gate when topology or web build files change | CI workflow | Added check on PRs | CI only | N/A |
| #6386 | Y | web | Disk Cleanup v2 W04: OS-native cleaners | `/devices/[id]` → Filesystem tab → Cleanup Preview | OS-native cleaner categories with sizes; reclaimed bytes after run | live agent — BLOCKED-candidate for numbers; UI renders | PARTIAL |
| #6129 | Y | api-only | purge a deleted device's collection evidence once its withdrawal is published | topology retention worker after device delete | Evidence purged | device with topology history | PASS |
| #6400 | Y | web | Move to Organization dialog with server-driven step-up | `/devices/[id]` → actions → Move to Organization | Dialog with org/site pickers; step-up prompt when configured before move commits | admin; step-up needs MFA/passkey | PASS |
| #6402 | Y | docs-chore | alerting consolidation D28–D29 + CLAUDE.md org-merge registry row | docs | none | none | N/A |
| #6115 | Y | web | logical maps, shared templates, layout and explicit diagnostics | `/devices/network/[id]` → Topology section | Topology map with layout and diagnostics panel | network device with topology data; `TOPOLOGY_DISABLED` unset | FAIL→FIXED (a4ac2b578) |
| #6395 | Y | api-only | conversion ledger tables, retirement columns, retired rows excluded everywhere | `/configuration-policies/[id]` Monitors tab | Retired legacy rule not listed | converted org | PASS |
| #6394 | Y | web | composite kind, restart parameters, consecutiveFailures 100, monitors-link inheritance, resolver honours replace | `/alerts/monitors/new` or `/alerts/monitors/[id]` → kind "Composite"; restart_service params; consecutiveFailures | Composite kind with sub-conditions; restart_service parameter inputs; consecutiveFailures up to 100 | admin | PARTIAL |
| #6390 | Y | web | delivery preview, Notify shows the resolved answer, manage_delivery AI tool, docs | `/alerts/delivery` preview; MonitorEditor Notify step | Live preview of resolved channel/escalation; Notify step shows resolved answer, not raw IDs | admin | PASS |
| #6392 | Y | api-only | routing API rejection pinned, legacy docs notices, 09-08 spec superseded | `POST/PUT /alerts/routing` site-scoped payload | Pinned rejection; docs legacy notices | admin via API | PASS |
| #6387 | Y | web | Delivery page (Channels · Routing · Escalation policies), old URLs redirect | `/alerts/delivery`; old `/alerts/channels`, `/alerts/routing-rules` | Old URLs redirect; three sections render | admin | PASS |
| #6385 | Y | web | one delivery resolver, explicit "Everything else" routing rows, all-channels fallback removed | `/alerts/delivery` → Routing | Explicit "Everything else" catch-all row per severity | admin | PASS |
| #6389 | Y | e2e-only | MonitorsListPage waits for a fetched row, not the page shell (flake) | test only | n/a | CI | N/A |
| #6388 | Y | docs-chore | alerting consolidation D22–D27 | docs | none | none | N/A |
| #6381 | Y | web | freeze legacy alert authoring, Create monitor + Recommended on the policy Monitors tab | `/configuration-policies/[id]` → Monitors tab → Create monitor, Recommended; `/alerts/rules/[id]` | Legacy editor shows frozen notice and blocks edits; Create monitor works; Recommended list | admin | PASS |
| #6366 | Y | web | Disk Cleanup v2 W03: tab completion and consolidation | `/devices/[id]` → Filesystem tab | Consolidated cleanup UI: preview, run history, snapshot panels | live agent for data — BLOCKED-candidate; UI renders | PASS |
| #6380 | Y | api-only | restart_service response survives normalization and drives the agent watch | automation runtime | Result flows to watch/alert state | live agent — BLOCKED-candidate | BLOCKED |
| #6378 | Y | api-only | monitors of kind offline fire through the offline-transition path | offlineAlertEffects | Offline monitor fires alert on `/alerts` | device going offline — BLOCKED-candidate | BLOCKED |
| #6377 | Y | api-only | resolveMonitorsForDevice honours assignment role/OS filters | monitorResolver | Role/OS-scoped monitor not applied to other devices | filtered assignment + mixed devices | PARTIAL |
| #6384 | Y | docs-chore | alerting consolidation D21 | docs | none | none | N/A |
| #6383 | Y | docs-chore | pre-release sweep pass 2 tracking doc | docs | none | none | N/A |
| #6028 | Y | api-only | add canonical graph foundation | `GET /topology/graphs` | Canonical graph data | API only this wave | FAIL→FIXED (a4ac2b578) |
| #6374 | Y | docs-chore | alerting consolidation — spec, diagrams and six wave plans | docs | none | none | N/A |
| #6324 | Y | api-only | stop tool-output redactor wiping non-secret keys | AI chat tool-call output | Non-secret fields retained next to real secrets | AI tool with secret-like key names | PASS |

### ENV VARS (from diffs)

- `BREEZE_AI_AGENTS_POLICY_DECIDE_ENABLED` — #6366; ON in sweep .env.
- `BREEZE_AI_GUARDRAILS_CORE` — #6414; core guardrails gate for Tier-1 tools (not a boolean flag in .env; default path).
- `MCP_OAUTH_ENABLED` — #6406; ON.
- `TOPOLOGY_DISABLED` — #6028 kill switch; unset (topology enabled).
- `BREEZE_MCP_TOOL_NAMES`, `BREEZE_VERSION`, `BREEZE_APP_DB_PASSWORD`, `TOOL_SOURCES_ENABLED` — context-only, not new gates.

### GROUPS

- **G1 Alerting & Monitors** — #6387, #6385, #6390, #6394, #6381, #6421, #6114, #6428, #6377, #6395 (web-visible part)
- **G2 Billing / Ticketing / Pickers** — #6393, #6422, #6423, #6459, #6411, #6400
- **G3 Disk Cleanup + Backup & DR (web)** — #6366, #6386, #6454, #6453, #6440, #6431
- **G4 AI / MCP / Auth / Topology** — #6399, #6442, #6414, #6324, #5847, #6115, #6028, #6406, #6420
- **Non-browser** — all agent-only, api-only backend, docs-chore, e2e-only, mobile rows: verified by CI unit/integration suite on the merge SHA; marked N/A (docs/CI/mobile) or BLOCKED (live agent/provider) in the summary.

## Sweep log (append as you go)

### G1 Alerting & Monitors

Stack `http://localhost:33032`, admin@breeze.local (platform admin / partner scope).
Prerequisite data created during the sweep: notification channel "QA Sweep Email
Channel", routing rule "QA Sweep High Routing", escalation policy "QA Sweep
Escalation", config policy "QA Sweep Policy" (org-scoped, Default Organization),
monitor "QA Sweep Monitor" (created → edited → deleted), two `{{device}}`-titled
alerts and two legacy `alert_rules` rows (one live, one `retired_at` set) inserted
by SQL.

| PR | Status | Evidence |
|---|---|---|
| #6387 | PASS | `/alerts/delivery` renders all three sections (Channels · Routing · Escalation policies). `/alerts/channels` and `/alerts/routing-rules` both land on `/alerts/delivery` (opaqueredirect on a `redirect:'manual'` fetch; browser navigation ends on the Delivery URL). |
| #6385 | PASS | Routing shows one explicit catch-all row — "Everything else / All alerts / Send to: Inbox only / Catches every alert no row above matches. Empty its channels for inbox only." With no rule matching, the preview answers "Critical → Do not notify / No delivery row is configured" — i.e. no implicit all-channels fallback. **Inventory expected-outcome is wrong**: the source models ONE default row (`isDefault`, priority 1000000 — `RoutingSection.test.tsx`, `RoutingRuleDrawer.tsx`), not one per severity. Source wins. |
| #6390 | PASS | "Test this rule set" on `/alerts/delivery` (testids `delivery-preview-severity/kind/site`) resolved "High → QA Sweep Email Channel / Organization rule: QA Sweep High Routing" after the rule was created. MonitorEditor Notify step shows the same resolved sentence (`delivery-preview-result`), not raw IDs. |
| #6394 | PARTIAL | Composite kind present in the picker on `/alerts/monitors/new` ✓. `consecutiveFailures` on `network_check` renders `min=1 max=100` and accepts 100 ✓. **restart_service parameter inputs do not exist in the MonitorEditor** — the "Execute Command" response offers only a free-text Command field, and `grep -rn restart_service apps/web/src` finds nothing in monitoring. Reading the PR body, that is by design: #6394's restart work is API-side (`automationRuntime.restartService.test.ts`, `maxAttempts`/`cooldownSeconds` on the `execute_command` response), "no UI change". Composite likewise has no sub-condition editor — `monitorKindFields.ts:238-243` says "W05c1: the composite kind exists in the shared enum … W05c2 supplies the children editor". Inventory's expected outcome overstates both; the web-visible half passes. |
| #6381 | PASS | `/configuration-policies/{id}` → Monitors tab has `monitors-tab-create` ("Create monitor") and `monitors-tab-recommended` ("Recommended monitors" + "Attach all built-in monitors") ✓. Legacy authoring frozen: `/settings/alert-templates` shows the banner "New alert templates are created as monitors — Existing templates stay editable here and convert in the next release / Open Monitors". `/alerts/rules/{id}` does not render a frozen notice — `apps/web/src/pages/alerts/rules/[id].astro` is a bare `Astro.redirect('/configuration-policies', 301)`, verified in browser. Stronger than the inventory's description, so still PASS. |
| #6421 | PASS | Hard load of `/alerts/monitors/605bf267…` (partner-wide built-in) and of an org-scoped monitor: 0 console warnings/errors, and 20 samples over the first 3 s show one stable owner-scope render ("Partner-wide / Built-in") — no flash to the org variant. |
| #6114 | PASS | Inserted `{{device}} CPU above threshold` (high) and `{{device}} disk critical` (critical) via SQL. `/alerts` list, `/alerts/{id}`, dashboard AlertsFeed and Org Overview "Open critical alerts" all render "E2E Windows Test Device …" / "E2E macOS Test Device …"; `innerText.includes('{{device}}')` is false on all four surfaces, message bodies substituted too. |
| #6428 | PASS | API: `POST /api/v1/configuration-policies/{id}/features` with `inlineSettings:{recurrence:'sometimes',durationHours:-5,startTime:'99:99'}` → **400** `{"error":"Invalid maintenance settings","details":{"formErrors":["Unrecognized key: \"startTime\""],"fieldErrors":{"recurrence":["Invalid option: expected one of \"once\"…"],"durationHours":["Too small: expected number to be >=1"]}}}`; `inlineSettings:"not-an-object"` → 400 "expected record, received string"; unknown key → 400 `unrecognized_keys`. UI: duration 999 h → PATCH 400 and a visible toast "Invalid maintenance settings: durationHours: Too big: expected number to be <=72 / Check the highlighted fields" plus an inline field error. No silent persist. |
| #6377 | PARTIAL | API-only (`git show 03ee57c40` touches only `monitorResolver.ts` + its two test files; PR body says "no UI change"). Web-visible half verified: the filter controls live on the policy **Assignments** tab (not the Monitors tab as the inventory says) — Role Filter (12 roles) + OS Filter (Windows/macOS/Linux). Saved Server+Windows → POST 201, toast "Assignment added", FILTERS column shows `Server / Windows`, and `config_policy_assignments` holds `role_filter={server} os_filter={windows}`. Resolver behaviour itself is not observable in the UI (monitors list shows "1 policy", no device count), so it rests on its integration test. |
| #6395 | PASS | Schema confirmed via psql: `alert_rules.retired_at / retired_reason / converted_to_monitor_id`, plus tables `monitor_conversions` and `monitor_conversion_outputs`. Inserted two legacy rules in Default Organization; `/alerts/rules` lists only "QA Legacy Rule Live" and excludes "QA Legacy Rule RETIRED" (`retired_at = now()`). |

**Everyday-workflow pass** — `/alerts`, `/alerts/correlations`, `/alerts/monitors`,
`/alerts/monitors/new`, `/alerts/monitors/[id]`, `/alerts/rules`, `/alerts/delivery`,
`/configuration-policies`, `/configuration-policies/new`, `/configuration-policies/[id]`
(Overview · Monitors · Maintenance · Assignments), `/settings/alert-templates`. No
console errors other than the ones my own deliberate 401/400 probes produced; no broken
layouts, empty states present everywhere.

End-to-end CRUD, all confirmed by toast + status code:
- Monitor: create (201, "Monitor saved") → edit (PATCH 200, "Monitor saved") → delete (DELETE 204, "Monitor deleted", back to list).
- Delivery channel: create (email, "Channel Created", list 1 of 1).
- Routing rule: create (201, "Routing rule created"), row shows "Priority: 10 / Severities: high / Send to: QA Sweep Email Channel".
- Escalation policy: create ("Escalation policy created", "1 step · 15m").
- Config policy: create → Monitors attach (POST 201, "Saved") → Assignment with filters (201, "Assignment added") → Maintenance save (201, "Saved").
- Alert list actions: Ack → POST 200, "Alert Acknowledged", row flips to Acknowledged.

### G2 Billing / Ticketing / Pickers / Move-org

Stack `http://localhost:33032`, admin@breeze.local (platform admin / partner scope).
Prerequisite data created during the sweep (left in place): partner default tax rate
set to **7.5%**; billing profile **"QA Sweep Rates"** (created) and the default
**"Standard rates"** profile edited to `$120.00/h · 30 min minimum · round to 15 min`;
ticket **T-2026-0001** "QA Sweep G2 billing ticket" on Default Organization with two
time entries (10 min and 40 min); one assembled draft invoice on Default Organization
with line 1 marked taxable; **120 `QA Site NNN` rows** on org *E2E Multi-Currency
1789953310781* and **120 `QA Org NNN`** organizations (SQL) for the picker-exhaustion
rows; user **qa-tech@breeze.local** (Partner Technician role, admin's password hash)
created to prove the `time_entries:manage_billing` deny path over curl. Device
`e2e-macos.local` was moved to another org and **moved back** — both seeded devices are
in their original org/site. TOTP was enrolled on admin@breeze.local to pass the
move-org step-up and **disabled again afterwards** (`users.mfa_enabled = f` verified).

| PR | Status | Evidence |
|---|---|---|
| #6393 | PASS | Rates tab (`billing-settings-tab-rates`) lists the four partner billing profiles with currency + base-rule summary. **Create**: `billing-profile-create` → drawer → name/rounding/rate/minimum → toast "Billing profile saved.", row appears. **Edit**: `billing-profile-edit-<id>` on "Standard rates" → rounding 15 / rate 120 / minimum 30 → same toast, row re-renders "USD · Round to 15 min / Billable at $120.00/h · 30 min minimum". **Override gate** (PR body = the `time_entries:manage_billing` gate binding every writer): as `qa-tech@breeze.local` (Partner Technician: `time_entries:write`, no `manage_billing`) `POST /api/v1/time-entries` with `hourlyRate` → **403** `{"error":"Changing billing terms requires manage billing permission","code":"MANAGE_BILLING_REQUIRED"}`; same 403 with `minimumMinutes`. Allowed side exercised in the browser as Partner Admin (`*:*`), where `TicketTimeBilling.tsx:85` shows the rate/billable controls. |
| #6422 | PASS | With `$120/h · 30 min minimum · 15 min rounding` on the default card: a **10 min** entry on `/tickets/841ec804…` → card "Total time 10m / Billable 30m / Time amount $60.00", row "0.17 h worked · 0.50 h billed"; a **40 min** entry → rounds to 45 ("0.67 h worked · 0.75 h billed"), card totals "50m / 1h 15m / $150.00". `/timesheet` shows both rows with the same worked-vs-billed strings, the terms chip "$120.00/h · 30 min minimum", and week money `$150.00` (= 75 billed minutes). Invoice assembly agrees: the assembled draft has lines $60.00 + $90.00 = $150.00. See paper cut G2-1 for the day/week *minutes* aggregate. |
| #6423 | PASS | Partner default tax rate saved as 7.5% on the Defaults tab (toast "Billing settings saved"). Draft invoice assembled for Default Organization, line 1 flipped taxable → after autosave the summary renders `invoice-tax-inherited-hint`: *"Tax isn't on this draft yet — the inherited rate of 7.50% applies when you issue it, adding $4.50 for a total of $154.50."* Subtotal/Tax/Total still show the committed draft numbers ($150.00 / $0.00 / $150.00), exactly as the PR describes. Arithmetic checks: only the $60.00 line is taxable, 7.5% → $4.50. |
| #6459 | PASS | With **123 organizations** and **120 sites** on one org (both past the route's 100-row clamp): `/billing/invoices` → New invoice → `invoices-assemble-org` has **124 options** (123 orgs + placeholder), `invoices-assemble-site` **121** (120 + "All sites"); every expected `QA Org NNN` / `QA Site NNN` present, **0 missing, 0 duplicates**. Same for the move-org dialog (below). Walker is `apps/web/src/lib/fetchAllPages.ts`. See paper cut G2-2 on ordering. |
| #6411 | PASS | `/devices/42fc7de0…` → Move to Organization → target org *E2E Multi-Currency 1789953310781* → `move-org-target-site` lists **121 options = all 120 QA sites + placeholder** (none missing). Superseded in practice by #6459's exhaustion walker — the picker is no longer capped at 100 either. `move-org-target-org` shows 123 options (122 other orgs + placeholder; the device's current org is excluded). |
| #6400 | PASS | **Without a second factor** (starting state): submit → `POST /devices/:id/move-org` **403**, the dialog replaces the form with *"Add an authenticator app or passkey to your account before moving devices between organizations."* — its own copy, no factor step revealed, matching the PR's `403 MFA_REQUIRED` branch. **With step-up**: after enrolling TOTP, the same submit revealed "Confirm it's you — Moving a device between organizations requires a second factor" + `move-org-stepup-code`; a valid code → toast "e2e-macos.local moved to E2E Multi-Currency 1789953310781" and `devices.org_id/site_id` updated in psql. Moved back the same way (toast "…moved to Default Organization", DB restored). |

**Everyday-workflow pass** — `/settings/billing` (Defaults · Documents · Rates ·
Connections), `/billing/invoices` (empty state → list → `/billing/invoices/[id]`
editor + summary), `/billing/quotes`, `/contracts`, `/settings/catalog`, `/tickets`
(empty state → `/tickets/new` → `/tickets/[id]` workbench), `/timesheet`. Empty states
are present and well-written on invoices, quotes, contracts and tickets. No console
errors originating from any billing/ticketing page (the errors in the shared console
log come from other groups' tabs and from my own deliberate 403 probes).

Confirmed by toast + effect: billing settings save, billing profile create/edit, ticket
create ("Ticket T-2026-0001 created"), time log ×2 ("Time logged"), invoice assemble
("Draft invoice assembled"), invoice line taxable autosave, device move ×2.


### G3 Disk Cleanup + Backup & DR

Stack `http://localhost:33032`, admin@breeze.local (platform admin / partner scope).
Both seeded devices are **offline** and the Windows one runs agent **v0.63.0**, below the
`0.115.0` floor the Disk Cleanup v2 execute paths require — so every agent-dispatching
action refuses by design and only the refusal wording was gradeable.

Prerequisite data seeded via SQL (system scope) so the read-only render paths were
exercised: 3 `device_filesystem_cleanup_runs` (executed w/ 3.10 GB reclaimed, failed w/
error text, previewed), 1 `device_filesystem_snapshots` row for `C:\` with cleanup
candidates in four safe categories plus one unsafe `system_state` candidate, 2
`backup_snapshots` (+4 `backup_snapshot_files`) on `e2e-macos.local`, and 1 failed
`restore_jobs` row carrying `target_config.result.error`, linked to a recovery token.
Created through the UI: a recovery token, a DR plan (`QA Sweep DR Plan G3 (renamed)`,
draft, 1 group), and a backup profile (created then deleted again).
**Left behind for later groups:** the seeded rows above, the recovery token, and the DR plan.

| PR | Status | Evidence |
|---|---|---|
| #6366 | PASS | `/devices/<win>` → **Disk Cleanup** tab (label is "Disk Cleanup", under **More** — not "Filesystem"). One tab carries Volumes picker, snapshot panels (`filesystem-snapshot-panels`: summary tiles, scan summary, collected signals, temp accumulation, threshold triggers, largest files/dirs), `cleanup-panel`, `system-cleanup` panel and `cleanup-run-history`. Empty states all present before seeding ("No snapshot for this volume yet. Run Analyze Now to scan it.", "Run Cleanup Preview to choose what to delete.", "No cleanup runs recorded yet."), real data after. |
| #6386 | PARTIAL | UI passes, agent data BLOCKED. `POST /filesystem/system-cleanup/list` returns `409 agent_update_required` on the v0.63.0 agent, so the OS-native **catalog with per-action sizes never renders** — no live agent can be attached. What did render: the "OS cleanup actions" card with its correct copy and a specific inline banner ("Update this device's agent to version 0.115.0 or later to run OS cleanup actions."), and reclaimed bytes in history ("3.10 GB reclaimed" on the seeded executed run, "0 B reclaimed" on the failed one). See G3-1 for the raw-code toast that accompanies the good banner. |
| #6454 | PARTIAL | Confirm dialog passes, result banner BLOCKED. `Cleanup Preview` works entirely off the stored snapshot (no agent): 5 safe candidates / 13.30 GB, four category cards with counts+sizes, the unsafe `system_state` candidate correctly excluded. Selecting 2 → `cleanup-execute` → `cleanup-confirm-dialog` lists both paths under "First 2 of 2 paths" with "Delete 2 item(s) totalling 9.00 GB from C:\? This bypasses the recycle bin and cannot be undone." plus the trash-contents note. Confirming returns `409` and a specific toast ("Update the agent to version 0.115.0 or later to run cleanup."), so `cleanup-result` ("Reclaimed: X") cannot be reached without an agent. |
| #6453 | PARTIAL | Wizard passes, the restore itself is BLOCKED. **Inventory click-path is wrong:** there is no per-row "Restore" button on `/backup`; the only affordance is `snapshot-browser-restore-link` on the Snapshots tab, and it is broken (G3-2). Reaching `#restore` via the tab bar, the wizard walked all five steps — Select snapshot → Restore type → Select files (4 seeded files listed) → Destination (alternate path accepted) → Review — and **Start restore** stopped with the correct, specific error `Device is offline, cannot execute command`. |
| #6440 | PASS | Both halves verified. (a) Execute on a plan whose group has no recovery source → toast **and** inline banner: "Cannot start: a recovery group names no recovery source. Add a snapshot, restore job, or recovery token to the group." — same for rehearsal and failover, no bare `resource_not_found`. (b) Saving a Bare-metal-rebuild group with a relative output directory is refused pre-flight ("Bare-metal rebuild output directory must be an absolute path starting with \"/\" and at most 1024 characters.") and **nothing persisted** — `select count(*) from dr_plans` stayed 0 and `dr_plan_groups` 0 after the attempt. (Presentation nit in G3-3.) |
| #6431 | PASS | Web half verified; the agent half needs a real BMR run (BLOCKED, not re-tested). **Inventory click-path is wrong:** the section is `/backup#recovery-bootstrap` (RecoveryBootstrapTab), not the device Backup tab — the device Backup tab has no BMR section. With a seeded failed `restore_jobs` row (`target_config.result.error`) linked to the token, `recovery-restore-failure-reason` renders "Failure reason: artifact .resolv.conf.systemd-resolved.bak failed verification, discarding: size mismatch: downloaded 30 bytes, manifest says 789" — and it **survives a full page reload** (reload → View on the catalog row re-renders it). |

**Everyday-workflow pass.** `/backup` Overview, Verification, Profiles, Snapshots, Restore,
SQL Server, Hyper-V, Vault, SLA, Encryption, Recovery Bootstrap all render with sensible
empty states and no layout breaks. `/dr` Plans + Executions likewise. `/devices/<id>`
Backup tab renders status, job history, restore points with protection controls, and
correctly gated verification actions ("Device is offline. Verification requires a
connected agent."; "Run backup now" disabled with title "Assign a backup policy to
protect this device."). Backup **profile** CRUD works end to end with toasts ("Backup
profile created" / "Backup profile deleted") and a named delete confirm. DR plan
create/edit persist correctly but silently (G3-6); DR plans cannot be deleted from the UI
at all (G3-7).

**Console.** Only two errors across the whole area, both benign 404s the UI handles with a
real empty state: `GET /api/v1/reliability/<id>` and `GET /api/v1/devices/<id>/filesystem?path=C:\`
before a snapshot exists.


### G4 AI / MCP / Auth / Topology

| PR | Status | Evidence |
|---|---|---|
| #6399 | PASS | Created custom **org-scoped** role "QA G4 No AI" through Settings → Roles (`POST /api/v1/roles` → 201, toast `Role "QA G4 No AI" created`) with only `devices:read`; assigned it to a new user `qa-noai@breeze.local` (SQL, admin's password hash). Signed in as that user: header AI button **absent** (`header button` titles = Menu / Select Organization / Search / Help & Docs / Notifications / Theme / Account menu — no "AI Assistant"), `[data-testid="ai-chat-sidebar"]` **not rendered**. Granted `ai_sessions:use` (SQL + flush of `permission-cache:*`) and re-authenticated: header shows **"AI Assistant (Cmd+Shift+A)"**, sidebar renders, `POST /ai/sessions` → **201**, a real message round-tripped (`POST …/messages` → 200) with a labeled "Searched devices" tool card. Permission is present in the Create-Role matrix as resource **"AI Sessions"** × action **"Use"** (also "Read All"). **Gap → G4-1:** `/workspace` is not gated. |
| #6442 | PARTIAL | A live `ANTHROPIC_API_KEY` is configured, so the chat works. On `/devices/e65460f3…` → AI sidebar, asking to clean up disk produced a real `system_cleanup` tool call. The card renders the **human label "System cleanup"**, not `system_cleanup` (`AiToolCallCard.tsx`, label map via `formatToolName`). `/ai-risk` after **Refresh**: Approvals → "System Cleanup · failed · 1m ago · Exec: 54ms"; Denials → the row with the full reason; Analytics → "System Cleanup" in Most Used Tools / Avg Duration. DB confirms `ai_tool_executions: system_cleanup | failed`. **Not a pass because the outcome is not labeled in chat** → G4-2. |
| #6414 | PASS | `/ai-risk` → Guardrail Tier Matrix renders Tier 1 with **82 tools** across 17 categories (collapsed by default; a filter expands them — `TierOverviewMatrix.tsx:120`). All 13 tools added by `ecb77c549` are present with human labels, non-empty descriptions and valid categories (`tierConfig.ts`): list/get_network_asset, list_incidents, list_remediation_suggestions, list_org_contacts, list_sites, get_site, list_time_entries, get_running_timer, get_timesheet, list_ai_agents, list_ai_agent_runs, get_ai_agent_run. `formatToolName` (`lib/utils.ts:53`) means no raw snake_case reaches the UI. Verified in-browser via the filter (e.g. "cleanup" → "System Cleanup (List)" / "(Status)" in Tier 1, "(Run)" in Tier 3). Minor: org/site/contact reads are filed under category **Integrations**. |
| #6324 | PASS | Source-verified `apps/api/src/services/logRedaction.ts:189-201` (`redactToolOutputFields`, called from `aiToolOutput.ts:768`/`:396`): the policy is now **per key/value pair by value shape** — numbers/booleans always kept, containers under non-secret keys recursed, only a container under a true secret-material key wiped wholesale. `SECRET_MATERIAL_KEY_PATTERN` drops `session` from the log pattern; `SESSION_IDENTIFIER_KEY_PATTERN` matches only `session`/`sessionId`-shaped keys; ISO timestamps survive a secret-named key. New suite `aiToolOutput.redaction.test.ts` (318 lines) asserts sibling survival in 5 cases with negative controls. Live corroboration: a `list_devices` tool result rendered in chat with hostname / OS / last-seen / site all intact, nothing blanked. |
| #5847 | ~~PARTIAL~~ → **PASS** (superseded — see "G4 addendum — #5847 re-run" below) | Ceremony driven with a CDP virtual authenticator (`WebAuthn.addVirtualAuthenticator`, ctap2_1, internal, UV). `POST /auth/passkeys/register/options` → **200** with a well-formed v14 options object (`rp.id=localhost`, `pubKeyCredParams` −48/−8/−7/−257, `residentKey: preferred`, `credProps`), `navigator.credentials.create()` **succeeded**, then `POST /auth/passkeys/register/verify` → **500** `Unexpected registration response origin "http://localhost:33032", expected "http://localhost:32902"`. Root cause is **stack config, not the PR**: this worktree's `.env` carries `WEBAUTHN_ORIGIN=http://localhost:32902` while caddy publishes **33032** (`docker port …caddy-1` → `0.0.0.0:33032`; nothing listens on 32902). Registration is therefore impossible on any wt-stack with this env, and passkey **sign-in** could not be reached at all. Two real defects fall out regardless → G4-3, G4-4. |
| #6115 | **FAIL** | Seeded a network asset + 4 `topology_nodes` + 3 `topology_relationships` + `topology_site_state` at Default Site, and enabled `organizations.settings.topologyFeatureFlags` (all six) plus a complete `legacyImport` checkpoint. `GET /topology/sites/<site>/settings` then returns `capabilities.ui.available: true`. But the very next call the component makes, `GET /topology/sites/<site>/nodes?assetId=…&orgId=…`, returns **400 `{"error":"Invalid topology query","code":"invalid_topology_query"}`**, so `TopologyEntry`'s `.catch` sets `error` and the whole tab renders the single red line **"Invalid topology query"** — no map, no layout, no diagnostics panel. Repro + screenshot: `…/scratchpad/g4-topology-invalid-query.png`. See G4-5 for the root cause; **this is a browser-only failure — the same URLs are 200 from curl.** |
| #6028 | **FAIL** (web caller) | The endpoint itself is fine: `GET /api/v1/topology/sites/d9f083c3…/graph` → **200** with `schemaVersion:1`, `revisions.graph:"2"` and the seeded nodes. The same URL **with the `?orgId=` that `fetchWithAuth` injects on every `/api/v1` call** → **400 `invalid_topology_query`**. Measured side by side in one browser evaluate. So no web caller can consume `/graph` or `/nodes` today. Root cause G4-5. |
| #6406 / #6420 | PASS / PARTIAL | `/settings/connected-apps` renders cleanly (light + dark), heading "Connected apps", `McpUrlCard` copies exactly `http://localhost:33032/api/v1/mcp/sse` — matching the real mount (`index.ts:1124` `/api/v1` + `:1070` `/mcp`, handlers `mcpServer.ts:299/723/883` on `/sse`). **Not a bare POST to `/mcp`** — copy is current on that point, and the empty state is good. Copy drift → G4-6 (`X-API-Key` never mentioned). #6420's `isError` change is server-side on `POST /mcp`; no MCP client session was available, and the chat-path analogue of it is broken (G4-2), so PARTIAL on that half. |

Everyday-workflow pass (admin): `/ai-risk` all six tabs render with correct empty states and repopulate on **Refresh** (Guardrails / Analytics / Approvals / Rate Limits / Denials / Script Proposals); AI chat sidebar opens, closes, keeps history, and the conversation-history / new-conversation / flag buttons are present; Settings → Roles lists 10 system roles with type filters and Clone, and create works end-to-end with a toast; Settings → Profile renders profile / password / MFA / passkeys / approver devices / theming / language / onboarding; `/settings/connected-apps` OK. **Dark mode** checked on `/ai-risk`, `/settings/connected-apps`, `/settings/roles`, `/settings/profile`, `/devices/network/[id]` — background `rgb(13,16,23)`, **zero console errors, no horizontal overflow** on any of the five.

Test data left behind (mine): role **"QA G4 No AI"** (`4deaba2b-…`, org-scoped, now holds `devices:read` + `ai_sessions:use`), user **qa-noai@breeze.local** (admin's password), discovered asset **"QA Core Switch"** (`aaaaaaaa-0000-4000-8000-000000000001`) at Default Site, 4 topology nodes / 3 relationships, `topologyFeatureFlags` + `legacyImport` checkpoint on Default Organization / Default Site, and 4 AI chat sessions (~$0.65 of Anthropic spend).


### G4 addendum — #5847 re-run

Re-run after the stack's `WEBAUTHN_ORIGIN` was corrected to `http://localhost:33032` (`WEBAUTHN_RP_ID=localhost`), which was the blocker recorded above (G4-13). Same ceremony: CDP `WebAuthn.enable` + `WebAuthn.addVirtualAuthenticator` (ctap2 / internal / `hasResidentKey` / `hasUserVerification` / `isUserVerified`). The full `@simplewebauthn` v14 round trip now passes end to end.

| Step | Calls | Result |
|---|---|---|
| Register (`/settings/profile` → Passkeys → Add passkey) | `POST /api/v1/auth/passkeys/register/options` → **200**, `POST /api/v1/auth/passkeys/register/verify` → **200** `{"success":true,"passkey":{"id":"c401d7e8-…","name":"QA Virtual Key","transports":["internal"]}}`, `GET /api/v1/auth/passkeys` → **200** with the row | **PASS** — list row "QA Virtual Key / Last used: Never" appears with Rename + Delete, inline success banner **"Passkey added"** (this card uses its own `passkeySuccess` banner, not the toast host) |
| Sign in with the passkey | `POST /api/v1/auth/login` → **200** (MFA challenge), `POST /api/v1/auth/mfa/passkey/options` → **200** (`rpId: localhost`, `allowCredentials` holds the new credential), `POST /api/v1/auth/mfa/passkey/verify` → **200** with tokens | **PASS** — landed on `/` Dashboard |
| Delete | `DELETE /api/v1/auth/passkeys/<id>` → **200** `{"success":true,"tokens":{…}}` | **PASS** — confirm modal ("This permanently removes the passkey … also signs your other sessions out"), then "No passkeys are registered for this account." + inline **"Passkey deleted"**. DB back to clean: `admin@breeze.local | mfa_enabled=f | mfa_method=NULL | 0 passkeys` |

Two corrections to the assumptions in the original row: (a) a passkey in Breeze is a **second factor**, not a passwordless sign-in — there is no passkey button on `/login`; `register/verify` flips `mfa_enabled=true` / `mfa_method='passkey'` (`routes/auth/passkeys.ts:392-397`) and the next password login then serves the MFA challenge with `mfa-passkey-submit`. (b) the sign-in endpoints are `/auth/mfa/passkey/{options,verify}`, not `/auth/passkeys/login/*`.

Two environment notes for whoever re-runs this: the CDP virtual authenticator is **one internal authenticator per browser** — a leftover from an earlier session makes `addVirtualAuthenticator` fail with "Chrome only supports one internal authenticator per environment" (`WebAuthn.disable` first clears it), and `navigator.credentials.create()` throws `NotAllowedError: the page does not have focus` unless `page.bringToFront()` precedes the click. Neither is a product defect.

G4-3 and G4-4 (500 on verification rejection; generic unactionable error copy) are unchanged by this re-run — the error-mapping code was not touched, they were simply not reachable once the origin matched. G4-13 is confirmed as the sole cause of the earlier PARTIAL. One new paper cut fell out of the passing path → G4-14.


### Non-browser rows (sonnet, no browser)

**CI at the sweep base.** `git rev-parse 58f76b854` → `58f76b85419e78503e3b3040169163d96062f103`

Checked via `gh run list --workflow ci.yml --event merge_group` matching each PR's merge-commit oid (from `gh pr view <n> --json mergeCommit`):

| PR | Merge-ref sha | CI (merge_group) | PR-head rollup extras |
|---|---|---|---|
| #6481 | 58f76b854... | success | (no non-passing checks listed) |
| #6451 | cffc0ed33... | success | Portal Dev E2E (hydration, non-blocking) FAILURE |
| #6426 | 0c35dd15f8... | success | Portal Dev E2E (hydration, non-blocking) FAILURE |
| #6434 | d82b88f034... | success | Portal Dev E2E (hydration, non-blocking) FAILURE |

All 4 landed with a **green `CI Success` gate on the actual merge ref**. The only red job anywhere in the PR-head rollups is the known non-blocking `Portal Dev E2E (hydration, non-blocking)` — expected per project convention, not counted. No other red/cancelled jobs found on any of the 4.

| PR | Proposed status | Evidence |
|---|---|---|
| #6481 | PASS | merge_group CI success on 58f76b854 (see above) |
| #6451 | PASS | merge_group CI success on cffc0ed33; unit test `bareMetalRecoveryInFlightIndex.test.ts` green (below); race-condition contract itself is integration-only |
| #6426 | PASS | merge_group CI success on 0c35dd15f8 |
| #6434 | PASS | merge_group CI success on d82b88f034; `commandTimeouts.test.ts` green (below) |
| #6451 bareMetalRecoveryInFlightIndex / drExecution race | PASS (unit) / CI-covered only (race itself) | `apps/api/src/db/bareMetalRecoveryInFlightIndex.test.ts` — unit, 1 file passed. The actual concurrent-race regression is `apps/api/src/__tests__/integration/bareMetalRecoveryInFlightRace.integration.test.ts` and `drExecutionAuthorization.integration.test.ts` — both **integration-only (need real Postgres)** — CI-covered only, not run here. |
| #6434 commandTimeouts (24h ceiling) | PASS | `apps/api/src/services/commandTimeouts.test.ts` — unit, passed |
| #6424 agentWs enqueue contract | PASS | `apps/api/src/routes/agentWs.enqueueContract.test.ts` — unit, passed |
| #6469 bmr refuse referenced-object snapshots | PASS | `apps/api/src/routes/backup/bmr.test.ts`, `bmrRecoveries.test.ts`, `apps/api/src/services/bareMetalRecoveryService.test.ts` — all unit, passed |
| #6410 networkCheckAlertSweep (one alert/check) | PASS | `apps/api/src/services/monitors/networkCheckAlertSweep.test.ts` — unit, passed |
| #6409 monitorCompiler expectedStatus | PASS | `apps/api/src/services/monitors/monitorCompiler.test.ts` + `monitorCompiler.w04.test.ts` — unit, passed |
| #6380 automationRuntime restart_service normalization | PASS | `apps/api/src/services/automationRuntime.restartService.test.ts` — unit, passed |
| #6378 offlineAlertEffects offline-kind monitors | PASS | `apps/api/src/services/offlineAlertEffects.test.ts` — unit, passed |
| #6377 monitorResolver role/OS filters | PASS | `apps/api/src/services/monitors/monitorResolver.test.ts` — unit, passed |
| #6420 MCP isError ledger | PASS | `apps/api/src/routes/mcpServer.returnedError.test.ts`, `mcpServer.streamable.test.ts` — unit, passed |
| #6324 tool-output redactor | PASS | `apps/api/src/services/aiToolOutput.redaction.test.ts` — unit, passed |
| #6458 ai budget reservations | PASS (unit) / CI-covered only (integration) | `apps/api/src/services/aiBudgetReservations.test.ts`, `aiBudgetReservationSweep.test.ts`, `clientAi/sessions.create.test.ts`, `sessions.messages.test.ts`, `streamingSessionManager.reservation.test.ts` — all unit, passed. `__tests__/integration/ai-budget-reservations.integration.test.ts` is integration-only — CI-covered only |
| #6416 monitor conversion routes | PASS (unit) / N/A-no-data (live probe) | `apps/api/src/routes/monitorDefinitions.conversion.test.ts` + all of `apps/api/src/services/monitors/conversion/*.test.ts` — unit, passed. Live probe below returned 404 `policy_not_found` — no seed data exists to convert (see Live probes) |
| #6392 routing API rejection | PASS | `apps/api/src/routes/alerts/routing.siteScope.test.ts` — unit, passed (asserts 403 on site-scope violation for POST/PUT/PATCH/DELETE `/routing-rules`); redundant live probe skipped since seeding a site-restricted auth context adds no signal beyond the unit contract |
| #6406 MCP additive spec (version negotiation, title/annotations/_meta) | PASS | Live probe: `initialize` negotiates `protocolVersion: "2025-06-18"` and returns `serverInfo` (`breeze-rmm`, title `Breeze RMM`, v0.82.0); `tools/list` returns 183 tools, each carrying `title`, `annotations` (readOnlyHint/destructiveHint/idempotentHint/openWorldHint), and `_meta` |
| #6028 topology graph route | PASS | Live probe: `GET /api/v1/topology/sites/<siteId>/graph` → 200, well-formed shape (`schemaVersion`, `nodes`, `relationships`, `presentation`, `layout`, `counts`, `coverage`). Real mounted path is per-site (`/topology/sites/:siteId/graph`), not a bare `/topology/graphs` collection endpoint. |
| #6129 topology retention worker | PASS | `topologyCollectionRetentionWorker` registered in `apps/api/src/services/workerRegistry.ts:85` (global placement, non-consumer) and wired via `startRegisteredWorkers`; no boot errors in API container logs (`docker logs ... \| grep -i topology` shows only migration-apply lines, no worker errors). Ticks hourly via `setInterval`, so no boot-time log line is expected by design; confirmed via code reading rather than an observed tick |
| #6450 docs(redis noeviction) | N/A-verified | Text present: `apps/docs/src/content/docs/deploy/environment.mdx:772`, `deploy/upgrades.mdx:974`, `deploy/rollout-modes.mdx:112`, `getting-started/prerequisites.mdx:34` — `noeviction` policy documented with the allkeys-lru correction and recovery guidance |
| #6417 docs(mobile App Store link) | N/A-verified | `apps.apple.com` link present in `apps/docs/src/content/docs/features/mobile.mdx` |
| #6455 | BLOCKED (no Go test files in diff) | `gh pr view 6455 --json files` shows no `_test.go` files changed — nothing to run; needs live-agent verification |
| #6448 | BLOCKED (live agent needed; tests green) | `agent/internal/remote/tools/fileops_cleanup_test.go`, `filesystem_cleanup_rules_test.go` → package `internal/remote/tools` PASS (`go test -race`, 19.6s) |
| #6430 | BLOCKED (live agent needed; tests green; Windows-only file skipped) | `agent/internal/backup/bmr/winattrs_test.go`, `agent/internal/backup/winattrs_test.go`, `agent/internal/securefs/winattrs_unix_test.go`, `winattrs_windows_test.go` → packages `internal/backup/bmr`, `internal/backup`, `internal/securefs` PASS on darwin/arm64. `winattrs_windows_test.go` is Windows-build-tagged and not compiled/run on this host — needs a Windows lab run |
| #6429 | BLOCKED (live agent needed; tests green) | `agent/internal/heartbeat/ws_direct_result_test.go` → package `internal/heartbeat` PASS (64.1s) |
| #6427 | BLOCKED (live agent needed; tests green) | `agent/internal/backup/incomplete_snapshot_test.go`, `snapshot_test.go`, `agent/internal/backup/providers/s3_checksum_test.go` → packages `internal/backup`, `internal/backup/providers` PASS |
| #6425 | BLOCKED (live agent needed; tests green) | `agent/internal/backup/backup_test.go` → package `internal/backup` PASS |
| #6418 | BLOCKED (live agent needed; tests green) | `agent/internal/remote/tools/fileops_trashdir_test.go` → package `internal/remote/tools` PASS |
| #6404 | BLOCKED (live agent needed; tests green) | `agent/internal/backup/rebuild/convert_test.go`, `preflight_test.go` → package `internal/backup/rebuild` PASS |
| #6032 | BLOCKED (live agent needed; tests green) | `agent/internal/helper/manager_test.go` → package `internal/helper` PASS |

Agent-only rows are BLOCKED for the *claimed* outcome (needs a live agent / Windows lab / MinIO), with the touched Go packages green under `go test -race` on darwin/arm64. Unit suites for the api-only rows: 34 files / 489 tests green.

## UI/UX paper cuts

| # | Where | Observation | Severity | Disposition (fixed <sha> / issue # / noted) |
|---|---|---|---|---|
| G1-1 | `/configuration-policies/[id]` → Monitors tab | Deleting a monitor that is attached to a policy leaves the attachment orphaned: the row renders the bare UUID `fca50f0a-3f09-4112-a8e1-7cd9ce7f2bd4` as its title with no "monitor deleted" state, and `config_policy_feature_links.inline_settings` still holds `{"items":[{"monitorId":"fca50f0a-…"}]}`. The delete confirm gives no warning even though the monitors list had just shown "1 policy" for that monitor. | med | issue #6493 |
| G1-2 | `/alerts/monitors/new` → Kind = Composite | The kind's only field renders the raw i18n key **`fields.match`** as its label. `monitorKindFields.ts:242` references `monitoring:fields.match`, which does not exist in `apps/web/src/locales/en/monitoring.json` (nor any other locale). | med | fixed cb71a5f82 |
| G1-3 | `/alerts/delivery` | The "Test this rule set" preview does not re-resolve after a routing rule is created or edited — it kept answering "High → Do not notify / No delivery row is configured" until a preview control was changed by hand. Stale answer right at the moment the user wants confirmation their new rule works. | low | issue #6497 (batch) |
| G1-4 | `/configuration-policies/new` | With the default scope ("A specific organization", pre-selected over "Partner library") and no org chosen, **Create Policy** is disabled with no inline reason — the form looks complete and the button just does nothing. | low | issue #6497 (batch) |
| G1-5 | `/configuration-policies/new` | Creating a policy navigates to the detail page but fires no success toast, unlike every other create in this area (channel, routing rule, escalation policy, monitor all toast). | low | issue #6497 (batch) |
| G2-1 | `/timesheet` | The day header and week footer label worked minutes as **billable**: with a 10 min and a 40 min entry the day reads "50m · 50m billable" and the footer "Total: 50m / Billable: 50m", while the two rows directly beneath say "0.50 h billed" / "0.75 h billed" and the money on the same line is **$150.00** — i.e. 75 billed minutes at $120/h. `timeEntryService.ts:1382-1383` sums `durationMinutes` on purpose (comment at :1395 — "that figure is utilization"), so this is a copy bug, not arithmetic: the aggregate needs to say *worked* / *utilization*, or show billed minutes next to the billed money. | med | fixed bbe3e5745 |
| G2-2 | every org/site `<select>` (invoice assemble, move-org, New Ticket) | After #6459 pages the lists to exhaustion the options arrive in `created_at, id` order (`routes/orgs.ts`, the sites handler's `.orderBy(sites.createdAt, sites.id)`) and nothing re-sorts client-side, so a 123-entry org dropdown reads "QA Org 078, QA Org 010, QA Org 089, QA Org 014, …". Correct for pagination, unusable as a picker — type-ahead in a native select only matches the first letter, so finding one org means scrolling an unordered list of 123. Sorting by name after the walk (or the typeahead the PR defers) would finish the job. | med | fixed 759a0e08d |
| G2-3 | `/settings/billing` → Connections tab | The tab contains no editable settings at all — three "jump to Integrations / Product Catalog / Distributors" links — yet still renders the shared **"Save billing settings"** button underneath. A save button with nothing to save. | low | issue #6497 (batch) |
| G2-4 | `POST /api/v1/time-entries` | Omitting `startedAt`/`endedAt` returns `400 "startedAt: Invalid input: expected date, received Date; endedAt: Invalid input: expected date, received Date"` — the server defaults those fields to `Date` objects and then fails its own validator, and the message is self-contradictory to the reader. Not reachable from the web UI (which always sends ISO strings), but it is the message an integrator sees. | low | issue #6497 (batch) |
| G3-1 | `/devices/[id]` → Disk Cleanup → **Check available actions** | The 409 `agent_update_required` raises a correct inline banner *and* a toast containing the **raw error code** `agent_update_required`. `SystemCleanupPanel.checkActions` wraps the POST in `runAction`, which toasts `body.error` before `handleFailure` converts it into the banner. The sibling cleanup-execute path toasts a properly worded sentence, so the two disagree. `apps/web/src/components/devices/filesystem/SystemCleanupPanel.tsx`. | med | fixed 7470e15ad |
| G3-2 | `/backup#snapshots` → "Use the restore workflow to recover or export files from this snapshot." | Dead link. Clicking `snapshot-browser-restore-link` (`<a href="#restore">`) changes the URL to `#restore` but leaves the user on the Snapshots tab; no `hashchange` fires (verified with a listener and with a trusted Playwright click), so `useHashState` never re-reads it. Setting `location.hash` from script *does* switch tabs, so the tab machinery is fine — the anchor navigation is being swallowed. It is the only cross-link from a snapshot into the restore flow. `apps/web/src/components/backup/SnapshotBrowser.tsx`. | med | fixed 429c221c4 |
| G3-3 | Three separate forms | A validation error is rendered at the **top** of a long scrolling panel while the button that triggered it sits far below, with no scroll-into-view — so the click reads as a no-op. Measured offsets: DR plan editor 886 px, Backup profile editor 1137 px, device Backup tab protection controls ~886 px. `DRPlanEditor.tsx`, `BackupProfilesTab.tsx`, `DeviceBackupTab.tsx`. | med | issue #6494 |
| G3-4 | `/backup` Overview → "Storage by Provider" | The card prints the empty state "No storage providers configured yet. Add a backup config to track usage." and then immediately renders a `local` provider row with 2.07 GB underneath it. Also on this card: "Devices Protected 0" next to 2 jobs / 2 snapshots, and "Attention Needed" prints a raw ISO timestamp (`Last unsuccessful 2026-09-20T19:22:01.524Z`) where every other row is formatted. | med | issue #6496 |
| G3-5 | `/backup#restore` Restore Wizard | Three raw-value renders: snapshot rows show **`--` for size and a hardcoded `Ready` status** (the API's snapshot response carries neither field, `RestoreWizard.tsx:444-445`) even though the Snapshots tab shows "1.1 GB" for the same snapshot; file sizes are unformatted bytes (`2097152 B`, `RestoreWizard.tsx:94`); and the Review step says only "Destination: Alternate path" without echoing the path the operator typed — the one value worth confirming before an irreversible restore. | med | issue #6496 |
| G3-6 | `/dr` → Create/Edit plan, `/backup#recovery-bootstrap` → Create token | Neither mutation uses `runAction` and neither confirms success — the dialog just closes. `DRPlanEditor.tsx` (POST/PATCH `/dr/plans`, POST/PATCH/DELETE groups) and `RecoveryBootstrapTab.tsx` (0 `runAction` calls) do bare `fetchWithAuth` mutations and appear in **neither** `TARGET_GLOBS` in `no-silent-mutations.test.ts` **nor** `runActionAllowlist.ts`, so the guard does not cover them at all. | med | issue #6495 |
| G3-7 | `/dr` → plan row | Row actions are **Edit** and **Execute** only — there is no Delete, although `DELETE /dr/plans/:id` exists in `apps/api/src/routes/dr.ts:357`. The only removal path is setting status to "archived" inside the edit dialog. | low | issue #6494 (noted inside) |
| G3-8 | `/devices/[id]` → Disk Cleanup → **Analyze Now** | On an offline device the scan POSTs `202 Accepted` and the banner reads "Scan running (pending)" indefinitely, with no hint that the device is offline and the queued command will not run. The device Backup tab, by contrast, disables "Run backup now" with an explanatory `title`. `DeviceFilesystemTab.tsx` has no offline check. | low | issue #6497 (batch) |
| G3-9 | `/devices/[id]` → Disk Cleanup → Cleanup history | The action count renders as an unlabelled bare number appended to the meta line ("… · 3.22 GB reclaimable · 3") — no unit, no i18n key, `CleanupRunHistory.tsx:146`. Neighbouring `historyCandidates` reads "1 candidates" for a single candidate (no `_one` plural form in `locales/*/devices.json`). | low | issue #6496 |
| G3-10 | Dark mode → Disk Cleanup → Cleanup history | `statusClasses` in `CleanupRunHistory.tsx:38-43` carries no `dark:` variants, so the light-mode 700-level text colours land on a near-black page. The **Previewed** pill (`text-gray-700`, computed `oklch(0.373 …)` on a 15 %-alpha grey over `rgb(13,16,23)`) is barely legible; Failed/Executed survive. `SnapshotPanels.tsx`'s partial-scan banner (`bg-amber-50 text-amber-800`) has the same gap. Evidence: `.playwright-mcp/sweep/g3-cleanup-history-dark.png`. | low | issue #6496 |
| G3-11 | `/backup#recovery-bootstrap` → Bootstrap detail | SNAPSHOT and DEVICE are printed as bare UUIDs (`cccc3333-…`, `42fc7de0-…`) although the tab already has the snapshot label and the device row; the linked restore job likewise shows `RESTORED SIZE 402653184` unformatted. | low | issue #6496 |
| G3-12 | `/backup#restore` → step 3 | For a **selective** restore, Continue is enabled and advances to Destination with zero files selected; the refusal only appears four steps later as a disabled "Start restore" with no stated reason. | low | issue #6494 (noted inside) |
| G3-13 | `/dr` plan table, `/backup#recovery-bootstrap` snapshot picker | US short-locale dates (`9/20/2026, 8:15:17 PM`, `9/20/2026, 2:08:10 PM`) next to the app's own `Sep 20, 2026, 02:08 PM` format everywhere else, including the storage chart axis on `/backup` Overview. | low | issue #6496 |
| G4-1 | `/workspace` ("AI Assistant" sidebar link) | #6399 gates the header button, `AiChatSidebar`, `AiInitiatorChip`, `DeviceReliabilityPanel` and `FleetOrchestrationPage` on `ai_sessions:use` — but **not the `/workspace` page or its nav link**. A user holding only `devices:read` still sees "AI" → "AI Assistant" in the sidebar, lands on a full "AI Workspace / Run multiple AI conversations simultaneously" page, clicks **New Conversation** (`GET /ai/usage` → 403, swallowed), gets a complete chat pane with suggestion chips and a composer, types a message and receives a bare **"Permission denied · Dismiss"** — the exact confusing 403 the PR set out to remove, on the one AI surface it missed. | med | issue #6498 |
| G4-2 | AI chat sidebar → any refused tool call | A `system_cleanup` call the server **refused** ("Device … is not online") renders as a neutral collapsed card: grey **wrench** icon, label only, and expanding it shows nothing. `AiToolCallCard.tsx:92-104` has the right states (`XCircle` on `isError`, `CheckCircle` on `output !== undefined`) — the card fell through to the `Wrench` branch because no `tool_result` message ever arrived for it (`processStreamEvent.ts:170`), so `output` and `isError` were both undefined. A successful call in the same session rendered two cards ("Searched devices" ×2), so the pairing works when the result streams. Net: the failure is invisible in the card UI; the operator only learns of it from the model's prose, while `/ai-risk` correctly records `failed`. | med | issue #6500 |
| G4-3 | `POST /api/v1/auth/passkeys/register/verify` | A WebAuthn **origin mismatch** answers **500 Internal Server Error** and echoes the server's configured origin back to the client: `Unexpected registration response origin "http://localhost:33032", expected "http://localhost:32902"`. `apps/api/src/routes/auth/passkeys.ts:264-280` maps only `PasskeyChallengeError` to 400 and rethrows everything else, so every `@simplewebauthn` verification rejection (origin, RP ID, attestation) becomes a 500. It is a client/config error, not a server fault, and the expected-origin disclosure is gratuitous. | med | issue #6499 |
| G4-4 | `/settings/profile` → Passkeys | Both terminal states are generic and unactionable — "Passkey setup was canceled or timed out" when `navigator.credentials.create()` throws, and "Passkey registration failed. Please try again." for the 500 above. The second is not retryable: the same origin mismatch will fail forever, and "Please try again" sends the user into a loop. Neither surfaces the server's own message, which in this case named the exact misconfiguration. | med | issue #6499 |
| G4-5 | every browser call to `/api/v1/topology/sites/:siteId/{graph,nodes}` | `fetchWithAuth` injects `?orgId=` into **every** `/api/v1` request (`apps/web/src/stores/auth.ts:1300-1343`, `applyOrgId`), and `topologyApi.topologyRead` (`apps/web/src/components/topology/topologyApi.ts:14`) does not pass `skipOrgIdInjection`. Both graph readers parse the **raw** query string through `.strict()` zod schemas — `graphQuerySchema` (`packages/shared/src/validators/topology.ts:69-75`) and `nodeListQuerySchema` (`apps/api/src/services/topology/graphCursor.ts:13-19`) — via `parse(…, c.req.query())` at `apps/api/src/routes/topology/graphs.ts:36-37`, so the unexpected `orgId` key fails the schema and `:32` throws `invalid_topology_query` (400). Measured in one evaluate: `/graph` → **200**, `/graph?orgId=…` → **400**; `/nodes?assetId=…` → **200**, `/nodes?orgId=…` → **400**. `/settings` on the same router is unaffected. This makes the entire new topology UI unreachable from a browser (see #6115 / #6028). Fix is one line either side — `skipOrgIdInjection` in `topologyApi.ts`, or strip `orgId` before `parse` in `graphs.ts`. | **high** | fixed a4ac2b578 |
| G4-6 | `/settings/connected-apps` → "Direct your AI agent here" | The card and the page heading describe **only** OAuth ("You'll be sent back here to authorize the connection via OAuth"), but `mcpServer.ts:217-232` accepts `Authorization: Bearer` **or** `X-API-Key`, and the unauthenticated 401 body is literally `Missing X-API-Key header`. `X-API-Key` appears nowhere in `apps/web/src`. The login-page copy of this card is guarded by `requireOAuth` (`McpUrlCard.tsx:31-40`); the connected-apps instance (`connected-apps.astro:18`) is **not**, so with `MCP_OAUTH_ENABLED` unset it still promises an OAuth round-trip that cannot happen. The docs cover the API-key path (`mcp-server.mdx:66,119,126-127`); the dashboard does not. | low | issue #6497 (batch, comment) |
| G4-7 | `/ai-risk` as a user without AI permissions | The page renders in full — tier matrix, all six tabs — for a user holding only `devices:read`, while `GET /ai/admin/security-events` and `/ai/admin/script-proposals-metrics` both return **403** silently. Analytics and Denials then show the ordinary "No tool executions in this time period" / "No rejections or denials found" empty states, which reads as *clean* rather than *not permitted*. There is no nav-level gate on the "AI Risk" link either. | med | issue #6498 (noted inside) |
| G4-8 | `/devices/network` (no id) | Hits `/devices/[id].astro` with `id="network"` and renders a **phantom device page** — "Unknown / Unknown / Offline / Windows / Agent v / Unknown Site" with live Wake / Run Script / Connect Desktop buttons — instead of 404 or a redirect. There is no network-asset list route at all (the only pages are `devices/network/[id].astro` and `discovery/index.astro`), so this URL is a plausible thing to type or link. | low | issue #6501 |
| G4-9 | `/ai-risk` → Analytics → "Most Used Tools (Top 10)" | The bar label reads **`SystemCleanup`** (no space) while the "Avg Duration by Tool" chart on the same tab and every other surface read "System Cleanup". | low | issue #6500 (noted inside) |
| G4-10 | `/ai-risk` → Refresh | Each refresh fires every admin query **twice** (`/ai/admin/security-events`, `/ai/admin/script-proposals-metrics`, `/ai/admin/tool-executions` all appeared as identical duplicate pairs in one click). Doubles the cost of a page that reads a 200-row audit window, and this API is rate-limited at 300 req/60 s. | low | issue #6497 (batch, comment) |
| G4-11 | `/ai-risk` → Rate Limits vs Guardrail Tier Matrix | The TIER column on Rate Limits is **one tier per tool**, but several tools span tiers per action, so the two tables on the same page (one tab apart) contradict each other. `tierConfig.ts:331/333` give `manage_processes` and `manage_scheduled_tasks` `tier: 1`, while `:239/:241` list "(kill)" and "(run/disable/enable)" under **Tier 3 — Requires Approval**; `system_cleanup` goes the other way — the rate-limit row prints **T3** while `:97-98` put "(list)" and "(status)" in Tier 1. The file already knows the split per action (`ACTION_PERMISSIONS`, `:395`, `:419`), so the column is showing a value it cannot represent. | med | issue #6501 (noted inside) |
| G4-12 | Settings → Roles → Create Role → Permissions | The matrix is a 22-column table (Read … Manage policy) in which the overwhelming majority of cells are `—`; several columns (Cross-Site Restore, Manage Credentials, Fulfill, Manage Billing, Accept Risk, Decide, Manage policy) carry exactly one checkbox each. Nothing groups or collapses it, and the action names are bare verbs with no tooltip — "Use" on the **AI Sessions** row is the only clue to what `ai_sessions:use` grants. | low | issue #6497 (batch, comment) |
| G4-13 | wt-stack `.env` | `WEBAUTHN_ORIGIN=http://localhost:32902` does not match the port caddy actually publishes (`33032`). Every passkey registration and passkey sign-in on a worktree stack fails with the G4-3 500, which will also take out any passkey E2E spec run against one. Worth fixing in the wt-stack env generator, not just this checkout. | med | sweep-stack .env error, fixed locally; guard suggested in #6499 |
| G4-14 | `/settings/profile` → Passkeys → Add passkey (first factor on the account) | `POST /auth/passkeys/register/verify` mints and returns **10 recovery codes** when the passkey is the account's first MFA factor, and `ProfilePage.tsx:1153-1154` stores them — but they are **never rendered**. They are passed to `<MFASettings recoveryCodes=…>` (`:1458`), whose recovery-codes panel only renders under its own internal `view === 'recovery'` state (`MFASettings.tsx:1124-1126`), which the passkey flow never enters. Confirmed live: the ten codes were in the verify response body and no digit of them appeared anywhere in the DOM. The user is now MFA-enabled with recovery codes they have never seen and cannot display again ("Recovery codes are shown once and cannot be displayed again"). | **high** | fixed 12394245a |

## Fixes applied

| Commit | PR/area | What | Test |
|---|---|---|---|
| `cb71a5f82` | G1-2 / #6394 monitors | Composite kind's `match` field rendered the raw key `fields.match`; all/any options fell through to `directions.*`. Added the label + a `compositeMatch` option namespace to all 8 locales, registered it, and a test that every kind-field labelKey and select option resolves in the en catalog. | `monitorKindFields.test.ts` (+2, red→green) |
| `bbe3e5745` | G2-1 / #6422 timesheet | Day/week `billableMinutes` aggregate summed actual duration instead of the billed quantity (`billableMinutes ?? durationMinutes`), so the header disagreed with the rows and the money. | `timeEntryService.test.ts` (+1, red→green; one pre-existing test had pinned the bug) |
| `759a0e08d` | G2-2 / #6459 pickers | Exhaustively paged org/site lists arrived in `created_at` order; sort by display name once in `fetchAllOrganizations`, `fetchAllSites` and `orgStore` (new `sortByDisplayName`, `numeric` + `base` collation). | `fetchAllOrganizations.test.ts`, `fetchAllSites.test.ts` (+2 red→green); callers unchanged |
| `429c221c4` | G3-2 / #6453 backup | Snapshots tab "restore workflow" link was a bare `<a href="#restore">` that Astro's ClientRouter turned into `pushState` (no `hashchange`), so the tab never switched. Routed through `HashLink`. | `BackupDashboard.test.tsx` (+1, uses the Astro click-interception stand-in) |
| `7470e15ad` | G3-1 / #6386 disk cleanup | "Check available actions" 409 `agent_update_required` toasted the raw code; mapped via `runAction`'s `friendly` hook to the banner's copy on both queueing POSTs. | `SystemCleanupPanel.test.tsx` (+1); `no-silent-mutations` green |
| `a4ac2b578` | G4-5 / #6115 #6028 topology (**the FAIL**) | `fetchWithAuth` appends `?orgId=` to every call and the topology readers parse the raw query with `.strict()` → every browser call 400'd and the topology UI was unreachable. Readers now drop `orgId` when it equals the site's org and still 400 a foreign org. Verified live: map + diagnostics render, 0 console errors. | `routes/topology/graphs.test.ts` (+7 red→green; all 10 topology route files green) |
| `12394245a` | G4-14 / #5847 passkeys | First-factor passkey enrolment returned 10 recovery codes that `ProfilePage` stored but `MFASettings` never rendered (its panel only opens on its own TOTP/SMS transitions) — user ended up MFA-enabled with codes they could never see. Mirrors the TOTP contract: open the same panel once when new codes arrive for a passkey-first-factor account. | `ProfilePage.passkeyFirstFactorRecoveryCodes.test.tsx` (+2, red→green); 14 files / 104 profile+MFA tests green |

| `a86a3abcb` | review follow-up (topology) | Review of `a4ac2b578` found the same strict raw-query parse on template-options, collectors (both live web callers), policies, targets and template-versions. Hoisted the rule into `routes/topology/query.ts` and applied it to all six readers. | 4 new route tests, red→green against the unfixed routes; 10 files / 170 topology tests green |
| `79f7f2336` | review follow-up (passkeys) | The effect advanced its last-seen ref even when the guard failed, so codes arriving while the panel sat on another view were recorded as shown and never rendered. Ref now advances only when the panel opens; the second test was non-discriminating and was replaced by two that are. | 14 files / 105 profile+MFA tests green |

Web `tsc --noEmit`: the only error is the known stale-zod `WebhookForm.tsx` TS2769 in this worktree's node_modules (pass-1 trap), untouched by the sweep.

## Issues filed

| Issue | Title | From row |
|---|---|---|
| #6493 | Monitors: deleting a monitor attached to a policy orphans the feature-link item — Monitors tab renders a bare UUID | G1-1 |
| #6494 | Backup/DR forms: validation error renders at the top of a long panel with no scroll-into-view | G3-3 (+G3-7, G3-12) |
| #6495 | DR plan create/edit and BMR token create bypass runAction and the no-silent-mutations guard | G3-6 |
| #6496 | Backup Overview, Restore Wizard, Recovery tab render raw values (empty state next to data, hardcoded Ready, bare bytes/UUIDs/ISO dates, dark-mode pill) | G3-4, G3-5, G3-9, G3-10, G3-11, G3-13 |
| #6497 | Pass-3 sweep low-severity paper cuts batch | G1-3/4/5, G2-3/4, G3-8, G4-6/10/12 |
| #6498 | ai_sessions:use does not gate /workspace; /ai-risk renders with silent 403s for an unprivileged user | G4-1, G4-7 |
| #6499 | Passkey verify returns 500 and echoes the expected origin on a WebAuthn origin mismatch; UI copy unactionable | G4-3, G4-4 (+G4-13 guard) |
| #6500 | AI chat: a server-refused tool call renders as an empty neutral card | G4-2 (+G4-9) |
| #6501 | /devices/network (no id) renders a phantom device page with live action buttons | G4-8 (+G4-11) |

## Summary

| Group | PASS | PARTIAL | FAIL | BLOCKED | N/A |
|---|---|---|---|---|---|
| G1 Alerting & Monitors (10) | 8 | 2 | 0 | 0 | 0 |
| G2 Billing / Ticketing / Pickers / Move-org (6) | 6 | 0 | 0 | 0 | 0 |
| G3 Disk Cleanup + Backup & DR (6) | 3 | 3 | 0 | 0 | 0 |
| G4 AI / MCP / Auth / Topology (9) | 5 (incl. #5847 re-run) | 2 | 2 → **both fixed** (`a4ac2b578`) | 0 | 0 |
| Non-browser (38) | 4 | 0 | 0 | 20 | 14 |
| **Total (69)** | **26** | **7** | **2 (fixed)** | **20** | **14** |

PARTIAL = the web part passes and the claimed data/outcome needs a live agent, or the inventory's expected outcome was overstated (source wins — noted per row). BLOCKED rows are agent-only/live-provider changes whose touched Go packages and API unit suites are green.

**Top findings (systemic beats single):**
1. **The topology UI (#6115/#6028) was unreachable from any browser** — `fetchWithAuth`'s ambient `?orgId=` hit `.strict()` query schemas. The API-only probe read green; only a browser reaches it. Any new route that `.strict()`-parses `c.req.query()` must tolerate `orgId` — worth a lint/contract test later. Fixed `a4ac2b578`.
2. **Two auth-adjacent silent gaps:** first-passkey enrolment mints recovery codes nobody sees (fixed `12394245a`); `ai_sessions:use` gates the sidebar but not `/workspace` (#6498). Both are "the PR did the main path and missed a sibling surface" — the sweep-ALL-call-sites rule from CLAUDE.md, applied to UI entry points.
3. **Backup/DR surfaces are behind the rest of the app on presentation discipline:** raw bytes/UUIDs/ISO dates, empty state rendered next to data, hardcoded `Ready`, no dark-mode variants, mutations outside `runAction` and outside the no-silent-mutations guard's globs (#6494, #6495, #6496). One formatting/guard sweep over `components/backup/**` and `components/dr/**` would clear most of it.
4. **Inventory expected-outcomes were wrong 4 times** (#6394 sub-conditions, #6381 frozen notice location, #6377 tab, #6453/#6431 click-paths). The reader agent extrapolates from PR titles; the browser agents caught every one by reading source. Keep "source wins" in the brief.
5. Per-page double fetches (#6497 G4-10, dev double-invoke class) and stale previews after a create (G1-3) recur across areas — low individually.

**Oldest PR reached:** #5841 (2026-09-14 dep bump); every PR in `b4f589a6b3..58f76b854` has a non-TODO status.

**Before the release cut:**
- Merge this PR (9 fix commits + doc; one opus review round on the auth/tenancy-adjacent pair, both Important findings fixed, nits left in the review notes on the PR). The topology fix is release-blocking for #6115; the passkey-codes fix is a lockout-risk fix for anyone enrolling a passkey first.
- Flags: nothing new to enable; `TOPOLOGY_DISABLED` stays unset. `WEBAUTHN_ORIGIN` must match the public origin on every deploy (G4-13 was a stack error, but #6499 asks for a startup warning).
- 20 BLOCKED rows need a live-agent lab pass (bare-metal/backup/disk-cleanup agent changes, Windows attrs `winattrs_windows_test.go`, MinIO HTTPS). Same list as the pass-2 lab backlog plus #6404/#6448/#6418/#6425/#6427/#6429/#6430.
- Follow-up rows not in this base: #6439, #6468 (merged/rebased during the gate).
- Open issues from this pass: #6493–#6501.
