# Pre-release sweep — v0.112.0 → main (2026-09-12)

| | |
|---|---|
| Range | `v0.112.0` (`9db3972e3a`, 2026-09-11) → `origin/main` `f175ac6f05` (46 commits, 974 files, 16 migrations); follow-ups #5648 #5660 landed at `3815ed37b` (no migrations, no release.yml change) |
| Gate | Merge queue at start: #5648, #5660 (awaiting checks), #5649 (UNMERGEABLE) — not waited on; base is f175ac6f05 |
| Stack | `pnpm wt-stack up` on `qa/sweep-post-v0.112.0` @ `f175ac6f05`, baseUrl `http://localhost:32790` (api `/api`, portal `/portal`), 698 migrations applied incl. all 16 in range |
| Flags enabled | `BREEZE_AI_SCRIPT_AUTHORING_ENABLED=true` (also the code default), `BREEZE_WORKSPACE_ENABLED=false` (no pgvector locally) |
| Driver | skill `pre-release-sweep` via `release` Step 0; sweep agents: TBD |

## Change inventory

Status ∈ `TODO | PASS | PARTIAL | FAIL | BLOCKED | N/A`. Every row must leave `TODO`.

| PR | Merged | Surface | Title | Route / click-path | Expected visible outcome | Prereq | Status |
|---|---|---|---|---|---|---|---|
| #5627 | 2026-09-11 | portal | Customer Service and Documents surfaces (service deliverables W04) | Portal → "Service" tab (`/service`, ServiceScorecard) and "Documents" tab (`/documents`, DocumentLibrary); nav gated by org branding `enableService`/`enableDocuments` | Customer sees a Service scorecard and a Documents library shared by their MSP | org portal settings `enable_service`/`enable_documents` on (Settings → Organizations → Portal settings) | PASS |
| #5644 | 2026-09-12 | web | AI-authored script runs in device activity, risk dashboard metrics | Device → Activity tab; `/ai-script-proposals/[proposalId]` (ScriptProposalDetail) | AI-proposal runs carry an "AI-authored" provenance badge and count in AI risk dashboard metrics | `BREEZE_AI_SCRIPT_AUTHORING_ENABLED=true` | PARTIAL |
| #5642 | 2026-09-12 | web | Reviewer-gated unattended lane — script policies, lane state, checkpoint | Settings → AI → "Script authoring" (`/settings/ai-script-authoring`, ScriptAuthoringPage) unattended-lane toggle | Enabling the lane shows lane-state indicator; reviewer-approved proposal can run without a human click | `BREEZE_AI_SCRIPT_AUTHORING_ENABLED=true`, partner admin | PARTIAL |
| #5640 | 2026-09-12 | web | Script proposal approval cards, STRICT acknowledgement, request-changes, verification, promote | `/scripts` → script with pending AI proposal → ScriptProvenancePanel / AiApprovalDialog — "Approve" / "Request changes" / "Promote" | STRICT acknowledgement required before Approve enables; Promote moves reviewed version live | `BREEZE_AI_SCRIPT_AUTHORING_ENABLED=true` | PASS |
| #5636 | 2026-09-12 | api-only | Script proposal reviewer worker — structured verdict, classifier floors, budget reservation | `GET /api/v1/ai/script-proposals/:id` after submit → `reviewVerdict`/`reviewScore` | Submitted proposal gets an automatic structured reviewer verdict on the detail page | flag on; `BREEZE_AI_SCRIPT_REVIEWER_MODEL` optional; needs LLM key | PASS |
| #5630 | 2026-09-12 | api-only | Script proposals, proposal-backed executions, scanner + classifier, propose_script | MCP tool `propose_script` from AI chat; `GET /api/v1/ai/script-proposals` lists new proposal with `scannerFindings` | Asking the AI to write/run a script creates a scanned, classified proposal instead of executing directly | flag on; AI agent access | FAIL |
| #5646 | 2026-09-12 | api-only | Stamp the real approval method on proposal runs and promoted versions | `GET /api/v1/ai/script-proposals/:id` `approvalMethod` after passkey vs step-up-grant vs auto approve | `approvalMethod` matches how the run was actually approved | flag on | FAIL |
| #5608 | 2026-09-11 | web | Reusable step-up grant so consecutive Tier-3 approvals need one ceremony | AiApprovalDialog — approve two sequential Tier-3 actions in one session | Second Tier-3 approval inside the grant window skips the passkey ceremony | MFA-enrolled approver, Tier-3 action | PARTIAL |
| #5602 | 2026-09-11 | web | Stop running a passkey ceremony for every supervised self-approve | AiApprovalDialog — self-approve in "supervised" mode | Self-approve completes without a redundant passkey prompt | MFA-enrolled user, approval mode = supervised | PASS |
| #5598 | 2026-09-11 | web | Bind chat sessions to the page-context device org and use effective approval mode | Device page → AI Assistant chat (AiChatSidebar) → act on that device | Session scopes to the device's org (not default org) and honours that org's approval mode | device outside your default org | FAIL |
| #5595 | 2026-09-11 | web | Persist approvalMode on the first ai_budgets save | Settings → AI Usage & Budget (`/settings/ai-usage`) — Approval mode dropdown on a brand-new budget | Approval mode persists on first save | partner admin | PASS |
| #5597 | 2026-09-11 | api-only | AI credit calls must use the /billing/api/internal prefix | aiCostTracker → billing; `/settings/ai-usage` debits | AI runs debit credits instead of 404ing | billing service | BLOCKED (no billing locally) |
| #5628 | 2026-09-12 | web | Deliverable template sets — apply-to-org/contract, settings page, backfill | Settings → "Deliverable Templates" (`/settings/deliverable-templates`); Org → Contracts tab → "Apply Template" (`apply-template-modal`) | Template set on settings page; Apply Template on a contract creates the deliverable occurrences | partner admin | PARTIAL |
| #5606 | 2026-09-11 | web | Org document library, blob storage extraction and document evidence | Org → "Documents" tab (OrgDocumentsTab) → "Upload document" (`org-documents-upload-open`) | Upload lists with category/portal-visibility flags; can attach as deliverable evidence | `documents:read/write` | PASS |
| #5603 | 2026-09-11 | web | Deliverable sweep worker, ticket integration, key-date reminders, auto-evidence, MCP tools | Org → "Service" tab (OrgServiceTab) — occurrence status/reminder badges | Occurrence auto-transitions (overdue/at-risk) and creates linked ticket/reminder | workers running | PASS |
| #5596 | 2026-09-11 | web | Render deliverable due dates and key dates as calendar dates | Org → Service tab / OrgKeyDatesCard due-date display | Due "2026-09-15" renders as that date regardless of viewer TZ | none | PASS |
| #5556 | 2026-09-12 | web | Fail-closed Stripe Checkout session revocation (SEC-150) | Settings → Connected Apps → Stripe Payments → "Disconnect" (`stripe-disconnect-button`); void/reset/pay with an open Checkout session | Unproven revocation shows `stripe-revocation-blocked` banner + API 503 `STRIPE_REVOCATION_PENDING`; normal path closes the session | Stripe test-mode connected | BLOCKED (no Stripe locally; card render verified) |
| #5560 | 2026-09-11 | web | Stabilize webhook credential identity; stop echoing the secret (SEC-065) | Settings → Connected Apps → Monitoring integration card — save/edit credentials | Re-opened form does not show plaintext secret; identity stable across edits | `APP_ENCRYPTION_KEY_ID` set (else 503) | PASS |
| #5656 | 2026-09-12 | web | M365 manifest v3 + non-interrupting upgrade consent | Settings → Connected Apps → Microsoft 365 cards — Connect/re-consent | Old-manifest tenant upgrades to v3 without forced disconnect | real M365 tenant | BLOCKED |
| #5605 | 2026-09-12 | web | Devices & Assets, recent devices, jump-back palette, keyboard shortcuts | Sidebar "Devices" → "Recent" (`sidebar-recent-devices`) after opening a device; `?` opens shortcuts dialog; `g` then `d`/`a`/`s`/`r` jumps | Recent devices list populates; `?` modal lists shortcuts incl. `g` chords | none | PASS |
| #5274 | 2026-09-11 | web | SSR shell translation — tServer + page titles + error pages | Any `/settings/*` page with a non-English locale — tab title + SSR shell text | Translated title/shell on first paint, no English FOUC | non-English locale | PASS |
| #5588 | 2026-09-11 | web | Bare-metal recovery — Linux live media, recovery console, QEMU e2e (W04b) | Device → Backup tab → RecoveryBootstrapTab "Recovery media" | Generates downloadable live-media image + recovery-console panel | backup-enrolled device | PARTIAL (UI smoke PASS) |
| #5662 | 2026-09-12 | api-only | Desired-state install arming — autoInstall, verb-aware gate, devices.execute+MFA | `PATCH /api/v1/software-policies/:id` `{remediationOptions:{autoInstall:true}}` (MFA step-up) | Policy persists `autoInstall: true`; remediation installs rather than reports | MFA user with software-policy write | PARTIAL |
| #5661 | 2026-09-12 | api-only | Treat lowercase "online"/"connected" UniFi status as online | unifiSyncService background sync | UniFi device with lowercase status shows Online | live UniFi controller | BLOCKED |
| #5620 | 2026-09-12 | api-only | Unblock partner-trust auto-promotion — billing route, IP backfill, parser, hard-deny | partnerTrustJobs background job | Stuck partner auto-promotes on next run | billing service + IP classifier | BLOCKED (verify in prod post-deploy) |
| #5621 | 2026-09-11 | api-only | Immutable script versions — every writer cuts one | `PUT/PATCH /api/v1/scripts/:id`; `GET /api/v1/scripts/:id/versions` | Each edit creates a new immutable version row; versions endpoint lists them | none | PASS |
| #5558 | 2026-09-11 | api-only | MCP re-authorize fails (DCR GC deletes live clients; re-consent invalid_scope) | `GET /interaction/:uid`, `POST /interaction/:uid/consent` during MCP re-auth | Re-auth of a GC'd client succeeds | MCP OAuth client past DCR TTL | PARTIAL (discovery + interaction routes confirmed live; full repro needs prod-aged DCR client) |
| #5540 | 2026-09-11 | api-only | Scope legacy correlations to visible alerts (SEC-049) | `GET /api/v1/alert-templates/correlations` → CorrelatedAlertGroups | Restricted user does not see another org's alerts in a correlation group | two orgs + restricted user | PASS |
| #5594 | 2026-09-11 | api-only | Stop reconciling decommissioned devices (peripheral policy) | peripheralPolicyReconciliation job | Decommissioned device gets no reconciliation writes | decommissioned device w/ policy | PASS |
| #5659 | 2026-09-12 | agent-only | Remote server generation columns + start-intent CAS on all three desktop start sites (SEC-038 W02) | `POST /api/v1/remote/sessions` ×2 rapid from one device | Second launch rejects/reuses cleanly via CAS | live agent | BLOCKED → lab rig |
| #5624 | 2026-09-11 | agent-only | Stop the desktop orphan sweeper finalizing P2P viewer sessions | Remote desktop P2P session held > 5 min; `remote_sessions.error_message` | No `orphan_recovery` finalization mid-session | live agent | PASS (server-side reproduction, see log) |
| #5599 | 2026-09-11 | agent-only | Per-monitor DPI awareness (secondary monitors) | Remote desktop on a 2-monitor Windows device at different DPI | Cursor/click land correctly on monitor 2 | Windows rig, 2 monitors | BLOCKED → lab rig |
| #5638 | 2026-09-12 | agent-only | Recovery console stty so reboot countdown times out | Recovery console session | Countdown elapses | BMR live media | BLOCKED (KIT proof ledger #5639) |
| #5634 | 2026-09-12 | api-only | Re-authenticate slides the recovery download session window | `/api/v1/backup/bmr/tokens` re-auth | Session window extends | BMR media | BLOCKED |
| #5632 | 2026-09-12 | agent-only | Envelope-aware bootstrap decoder for BMR exchange | BMR bootstrap | Envelope decodes | BMR media boot | BLOCKED |
| #5647 | 2026-09-12 | mobile | Mobile platform attestation W07 — surfacing, alerting, docs | Settings → Profile → "Approver devices" (ApproverDevicesSection); mobile banner | Failed-attestation device shows warning state | mobile build | PARTIAL (web section verified) |
| #5168 | 2026-09-11 | mobile | Android StrongBox/TEE key attestation + Play Integrity client | Mobile approver enrollment | Hardware-backed key + Play Integrity | Android device | BLOCKED |
| #5648 | 2026-09-12 | web | Jobs nav + Monitoring hub — W01 discoverability (#5287) — **landed after sweep base** (main 3815ed37b) | Sidebar "Jobs" entry + Monitoring hub page (read apps/web/src/pages for the route) | Jobs nav item present; Monitoring hub lists the monitoring surfaces | none | PASS |
| #5660 | 2026-09-12 | agent-only | In-memory desktop start fence — HWM + terminal tombstone (SEC-038 W04) — **landed after sweep base** | agent Go code; CI `Test Agent` (race) | Two rapid desktop starts fenced in-agent | live agent | BLOCKED → lab rig / CI race tests |
| #5530 | 2026-09-11 | ci-chore | Customer-PII guard ignores systemd unit names | CI script | — | none | N/A |
| #5524 #5658 #5639 #5527 #5633 #5604 | — | docs-chore | plan/spec/docs commits | — | — | none | N/A |

### Env vars new / default-changed in range

| Var | Default | Note |
|---|---|---|
| `STRIPE_SESSION_REVOCATION_MODE` | `enforce` (new; `observe` is the incident de-escalation lever) | #5556 |
| `BREEZE_AI_SCRIPT_AUTHORING_ENABLED` | `true` (new, ON by default) | unset on both prod droplets → will turn ON at deploy; **owner decision** |
| `BREEZE_AI_SCRIPT_REVIEWER_MODEL` | unset → platform default | #5636 |
| `APP_ENCRYPTION_KEY_ID` | behaviour change: API refuses to boot without it when `M365_GRAPH_ACTIONS_TOOLS_ENABLED=true` or `BREEZE_ROLE` is api/worker; integration credential saves 503 without it | #5560 — **verified set + compose-mapped on EU and US** |
| `LENOVO_WARRANTY_ENABLED` | shipped in 0.112.0, still **unset on both droplets** | deploy-day TODO |

### Groups
1. Portal Service & Documents (#5627) · 2. AI script authoring & approvals (#5644 #5642 #5640 #5636 #5630 #5646 #5608 #5602 #5598 #5595) · 3. Deliverables & documents (#5628 #5606 #5603 #5596) · 4. Integrations (#5560, #5540, #5621, #5662, #5594) · 5. Shell & navigation (#5605 #5274 #5647-web #5588-ui) · Lab: #5624 #5659 #5599

## Pre-tag gates (release skill Step 0)

- ✅ **Migration prod-role dry run** — all 16 migrations concatenated into one `BEGIN … ROLLBACK` with `ON_ERROR_STOP`, run as `doadmin` (non-superuser) against BOTH managed DBs (EU 22:5xZ, US 22:5xZ). Result `ALL 16 MIGRATIONS OK IN TXN` on both. Backfill counts: EU `script_versions` head rows 24, US 105; `scripts.origin=system` 24 both; documents permissions seeded + granted to 3 existing role kinds. No duplicate `(script_id, version)` groups. No `OWNER TO` / `CREATE ROLE` / `SECURITY DEFINER` in the range.
- ✅ **Draft GHSA advisories** — none.
- ✅ **Queued-ahead migration pre-check** — merge queue at 00:5xZ held #5649 (m365 tenant sync foundation, adds `2026-10-16-170200-m365-tenant-sync-foundation.sql`, DDL only), #5657, #5485 ahead of #5680. Re-ran the doadmin `BEGIN…ROLLBACK` on EU with all 16 + that file: `ALL 17 MIGRATIONS OK IN TXN`. #5485 (backup GC sweep reclaiming retired/orphaned prefixes) carries no migration but is a deleting worker — call out in the release body.
- ⚠️ **release.yml dry run** (COMPLETED 23:2xZ) (`workflow_dispatch skip_release=true`, run 34723701745) — validates the reworked workflow (#5430 build reuse, #5588 recovery media). Build Helper (linux, windows) + Generate Viewer Update Manifest fail with `tauri.conf.json > version must be a semver string`: **dispatch artifact** — `GITHUB_REF_NAME` is `main`, not a tag, so `MSI_VERSION="main"`. Not a defect; those jobs are unchanged since v0.112.0 (green there). Watching the changed jobs. Build Agent ×5, Build API, Build Web, Release Integrity Gate: ✅. `Build recovery media (arm64)`: ❌ `lb bootstrap` skips foreign-arch stages (`W: skipping … foreign architecture(s)` → `E: the following stage is required to be done first: bootstrap`) — job is `continue-on-error` for arm64 by design (#5588; W08 of #5493 promotes it) and create-release only requires the amd64 ISO, so **not release-blocking**; follow-up for W08: cross-bootstrap needs `--bootstrap-qemu-arch`/`qemu-user-static` inside the container or a native `ubuntu-24.04-arm` runner. `Build recovery media (amd64)` ✅ (the required one). **Coverage gap:** every `Build & Push … Docker Image` job, signing, macOS installer, viewer, and Create Release are `startsWith(github.ref,'refs/tags/v')`-gated and were SKIPPED — so the #5430 release-image path (`apps/api/Dockerfile` + `release-dist` named build-context, different from ci.yml's `docker/Dockerfile.api` smoke build) has zero coverage. Mitigation: local reproduction of the exact buildx invocation + boot + /health (agent, 23:35–23:47Z): ✅ `apps/api/Dockerfile` and `apps/web/Dockerfile` built locally with `--build-context release-dist=<dir laid out exactly as build-docker-api/web download it>`; buildx honoured the named context (no builder rebuild). API image: `dist/index.cjs` + `worker.cjs`, `ee/workspace/dist/web` non-empty, all `2026-10-16-*` migrations present, no `src/` leak; booted on fresh PG16+Redis7 → **698 migrations applied**, `/health` `{status:ok, version:0.113.0-dryrun}`, second boot no-op. Web image: `dist/server/entry.mjs` + `dist/client/` present, answers 200 on :4321. Note: `docker/Dockerfile.{api,web}` (CI smoke) are DIFFERENT files from `apps/*/Dockerfile` (release) — CI smoke does not cover the release path; follow-up to unify.
- ⏳ In-app What's New entry for 0.113.0 — TODO before tag.

## Sweep log (append as you go)

### [Lab gate — #5624 desktop orphan sweeper vs P2P] — main session, 23:06–23:09Z

#### [#5624] — PASS
- Reproduced the exact bug condition against the local API (f175ac6f05): inserted two `remote_sessions` rows `type=desktop status=active started_at=now()` for the seed device. **A** = never owned (no `remote:ws:{desktop:A}:*` keys, i.e. the WebRTC P2P viewer path). **B** = positive control: `remote:ws:{desktop:B}:generation=1` set by hand (once-owned, owner key gone = lost lease).
- After 3 sweep intervals (`REMOTE_WS_SHARED_LEASE_TTL_MS` = 30 s): ✅ **A untouched** — still `active`, no Redis keys, no device command. ✅ **B claimed** — `:finalizing` + `:finalization-payload` keys written with `reason:"orphan_recovery"`, and a `desktop_stream_stop` `device_commands` row (pending, device offline) — i.e. the sweeper still reaps genuine WS-lease orphans.
- Pre-fix behaviour (v0.112.0) would have claimed A the same way as B. Prod confirmation after deploy: `select count(*) from remote_sessions where error_message='orphan_recovery' and created_at > <deploy time>` must stay 0 while a real P2P session is held > 5 min.
- Fixtures deleted after the check.

### [Group 1 — Deliverables, documents, customer portal] — opus, 23:20Z

#### [#5628 Deliverable template sets] — PARTIAL
- ✅ Settings → Deliverable Templates renders; created partner-wide set "QA Gold Tier" (ownerScope radio present, "All orgs" badge shown), added item "Monthly Health Report" (cadence/lead/grace/completion/artifact fields all persist).
- ✅ Created org-owned set "QA Org Tier" + item; Apply Template on an ACTIVE contract (`contract-deliverables-apply-template` → `apply-template-modal`) applied it with effective-from 2026-07-01 and created deliverable `92c6e014…` (anchor 2026-07-31) — `POST /orgs/:orgId/deliverables/apply-template` 200.
- ❌ BUG (partner-wide sets invisible once an org is pinned): `GET /api/v1/deliverable-templates?orgId=22fffba4-…` → **200 `{"data":[]}`** while `deliverable_template_sets` holds the partner-wide row (`org_id NULL`, `partner_id 35be555f-…`). Effect: the Apply-Template dropdown offers only org-owned sets, AND the set disappears from its own Settings page after visiting any org (page then reads "No template sets yet."). Root cause (source-verified): `listTemplateSets` (`apps/api/src/services/deliverableTemplateService.ts:125-128`) appends `eq(deliverableTemplateSets.orgId, q.orgId)` whenever an `orgId` query param is present, and the web `fetchWithAuth` wrapper appends `orgId` to every request once an org is selected — so `org_id IS NULL` partner-wide rows are filtered out. Needs `or(eq(orgId, q.orgId), and(isNull(orgId), eq(partnerId, actor.partnerId)))`. This defeats the headline partner-wide-first capability the page itself advertises.
- ⚠️ UI/UX: Apply Template / the whole deliverables section only appears on an **active** contract — a draft contract renders `ContractEditor`, which has no deliverables block. The inventory click-path ("Org → Contracts tab → Apply Template") is also wrong: the button lives on the *contract detail* page and on the Org Service tab, not on the org's Contracts list.

#### [#5606 Org document library] — PASS
- ✅ Org → Documents tab renders (`org-documents-tab`) with category filter (Baseline/Runbook/Policy/Evidence/Report/Export/Other) and "Show earlier versions" toggle.
- ✅ Uploaded a PDF as "QA Portal-Visible Runbook" (category Runbook, portal-visible ON) and a second as "QA Internal Only Policy" (category Policy, portal-visible OFF) — both land in `org_documents` with `storage_backend=db`, `content_type=application/pdf`, `byte_size=193`, `sha256`, `original_filename`, and the correct `portal_visible` flag.
- ✅ `GET /api/v1/orgs/:orgId/documents/:id/content` → 200 `application/pdf`, 193 bytes, byte-identical (`%PDF-1.4` header) — blob storage round-trips.
- ✅ Non-PDF rejected cleanly: uploading a `.txt` produced the toast "Only PDF, JPEG, PNG and WebP files can be stored." and no row was written (matches the form's stated "PDF, JPEG, PNG or WebP, up to 10 MB").
- ✅ Deliverable evidence: occurrence drawer → "Upload a file as evidence" filed a new `org_documents` row (`category=evidence`, title `qa-doc.pdf`) AND linked it via `service_deliverable_evidence` (`kind=document`, `document_id=6abfa583-…`, `occurrence_id=83371896-…`).
- Note (by design, worth knowing): evidence uploaded through the occurrence drawer defaults to `portal_visible=true`, so it appears in the customer portal's "Delivery evidence" group immediately.

#### [#5603 Deliverable sweep worker / occurrences] — PASS
- ✅ `[DeliverableWorker] Deliverable workers initialized` + daily cron `18 5 * * *`; triggered on demand by enqueuing `deliverable-sweep` onto the `deliverable-jobs` BullMQ queue from inside the api container.
- ✅ Sweep 1: `{"today":"2026-09-12","deliverables":1,"materialized":2,"opened":1,"missed":0}` → `service_deliverable_occurrences` got 2026-07-31 **missed** (past grace) and 2026-08-31 **open** with `ticket_id 7eb6fae4-…`.
- ✅ Ticket integration: `tickets` rows "Quarterly Business Review — Aug 2026" and "TZ Check Report — Sep 2026", both `work_kind='deliverable'`, `source='api'` — month labels match the due dates with no TZ shift.
- ✅ Status badges in UI: Org → Service shows "Missed" for the overdue deliverable, "Due soon" for the one due 9/15, "Late" pills on overdue occurrences in the occurrence drawer.
- ✅ Key-date reminders: added key date 2026-09-15 / remind 10 days before; next sweep logged `keyDateReminders:1`.
- ⚠️ UI/UX: the Org Service tab (and the contract deliverables section) never refetch on focus/interval — data that changed server-side only appears after a full page reload; during this run the table showed `—`/"On track" for several minutes while the API already returned `nextDue 2026-09-15`/`due_soon`.
- ⚠️ UI/UX: the occurrence drawer labels every occurrence's due date "Next due:" — including already-missed past occurrences ("Next due: 7/31/2026" on a row badged Missed).

#### [#5596 Due dates render as calendar dates] — PASS
- ✅ Browser TZ confirmed `America/Denver` (UTC-6) — a UTC-midnight parse would shift each date back one day.
- ✅ Deliverable due **2026-09-15** renders **9/15/2026** in the Service table NEXT DUE column and in the "Upcoming 90 days" card ("TZ Check Report · No contract · 9/15/2026"). Occurrence row 2026-08-31 renders "8/1/2026 – 8/31/2026"; 2026-07-31 renders 7/31/2026.
- ✅ Key date **2026-09-15** renders **9/15/2026** in OrgKeyDatesCard.
- ✅ Server-side month labels agree (ticket subjects "… — Aug 2026" / "… — Sep 2026").
- ⚠️ UI/UX: the key-date summary line prints raw form labels — "9/15/2026 · Remind (days before): 10 · Show in customer portal" — instead of readable prose.

#### [#5627 Customer portal Service + Documents] — PASS
- ✅ Gating proven in both states: Settings → Organizations → Default Organization → Customer Portal shows `org-portal-toggle-enableService` / `org-portal-toggle-enableDocuments`, both default **off**; with them off the portal nav had no Service/Documents entries. After "Save portal settings" (`portal_branding.enable_service='t'`, `enable_documents='t'`), the portal nav renders `Service → /portal/service` and `Documents → /portal/documents`.
- ✅ `/portal/service` scorecard shows both deliverables grouped by contract ("QA Managed Services" / "Other services") with customer-facing statuses MISSED and DUE SOON, "Next due: Sep 15, 2026", per-row "Show history", and a Key dates block listing the portal-visible key date "QA Cyber Insurance Renewal — Sep 15, 2026". Dates are calendar-correct in an America/Denver browser.
- ✅ `/portal/documents` lists exactly the two portal-visible documents, grouped by category ("Runbooks", "Delivery evidence"), "2 DOCUMENTS AVAILABLE". The non-portal-visible "QA Internal Only Policy" is absent.
- ✅ Server-side enforcement, not just UI filtering: `GET /api/v1/portal/documents/<visible-id>/content` → **200 application/pdf**; the same call for the non-visible document's id → **404**.
- ⚠️ UI/UX: a deliverable whose only live occurrence is already overdue reads "MISSED · Next due: Not scheduled" to the customer, even though an open occurrence and a linked ticket exist. `serviceReadModel.ts:320-322` filters nextDue on `o.dueAt >= today`, so past-due-but-open occurrences are dropped. "Missed and nothing scheduled" is the worst possible read for a customer whose work is in fact in flight.

### [Group 2 — AI script authoring & approvals] — opus, 2026-09-12 23:30Z

Setup: enrolled TOTP MFA on `admin@breeze.local` through Settings → Profile (`mfa-setup-start` → password → `mfa-totp-secret` → 6-box OTP → "Verify and enable"; `users.mfa_enabled = t`). Codes computed with pyotp.

#### [#5595] — PASS
- ✅ `ai_budgets` was **empty** before the test (brand-new budget, which is exactly the #5595 regression condition).
- ✅ Settings → AI Usage & Budget: set Approval Mode to **"Hybrid Plan + Abort"** (≠ the "Per Step (default)" preselect), Monthly $50 / Daily $10, clicked Save Budget.
- ✅ psql: the first-ever `ai_budgets` row landed as `approval_mode = hybrid_plan`, `monthly_budget_cents 5000`, `daily_budget_cents 1000` — the mode was **not** dropped on the first save.
- ✅ Full page reload → the `<select>` reads back `hybrid_plan` and the budgets read back 50.00 / 10.00.

#### [#5642] — PARTIAL (lane reset is broken — see BUG)
- ✅ `/settings/ai-script-authoring` renders both cards: "Partner ceiling" and "This organization".
- ✅ Partner ceiling saved as a true **partner-wide** row: `PUT /api/v1/partner/ai/script-policy` 200 → `ai_script_policies` row with `org_id NULL, partner_id 35be555f-…`, `unattended_allowed t`, `max_unattended_risk_tier medium`, `max_unattended_per_hour 5`, `unattended_allowed_classes {temp_files,processes}` — correct org-XOR-partner dual-ownership shape.
- ✅ **Ceiling really narrows the org card**: after saving the partner classes as `{temp_files,processes}`, every other class checkbox in the org card went `disabled` with the reason text "Your partner does not allow this.", and the permanently-forbidden set (firewall, credentials, users_groups, disk, boot, security_tooling, shell_eval) read "Never available unattended, whatever the ceiling says." The org's `max-per-hour` also inherited the ceiling's 5.
- ✅ **Org opt-in is genuinely step-up-gated**: with no MFA enrolled, clicking Save issued **no PUT at all** and surfaced "Enabling unattended runs needs a fresh security check." After TOTP enrolment, toggling the org switch rendered the "Confirm it is you" panel with `approver-stepup-code`; a valid TOTP + Save → `PUT /api/v1/ai/script-policy` 200 and a **second** `ai_script_policies` row (`org_id` set, `partner_id NULL`) with `unattended_enabled t`, `unattended_allowed_classes {temp_files}`, and crucially `unattended_enabled_by` + `unattended_enabled_at 2026-09-12 23:35:08Z` stamped. Reload → both toggles and the class read back from the server.
- ✅ **Lane-state indicator works**: forced `ai_script_lane_state` to `state='open', opened_reason='3 consecutive failed verifications'` → the page renders `script-lane-banner`: "Unattended lane is paused — 3 consecutive failed verifications Unattended runs are refused until an approver resets the lane. Approvals still work normally." with a "Reset the lane" button.
- ❌ **BUG (lane can never be un-paused from the UI by a TOTP approver).** Clicking "Reset the lane" → `POST /api/v1/auth/mfa/step-up` **400 Bad Request**, page shows `script-authoring-error` = "code: Invalid code". The grant is minted with an **empty** code because the TOTP input is never rendered on the reset path. Source-verified: `apps/web/src/components/settings/ScriptAuthoringPage.tsx:388` — `showTotpBox = orgDraft?.unattendedEnabled && !(orgPolicy?.unattendedEnabled ?? false) && reauthTier === 'totp'`, i.e. the code box only exists **while turning the lane ON**. `handleReset` (same file, ~line 353) reuses that same `reauthCode` state, which by definition is unreachable once `orgPolicy.unattendedEnabled` is already true — which is always the case when a lane is paused. Effect: the safety breaker trips and the MSP has no in-product way to clear it (passkey approvers are unaffected; `showPasskeyNote` is cosmetic and the passkey ceremony needs no typed code). Fix shape: drop the `!orgPolicy.unattendedEnabled` clause when a reset is pending, or give the banner its own StepUpPrompt.
- ⚠️ UI/UX: on a partner row that does not exist yet, every class checkbox renders **unchecked**, so the very first partner save writes `unattended_allowed_classes = '{}'` — silently discarding the column's own DB default (`{services,processes,temp_files,dns_cache,printing}`) and leaving the org card with *nothing* selectable ("Your partner does not allow this." on all 19 classes). Direction is fail-safe, but the form should seed from the defaults it is about to overwrite.
- ⚠️ UI/UX: the step-up error ("Enabling unattended runs needs a fresh security check.") renders in a banner at the **top** of the page, while both Save buttons sit far below the fold on this very long form — clicking Save appears to do nothing at all.

#### [#5630] — FAIL (propose_script cannot write a row for a partner-scoped user)
- ✅ The tool IS reachable and IS called: from the AI Assistant chat ("Use the propose_script tool to propose a PowerShell script for device e2e-windows.local that lists the 10 largest files in C:\\Temp"), the model called `propose_script` — `ai_tool_executions` shows two `propose_script` rows.
- ❌ **BUG: every `propose_script` call from a partner-scope session dies on a NOT NULL violation, so no proposal is ever written.** `ai_tool_executions.status = failed`, `error_message = "The tool could not complete this request. Details were recorded in the server logs."` (the user-facing text says nothing). API log: `[aiTools] propose_script threw: Failed query: insert into "script_proposals" (...)` and the bound params begin `params: ,chat_session,,,powershell,…` — **`$1` (`org_id`) is empty**. Reproduced the exact server error by hand: `insert into script_proposals (org_id, …) values (null, …)` → `ERROR: null value in column "org_id" of relation "script_proposals" violates not-null constraint`.
- **Root cause (source-verified, two separate sites, same mistake):**
  - `apps/api/src/services/scriptProposals/proposals.ts:50` — `orgId: auth.orgId!`. The non-null assertion is false for a partner-scope token: `apps/api/src/middleware/auth.ts:627` sets `orgId: payload.orgId`, which is null for `scope: 'partner'`, and `apps/api/src/routes/ai.ts:997` says so in a comment of its own — *"Fetch session first for orgId (auth.orgId is null for partner/system users)"*. Every other `ai.ts` route resolves `const orgId = c.req.query('orgId') || auth.orgId`; this service does not, and the chat tool path hands the handler the raw user `AuthContext` (`aiAgentSdkTools.ts:512` `getAuth()` → `executeTool(…, auth)`), never the session's org.
  - `apps/api/src/services/scriptProposals/runnable.ts:48` — `if (proposal.orgId !== auth.orgId) return { ok: false, reason: 'wrong_org' }`. Same null, so even a proposal created another way can never be RUN by a partner-scoped approver (proven below).
- **Blast radius:** the partner-scope MSP admin is the primary persona and is the only shape the seeded `admin@breeze.local` has. On this stack the feature is unusable end-to-end from the UI: you cannot create a proposal, and an approved proposal-backed run is refused. An org-scope token would work — but nothing in the product tells the operator that. Everything below had to be driven from a hand-seeded `script_proposals` row.
- Non-defect noted in passing: the model's *first* `propose_script` attempt failed input validation (`verification.kind: Invalid discriminator value`) and it self-corrected on the retry — the schema is doing its job.

#### [#5636] — PASS
- ✅ Seeded one `script_proposals` row (the exact content/goal/digest the model produced above, `status=proposed`) and enqueued a real `script-review` BullMQ job via `enqueueScriptReview()` inside the api container.
- ✅ Within ~20 s the worker wrote **two** `script_proposal_reviews` rows, in order: `reviewer_kind=static_scan` (`summary` "0 STRICT hit(s), 0 BASIC hit(s); touch classes: temp_files", verdict carries `scannerVersion 2026-09-11.1`, `touchedNames`), then `reviewer_kind=model` (`model claude-sonnet-4-6`, `reviewer_prompt_version 2026-09-11.1`).
- ✅ The model verdict is genuinely **structured**, not prose: `risk_tier low`, `goal_match yes`, `reversible t`, `verification_adequate f`, `recommended_action changes`, plus a `findings[]` array of 4 entries each with `severity` (info/info/warning/info) and `lineRef` (1,1,1,4), and `blastRadius: ["e65460f3-…"]`. The warning finding is substantively right: it caught that `verification = {kind: exit_code, equals: 0}` cannot prove the script produced output.
- ✅ **Budget reservation taken**: `budget_reservation_id ab887563-…` on the model row, `input_tokens 743 / output_tokens 524`. The proposal itself was moved to `status=reviewed`, `risk_tier=low` — the classifier floor honoured the org ceiling ("Risk is low and consistent with org ceiling").
- ⚠️ `script_proposal_reviews.cost_cents` is **NULL** on the model row (the static row has `0.0000`) even though a reservation exists and both token counts are populated — the per-review cost never lands, so a cost report over this table under-counts model reviews.

#### [#5640] — PASS
- ✅ Reached the real card by having the assistant call `run_script` with the seeded `proposalId` — `action_intents` row created (`risk_tier 3`, `approval_scope supervised`, `target_summary run_script(proposalId=…, deviceIds=[…])`) plus an `approval_requests` row (`risk_tier high`, `status pending`), and `script_proposals.intent_id` linked.
- ✅ `/approvals` shows the row with an **"AI-authored script"** disclosure (`approval-script-review-toggle-…`). Expanded, `ScriptProposalApprovalCard` renders every element the row promises, each with its own testid: `script-proposal-risk-low`, `-summary` (the reviewer's prose), `-goal`, `-expected-effect`, `-verification` (rendered as readable prose, "The script exits with code 0", not raw JSON), `-rollback`, `-finding-0..3` (severity-labelled, with "(L1)"/"(L4)" line refs), `-blast-radius`, `-touch-temp_files`, `-devices` + `-device-<id>` resolved to the hostname `e2e-windows.local`, `-body` (the full script with line numbers 1-5), `-expiry`, `-verification-state` ("Verification pending"), and the three actions `-approve-button` / `-request-changes-button` / `-reject-button`.
- ✅ "Request changes" opens an in-card note drawer (`script-proposal-note-input` + `script-proposal-request-changes-submit`) rather than deciding immediately.
- ✅ Approve works end to end: `approval_requests.status = approved`, `action_intents.decided_at` stamped.
- ⚠️ **NOT exercised: the STRICT acknowledgement ceremony.** This proposal scanned clean (`strict_hits {}`), so the gate correctly did not engage — the DTO carries `viewer.canAcknowledge: true` and `acknowledgedPatterns: []`, but no STRICT-tier proposal was available to prove Approve stays disabled until acknowledged. Needs a proposal with a STRICT pattern to close.
- ⚠️ **NOT exercised: Promote.** `viewer.canPromote` came back **false** for a platform-admin partner user with scripts:write and MFA enrolled — `POST /:id/promote` also requires the proposal to be *verified*, and verification can never complete while the run is blocked (below). Promote is untested.
- ⚠️ UI/UX: the approval row's headline reads **"Run script unknown... on 1 device(s)"** — for a proposal-backed run there is no library script, so the label falls back to "unknown" instead of using the proposal's goal, which the card right underneath displays in full. The one line an approver reads first is the least informative one on screen.

#### [#5602] — PASS
- ✅ Approved a **Tier-3 / High-risk** AI action (`action_intents.risk_tier = 3`, `approval_requests.risk_tier = high`) from `/approvals` with a single click: **no passkey prompt, no TOTP prompt, no re-auth panel of any kind** appeared, and the card cleared to "No approvals waiting".
- ✅ The decision record confirms the supervised path was the one taken, not a silently-skipped ceremony: `action_intents.approval_scope = supervised`, `decided_via = session_tap`, `decided_assurance_level = 1`, `decided_by_user_id` = the same user who requested it (self-approve), `approval_requests.decided_at 2026-09-12 23:47:33Z`.

#### [#5608] — PARTIAL (grant path not reachable with one user)
- ✅ First half established: a supervised self-approve is decided `via session_tap` with `decided_via_step_up_grant = false` — i.e. it consumed no grant, consistent with #5602 exempting supervised self-approve from the ceremony entirely.
- BLOCKED on the second half: the step-up grant only comes into play for a **four_eyes** approval (a different approver than the requester). This stack has exactly **one** user (`admin@breeze.local`), so every intent the AI raises classifies `supervised` and never prompts a ceremony in the first place — there is no "first ceremony" for a second approval to reuse. `approval_requests.decided_via_step_up_grant` stayed `false`, correctly, but that proves nothing about grant reuse. Needs a second MFA-enrolled user in the same org and a four_eyes-classified Tier-3 action.

#### [#5646] — FAIL (blocked by the #5630 root cause; and the field is not on the route the row names)
- ❌ **BUG: an approved proposal-backed run is refused with `wrong_org` for a partner-scoped approver.** After the approve above, the intent released and immediately failed: `action_intents.status = failed`, `error_code = tool_execution_failed`, `result = {"error": "proposal_not_runnable: wrong_org", "proposalId": "2bcec10a-…"}`. The proposal's `org_id` and the device's `org_id` are both `22fffba4-…`; the mismatch is against `auth.orgId`, which is null — `apps/api/src/services/scriptProposals/runnable.ts:48` `if (proposal.orgId !== auth.orgId)`. It should be `!auth.canAccessOrg(proposal.orgId)`. Consequence: **zero rows in `script_executions`**, so `approval_method` is never stamped on anything, and this row cannot be verified. This is the same partner-scope null as #5630 but in a second, independent code path — fixing only the insert would leave the run still broken.
- ⚠️ Row-description mismatch: `approvalMethod` is **not** a field of `GET /api/v1/ai/script-proposals/:id`. The full DTO (verified, 3282 bytes) is `{proposal, review, devices, executions, verification, viewer}` and contains no `approvalMethod` at any level. The field lives on `ScriptVersionDto` (`packages/shared/src/types/scriptProposals.ts:193`, i.e. a *promoted library version*) and on the execution provenance record. The derivation itself is source-correct (`approvalMethod.ts`: `decided_via='script_reviewer'` → `unattended_reviewer_gated`; else `supervised` → `supervised_self`, `four_eyes` → `four_eyes`), and this run's intent carries exactly `approval_scope=supervised, decided_via=session_tap`, so it *would* have stamped `supervised_self` had the run not been refused — but that is reasoning, not an observation.

#### [#5644] — PARTIAL
- ✅ **Risk dashboard metrics count it.** `/ai-risk` → "Script Proposals" tab renders `script-proposals-unattended-card` and `script-proposals-lane-card`; `GET /api/v1/ai/admin/script-proposals-metrics` returns `{"perDay":[{"date":"2026-09-12","count":1}],"unattendedRuns":0,"laneState":"closed","reviewerDisagreements":{"humanRejectedAfterApprove":0,"humanApprovedAfterReject":0}}` — the seeded proposal is counted, and `laneState` tracked my `ai_script_lane_state` edits live (read "Closed" after I reset it).
- ❌ **Provenance badge not verifiable**: Device → Activity for `e2e-windows.local` shows no "AI-authored" badge, because the run never produced a `script_executions` row (`wrong_org`, see #5646). The Activity feed itself works (it lists the CIS-benchmark agent command). Blocked on the same fix.
- ⚠️ UI/UX: the "Proposals per day" chart uses `<Line dot={false}>`, so a series with a **single** day renders an entirely empty plot area — the y-axis auto-scales to 0-4 and the date tick shows, but nothing is drawn, reading as "no data" when the count is 1. Needs `dot` enabled (or a bar) for short ranges.

#### [#5598] — FAIL (chat session does not rebind to the page-context device's org)
- Built the fixture the row asks for: created a second org **QA Sweep Org B** (`11111111-…`, same partner) with site "QA Site B" and device **`qa-orgb-win.local`** (`33333333-…`). Left the header org switcher **pinned to Org A ("Default Organization")**.
- ✅ Control: on `/devices/e65460f3…` (Org A) the sidebar answered correctly about that device; `ai_sessions` row `7aa4a9c8-…` carries `org_id = 22fffba4` (Org A).
- ❌ **BUG:** opened `/devices/33333333-…` — the page header correctly reads "QA Sweep Org B", and the chat sidebar's own context chip correctly reads **`qa-orgb-win.local`** — then asked "Which site is this device in?". The sidebar **reused the Org A session** instead of opening one for Org B: no new `ai_sessions` row was created (still 2 rows, both `org_id = 22fffba4`), every new `ai_messages` row landed on session `7aa4a9c8-…`, and the assistant answered about the **wrong device entirely**: *"The device **E2E Windows Test Device** is located in: Site: **Default Site**, Organization: **Default Organization**."*
- So the sidebar's device chip rebinds but the session does not: `ai_sessions.org_id` stays the previous page's org, the prior device context persists in the transcript, and the operator gets a confident, wrong answer about a device they are not looking at. Not a tenant-isolation breach here (one partner admin can see both orgs) — but an org-restricted user who navigated the other way would be transacting under the wrong org, and the effective approval mode read from that org would also be the wrong one, which is the other half of what #5598 claims.
- Fixture left in place (org `11111111-…`, site `22222222-…`, device `33333333-…`) so the repro survives; delete them before any tenancy-cascade check.

### [Group — area] — agent, timestamp

#### [#PR / area] — PASS | PARTIAL | FAIL | BLOCKED
- ✅ what was checked, concretely
- ❌ BUG: symptom → API actual (status + body) vs UI actual
- ⚠️ UI/UX: paper cut (also copy to the list below)
- BLOCKED: prerequisite

### [Group 4 — API-only rows] — sonnet, 2026-09-12 23:20Z

#### [#5621] — PASS
- Created script (POST /scripts, 201, origin=human), PATCH content twice (PUT /scripts/:id x2, 200/200): version 1->2->3.
- GET /scripts/:id/versions returned 3 rows, monotonic versions 3/2/1, each with distinct `contentDigest` set and `origin: human`.
- psql as table owner `breeze` (superset of `breeze_app`): `UPDATE script_versions SET content_digest=... WHERE id=...` -> `ERROR: script_versions rows are immutable (id=...); cut a new version instead` (trigger `breeze_script_versions_immutable()`). Immutability holds even for the owning role, not just breeze_app.

#### [#5662] — PARTIAL
- Confirmed route: `PATCH /api/v1/software-policies/:id` (apps/api/src/routes/softwarePolicies.ts:535) is gated by `requireMfa()` per the route source.
- Could not observe the 4xx: local stack has `ENABLE_2FA` unset (confirmed via `docker exec api env`), and `hasSatisfiedMfa()` (apps/api/src/middleware/auth.ts:915) short-circuits to `true` whenever `!ENABLE_2FA` — so `requireMfa()` is globally bypassed locally by design, not a defect. PATCH with `{"remediationOptions":{"autoInstall":true}}` returned 200 and persisted `autoInstall:true` (approvalGeneration incremented 1->2). This is expected given the flag; the MFA gate itself cannot be exercised without `ENABLE_2FA=true` configured.

#### [#5540] — PASS
- Built a real cross-org fixture: created "QA Sweep Org B" (2nd org under the same partner), an org-scoped role+user restricted to Org A (`alerts:read` only), 2 alerts in Org A + 2 in Org B, one correlation row within Org A and one cross-org (Org A parent -> Org B child).
- Logged in as the Org A restricted user (JWT `scope:"organization"`, `orgId`=Org A). `GET /api/v1/alert-templates/correlations` returned only the within-Org-A correlation (`total:1`) — the cross-org correlation was filtered out, confirming `filterCorrelationsToVisibleAlerts` scopes correctly.
- Cleanup: all QA fixture rows (org, role, user, sites, devices, alerts, correlations) deleted after the test; verified counts back to baseline (1 org, 1 user).

#### [#5594] — PASS
- Inserted a `decommissioned` device (protocol v2) + a `peripheral_policy_device_states` row (delivery_status=pending, baseline `updated_at`).
- Confirmed via psql that the sweep's own device-selection predicate (`is_ephemeral=false AND peripheral_policy_protocol_version=2 AND status != 'decommissioned'`) returns 0 rows for this device.
- Ran the real worker function in-process (`tsx` invoking `processPeripheralPolicyReconciliationSweep()` from apps/api/src/jobs/peripheralJobs.ts inside the api container) with a positive control (2 online org devices bumped to protocol v2): result `{scanned:2, queued:2, incompatible:0}` — the decommissioned device was never scanned. Its `peripheral_policy_device_states.updated_at` was byte-identical before and after the sweep ran.
- Cleanup: fixture device/state rows removed; protocol-version bump on the 2 control devices reverted.

#### [#5646 / #5630] — PARTIAL
- `docker exec api env | grep -i anthropic` shows `ANTHROPIC_API_KEY` set locally — not BLOCKED on missing LLM key as anticipated.
- Not exercised: `propose_script` is only reachable via the AI chat pipeline (MCP tool call from an active chat session), not a direct REST endpoint — `apps/api/src/routes/ai/scriptProposals.ts` only exposes GET-by-id and two POST review-action routes, no proposal-creation route. Standing up a full chat session (streaming + LLM tool-call round trip) is out of scope for an API-only curl/psql pass in the remaining time; recommend the browser-driving agent exercise this via the AI Assistant chat UI instead.

#### [#5558] — PARTIAL
- `GET /.well-known/oauth-authorization-server` -> 200, full discovery doc (issuer, authorization/token/registration endpoints, `mcp:read/write/execute` scopes).
- `GET /.well-known/oauth-protected-resource` -> 200.
- `GET /api/v1/oauth/interaction/:uid` (route confirmed mounted at apps/api/src/index.ts:478 -> apps/api/src/routes/oauthInteraction.ts:108) returns 401 "Missing or invalid authorization header" for a bogus uid — proves the route is live and reachable, not a 404. `/interaction/:uid/consent` POST route also confirmed present in source (line 222).
- BLOCKED (as expected): reproducing the actual DCR-GC'd-client re-auth failure needs a real client past its DCR TTL, which requires prod state per the row's own note.

### [Group 3+5 — Integrations, shell & navigation] — opus, 00:10Z

#### [#5605 Devices & Assets, recents, jump palette, shortcuts] — PASS
- ✅ **Fresh-login sidebar is complete** (the thing to re-check from the earlier report): after a clean login + TOTP, `aside` enumerates Dashboard, Organizations, **Devices & Assets**, Alerts, Approvals, Incidents, Remote Access, Scripts, Patches, Vulnerabilities as top-level items, plus collapsible AI / Fleet Management / Security / Backup / Service Desk / Billing / Reporting / Settings / Administration groups. Nothing is hidden from a partner platform-admin; the earlier "only Dashboard/Approvals/AI/Settings" observation is the **no-JS SSR shell** (confirmed by `curl` of `/settings/billing` — the server-rendered nav really does contain only Dashboard, Approvals, AI, Settings, Partner, Integrations, Saved Filters until the island hydrates), not collapsed state.
- ✅ `sidebar-recent-devices` populates: after opening seed device `42fc7de0-…`, the block lists "E2E macOS Test Device, qa-orgb-win.local, E2E Windows Test Device" (most-recent first).
- ✅ `?` opens the shortcuts dialog listing ⌘K / `/` / `[` / ⌘⇧H / `?` / Esc plus all nine GO TO chords with their destinations.
- ✅ Chords navigate: `g`→`d` → `/devices`, `g`→`a` → `/alerts`, `g`→`s` → `/scripts`, `g`→`r` → `/remote` (each verified by resulting URL + page title).
- ✅ Jump-back palette (`/` or ⌘K): empty-state sections render as `palette-section-recent-devices` (3 devices), `palette-section-recent-pages` (Scripts, Alerts, Remote Access, Devices & Assets, Dashboard — in visit order) and `palette-section-quick-actions`.
- Note, not a defect: `CHORD_TIMEOUT_MS = 1000`, so a `g` … `d` pair separated by >1 s is dropped. Human-speed typing is fine; an automated driver has to send both keys inside the window.

#### [#5274 SSR shell translation] — PASS
- ✅ Switched locale via Settings → Profile → Language → `locale-option-de-DE`; the tab title changed to "Profileinstellungen | Breeze RMM" immediately.
- ✅ **First paint with no JS at all** (`curl -s -H 'Cookie: breeze.locale=de-DE'`): `/settings/billing` → `<html lang="de-DE">`, `<title>Abrechnungseinstellungen | Breeze RMM</title>`, and the SSR page heading/description are German ("Rechnungswährung, Steuern, Nummerierung, Bedingungen und Fußzeile für Ihre Kunden."). `/settings/roles` → `<title>Rollen | Breeze RMM</title>`.
- ✅ 404 page fully translated in the SSR HTML: "Seite nicht gefunden … Die gesuchte Seite existiert nicht oder wurde möglicherweise verschoben. Zum Dashboard / Anmelden".
- ✅ Accept-Language fallback (no cookie): `Accept-Language: it-IT` on an unknown path → `<html lang="it-IT">` + `<title>Pagina non trovata | Breeze RMM</title>`, so the #5041 review request (browser-detected locale no longer pinned to `en`) is closed.
- Scope note: the React sidebar/header strings in the no-JS shell are still English (Dashboard, Approvals, Settings, "Search Breeze", "What's new"). That is **outside this PR** — #5274 covers titles, meta and static error-page prose; island adoption is Task 4 of #3859.
- Locale restored to English at the end of the run.

#### [#5560 SEC-065 monitoring integration credentials] — PASS
- ❌→✅ **Stack gap found first**: `APP_ENCRYPTION_KEY_ID` was **empty** in the sweep stack's api container (the inventory's prerequisite line assumed it set). The first save therefore returned **`PUT /api/v1/integrations/monitoring` → 503** with `{"error":"Integration secret grafana.apiKey cannot be stored: APP_ENCRYPTION_KEY_ID is not configured on this API instance, so the credential would be sealed as non-AAD enc:v1 ciphertext with no binding to its provider, organization or endpoint. Set APP_ENCRYPTION_KEY_ID (alongside APP_ENCRYPTION_KEY) and restart the API."} — i.e. **the fail-closed path works exactly as designed and the message is operator-actionable**. Set `APP_ENCRYPTION_KEY_ID=qa-sweep-key-1` in the worktree `.env` and recreated the api container to continue. (Prod parity is separately recorded as verified on EU/US.)
- ✅ No echo: saved `grafana.apiKey = QA-SECRET-5560-abc`; the PUT response and a **hard-reloaded** GET both return `"apiKey":"********"` and `webhooks.endpoints[0].url":"********"` — the plaintext appears in no response body (checked via `browser_network_request` response bodies, not just the rendered form).
- ✅ Webhook URL is treated as a secret too (`isSecretPath` covers `webhooks.endpoints[].url`), so the previously plaintext-echoed Datadog URL is now masked.
- ✅ Identity stable: saving again **without touching the secret** (form holds the `********` sentinel) returns 200 and the endpoint keeps `"id":"wh-1"`, and `grafana.apiKey` still masks as `********` rather than collapsing to `""` — the sentinel was not written over the stored credential.
- Evidence limitation (stated rather than glossed): the psql check in the row's expectation **cannot apply** — `monitoringSettings` (and every other family in `routes/integrations.ts`) is an in-memory `Map`, not a Postgres table, so there is no row id/ciphertext to diff, and these settings do not survive an API restart at all. Ciphertext-unchanged is therefore inferred from the stable id + still-masked (non-empty) value, not directly observed.

#### [#5647 web part — Approver devices] — PARTIAL (as scoped)
- ✅ Settings → Profile renders the **"Approval security"** section: explanatory copy, the empty state ("No approver devices registered yet. Sign in to the Breeze mobile app on your phone and it appears here automatically, or register this browser below…"), and the "Register this browser" form (Device name + Authenticator code + Register this device). No console errors.
- Mobile attestation + failed-attestation warning state remain unverified (needs a mobile build) — out of scope by instruction.

#### [#5588 recovery media — UI smoke] — PARTIAL (UI smoke PASS)
- ✅ `/backup#recovery-bootstrap` ("Recovery Bootstrap", ALPHA badge) renders fully after its lazy chunk loads: CLI template (`breeze-backup bmr-recover --token <token> --server <api-server>`), "Bare-metal recovery" (snapshot picker + Original/New device identity + Create recovery code), Create recovery token (snapshot / restore type / expiry / target-config JSON), Bootstrap detail, Load-token-by-ID, Filter catalog (status + restore type + search), empty token catalog, and the plaintext-token handling warning.
- ✅ **0 console errors** on that tab. No media generated, no token created (per instruction).
- Click-path correction for the inventory: the tab is on the **/backup dashboard** (`BackupDashboard` tab `recovery-bootstrap`), not on a device's Backup tab.

#### [#5556 SEC-150 Stripe revocation] — BLOCKED (no Stripe locally)
- ✅ Render check only: Settings → Integrations → **Accounting → Payments** shows the "Online payments" card ("Paste your Stripe secret key … Charges run directly on your own Stripe account — funds never touch Breeze") with **only** a "Save key" button — no Disconnect/Revoke/Replace control while unconnected, so `stripe-disconnect-button` correctly does not render.
- Note: the card is under Integrations → Accounting → Payments, **not** Settings → Connected Apps (that page is the MCP-client list). Worth fixing in the row's click-path.
- Revocation behaviour (`stripe-revocation-blocked` banner / 503 `STRIPE_REVOCATION_PENDING`) remains unproven — needs Stripe test mode.

### [Post-base — #5648 Jobs nav + Monitoring hub] — sonnet, 2026-09-13 00:52Z

#### [#5648] — PASS

- ✅ Fresh login (`admin@breeze.local`) shows a top-level **Jobs** nav entry (right after Scripts) → `/jobs`; renders `AutomationsPage` with heading "Jobs" and the 5 hash-based trigger tabs (All / Scheduled / On demand / Webhooks / Event rules). Clicking "Event rules" updates the URL to `/jobs#event-rules` with no reload and no console error.
- ✅ `/jobs/new` renders "New Job" (title `New Job | Breeze RMM`).
- ✅ Legacy `/automations` → 301 → `/jobs`; `/automations/new` → `/jobs/new`. Both confirmed via direct navigation.
- ✅ **Monitoring hub**: Fleet Management section's nav entry now reads "Monitoring" (not "Network Monitor") → `/monitoring`, which renders a path-based tab strip (Network / Delivery) above the existing `MonitoringPage` (Assets / Network Checks / SNMP Templates sub-tabs). `GET /api/v1/monitoring/assets` and `/api/v1/snmp/templates` both 200.
- ✅ Delivery tab → `/monitoring/delivery` renders breadcrumb (Monitoring › Delivery) + tab strip + `NotificationChannelsPage` (Notification Channels, Configuration Policies link, New Channel, Notification Routing Rules). `GET /api/v1/alerts/channels` and `/api/v1/alerts/routing-rules` both 200.
- ✅ No non-2xx API calls on any of the four pages visited (jobs, jobs/new, monitoring, monitoring/delivery), aside from the known pre-existing `/api/v1/reliability/<deviceId>` 404 on device pages (not touched here).
- ❌ **BUG**: `/monitoring` and `/monitoring/delivery` both throw a React hydration-mismatch console error on every load. `MonitoringTabStrip.tsx` calls `t('monitoringTabs.ariaLabel')` / `t('monitoringTabs.network')` / `t('monitoringTabs.delivery')` (`apps/web/src/components/monitoring/MonitoringTabStrip.tsx:34,41`) inside an Astro `client:load` island; on the server-rendered pass `react-i18next` isn't initialized yet, so the SSR HTML contains the literal keys (`monitoringTabs.ariaLabel`, `monitoringTabs.network`) while the client hydrates with the real translated strings ("Monitoring sections", "Network", "Delivery") — React discards and re-renders the whole subtree. Cosmetically invisible (resolves within a frame) but it is a genuine, reproducible console error on two brand-new pages, and the "no console errors" bar this PR was supposed to clear is not met. Filed as paper cut row 14 below (recommend: pass `keyPrefix` translations already resolved server-side, or gate the initial render on `i18n.isInitialized` like other SSR'd islands in this app, if any already do).

Setup note: `admin@breeze.local` had TOTP MFA enrolled by an earlier group in this same sweep; temporarily flipped `users.mfa_enabled` to `false` in the local Postgres container to reach the login-protected UI, then restored it to `true` immediately after this smoke (verified `mfa_enabled=t` post-restore). No code touched.

## UI/UX paper cuts

| # | Where | Observation | Severity | Disposition (fixed <sha> / issue # / noted) |
|---|---|---|---|---|
| 1 | Contract detail (draft) | Apply Template / the whole service-deliverables section only render once a contract is **active**; a draft contract shows `ContractEditor`, which has no deliverables block — no hint that activating reveals them | low | noted |
| 2 | Org → Service tab, contract deliverables | Tables never refetch (no focus/interval revalidation); server-side changes (sweep results) only appear after a full page reload — the table showed `—`/"On track" while the API already returned `nextDue 2026-09-15`/`due_soon` | medium | noted |
| 3 | Occurrence drawer | Every occurrence's due date is labelled "Next due:", including already-missed past occurrences ("Next due: 7/31/2026" on a row badged Missed) | low | noted |
| 4 | Org overview → Key dates card | Summary line prints raw form labels — "9/15/2026 · Remind (days before): 10 · Show in customer portal" — instead of prose | low | noted |
| 5 | Customer portal → Service | A deliverable with an open-but-overdue occurrence reads "MISSED · Next due: Not scheduled" to the customer (`serviceReadModel.ts:320-322` filters `dueAt >= today`) even though a ticket is open for it | medium | noted |

| 6 | Settings → AI → Script authoring | First partner save writes `unattended_allowed_classes = '{}'` because every class checkbox renders unchecked on a not-yet-existing row, discarding the column's DB default and leaving the org card with all 19 classes "Your partner does not allow this." | medium | noted |
| 7 | Settings → AI → Script authoring | The step-up error ("Enabling unattended runs needs a fresh security check.") renders at the top of a very long form while both Save buttons are far below the fold — Save appears to do nothing | low | noted |
| 8 | /approvals, AI-authored script row | Headline reads "Run script **unknown**... on 1 device(s)" for a proposal-backed run (no library script); the proposal's goal is right underneath but the first line an approver reads is the least informative | medium | noted |
| 9 | /ai-risk → Script Proposals | "Proposals per day" uses `<Line dot={false}>`, so a single-day series draws nothing at all — reads as "no data" when the count is 1 | low | noted |
| 10 | Script proposal reviewer | `script_proposal_reviews.cost_cents` is NULL on the model review row despite a budget reservation and populated token counts, so cost reports under-count model reviews | low | noted |
| 11 | Device details (any device) | Every device page load fires `GET /api/v1/reliability/<deviceId>?orgId=…` → **404**, logging a red console error on an otherwise healthy page | low | noted |
| 12 | Integrations → Monitoring | After a successful save, the form keeps the **plaintext** secret in component state; masking only appears after a true hard reload. An in-app navigation back to `/integrations` does not remount the island, so the typed credential stays readable in the DOM for the rest of the session | low | noted |
| 13 | Integrations → Accounting → Payments | The Stripe card lives under Accounting → Payments while "Connected Apps" (admin) is the MCP-client list — two plausible homes for "where do I manage Stripe", and the release inventory itself guessed wrong | low | noted |
| 14 | /monitoring, /monitoring/delivery | `MonitoringTabStrip` renders raw i18n keys (`monitoringTabs.ariaLabel`/`network`/`delivery`) on the server pass, then re-renders with real strings on hydrate — a React hydration-mismatch console error on every load of both new Monitoring hub pages (`apps/web/src/components/monitoring/MonitoringTabStrip.tsx:34,41`) | medium | noted |

## Fixes applied

| Commit | PR/area | What | Test |
|---|---|---|---|

## Issues filed

| Issue | Title | From row |
|---|---|---|

## Summary

| Group | PASS | PARTIAL | FAIL | BLOCKED | N/A |
|---|---|---|---|---|---|

**Top findings:**

**Oldest PR reached:** #…

**Before the release cut:** flags to enable, BLOCKED rows needing a live agent, open issues.
