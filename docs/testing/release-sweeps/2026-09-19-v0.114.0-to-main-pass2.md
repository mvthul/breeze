# Pre-release sweep — v0.114.0 → main (2026-09-19), pass 2 of 2 (delta)

| | |
|---|---|
| Range | pass-1 baseline `c14bb5e8d7` → `origin/main` `b4f589a6b3` (#6360). **PASS-2 BASE SHA = b4f589a6b3.** Anything merged after it is a follow-up entry, not absorbed. Pass 1: [`2026-09-19-v0.114.0-to-main.md`](2026-09-19-v0.114.0-to-main.md). |
| Gate | Not gated — merge queue kept moving during the pass at the owner's request. In queue at start: #6324. Held: #6028 (migration rename in flight), #5847/#5844/#5841 (manual gates), #5935. Known-noise check: Portal Dev E2E (hydration, non-blocking). |
| Stack | `pnpm wt-stack up --rebuild` on `qa/sweep-post-v0.114.0-pass2` @ `b4f589a6b3`, fresh DB (pass-1 volumes removed). baseUrl from `.breeze-stack.json`. |
| Flags enabled | stack defaults; `WEBAUTHN_RP_ID=localhost`, `MFA_FORCE_FOR_PARTNER_ADMIN=false`. No `BREEZE_*`/`*_ENABLED` flag gates any pass-2 surface (inventory §1) — all are role/permission gated. Seed admin promoted to platform admin. |
| Driver | skill `pre-release-sweep`; inventory by sonnet Explore agent from source. |

## Change inventory

Status ∈ `TODO | PASS | PARTIAL | FAIL | BLOCKED | N/A`. Click-paths are the inventory agent's reading of source — verify in the browser.

| PR | Surface | Click-path | Proof (visible outcome) | Prereqs / BLOCKED-locally reason | Group | Status |
|---|---|---|---|---|---|---|
| #6360 Disk Cleanup v2 W02 | web | `/devices/[id]` → **Filesystem** tab → `volume-picker` / `volume-chip`; "Open Files" → `/remote/files/[deviceId]` | Selecting a non-root volume re-keys scan state + snapshot summary (`volume-capacity`, `volume-usage-bar`); previously only `C:\` was root | Live agent with a reported filesystem scan, ideally a second fixed volume | 1 Disk cleanup | PASS |
| #6360 | api-only | `GET /devices/:id/filesystem/volumes`, `GET /devices/:id/filesystem?path=<volume>` | 200 lists each fixed volume with per-volume scan state + latest snapshot summary | Live agent scan; migration `2026-10-21-110000` applied | 1 | PASS |
| #6346 Disk Cleanup v2 W01 | web | `/remote/files/[deviceId]` → `disk-intelligence-toggle` → `disk-preview-button` → `disk-execute-button` | Execute actually frees space, returns `bytesFreed`/`skippedLocked`; partial failure renders amber (`disk-cleanup-result`) with `disk-cleanup-breakdown` / `disk-cleanup-child-issues`; WS `filesystem_analysis` results now persist a snapshot | Live agent ≥ 0.115.0 (`MIN_AGENT_VERSION_CLEANUP_GUARD`); irreversible deletes — disposable box only | 1 | PARTIAL |
| #6346 | api-only | `POST /devices/:id/filesystem/cleanup-execute` | success body `{rejectedPaths, bytesFreed,…}`; agent < 0.115.0 → `409 {error:"agent_update_required", minAgentVersion:"0.115.0"}` | A device row at version < 0.115.0 (409 path reachable without a live agent) | 1 | PASS |
| #6347 Billing W01 | web | `/settings/ticketing` → **Categories** tab → `work-types-card` (`work-type-new-name`, `work-type-create`); category editor "Default work type" | New row `work-type-row-<id>`; category default persists and becomes the default on new time entries (inert on money) | Partner Admin, partner-scope token (org token 403 by design) | 2 Billing + contracts | PASS |
| #6347 | web | Ticket quick-add time entry, Timer widget, Timesheet row editor — `WorkTypeSelect` | Work type saved on the entry and shown in the Timesheet Work Type column; omitted → falls back to category default | ≥ 1 work type | 2 | PASS |
| #6347 | AI/MCP | `log_time_entry` / `start_timer` with `workType` (id or name); `list_work_types`; `stop_timer` rejects `workType` | Entry carries resolved id; unmatched name fails loudly | AI chat with credits / model key | 5 | FAIL |
| #6347 | ops CLI | `pnpm --filter @breeze/api dry-run:labour-pricing` | Report printed, writes nothing (read-only txn) | DB access; owner gate = run on EU+US (tracked on #4628) | 2 | PASS |
| #6089 quote/invoice/portal-invite templates | web | `/settings/partner#email-templates` → `email-templates-list` | 6 rows (`email-template-row-<id>`): 3 ticket + `quote_send`, `invoice_send`, `portal_invite`; editor shows sample vars; variable chips render human text, not raw `emailTemplates.vars.*` keys (new keys: `quote_number`, `total`, `expiry_date`, `accept_url`, `invoice_number`, `due_date`, `invite_url`, `cta_button`) | Partner-scope settings access | 3 Email templates | PASS |
| #6089 | api-only | Send a quote / send an invoice / issue a portal invite | Captured mail carries the partner's custom HTML with a server-built accept/pay/invite URL (partner cannot inject an href) | SMTP capture on the stack | 3 | PARTIAL |
| #6088 ticket customer emails | web | same tab, rows `ticket_comment_notification`, `ticket_autoresponse`, `ticket_resolved` | Full layout (heading + portal button) instead of the old one-liner | — | 3 | PASS |
| #6088 | api-only | Public tech reply / ticket create / resolve → `ticketNotifyWorker` | Captured mail is full HTML; internal `ticket_comments` content never included | SMTP capture; ticket with a customer contact | 3 | PARTIAL |
| #6321 Bare-metal W05b | web | `/dr` → plan → group card → `dr-group-step-type` → "Bare metal rebuild" → `dr-group-rebuild-options` (`-output-dir`, `-wait-timeout`) | Type-specific config round-trips (old bug: web sent `restoreConfig: {}` for every step type, failed at dispatch). Execution view: `dr-device-recovery(-status)`, `dr-device-reissue-code` (MFA), `dr-device-cancel-recovery` | Editor round-trip is local; execution needs a Linux rebuild host + restorable snapshot (lab run tracked separately, W05 Task 10) | 4 DR editor | PASS |
| #6321 | api-only | DR execution trigger on a `BARE_METAL_REBUILD` group | `queuedRecoveries[]`; one `bare_metal_recoveries` row per device; timeout → `failed`/`timeout` | Linux host device — BLOCKED locally | 4 | BLOCKED |
| #6087 partner SP contract writes | web | `/settings/partner-service-principals` → create/edit → scopes | `scope-checkbox-contracts:write` present, unchecked by default | Partner admin | 2 | PASS |
| #6087 | api-only | `POST /api/v1/partner-api/contracts` with `X-API-Key: brz_sp_…`; `GET/PATCH …/:id`; `POST/PATCH/DELETE …/lines` | 201/200 persisted; foreign org → 403; no `contracts:write` → 403; key against human `/api/v1/contracts` → 401 | SP key minted with the scope | 2 | PASS |
| #6317 stable error codes | api-only + web | `POST /api/v1/auth/login` wrong password; users 404/409 paths; login form in the browser | `401 {error:"Invalid email or password", code:"INVALID_CREDENTIALS"}` (prose unchanged, `code` new); `NOT_FOUND`, `CONFLICT`, `RATE_LIMITED`; web login still shows readable text | — | 5 Auth + AI | PASS |
| #6341 / #6345 AI registry + harness | AI | one chat turn that routes to a tool | Tool resolves; prompt tool index is registry-generated | AI provider key on the stack | 5 | PASS |
| #6345 | api-only | `GET /api/v1/admin/ai/tool-usage?days=90` | 200 hot/cold report; `days` outside [1,365] → 400 | Platform admin | 5 | PASS |

### Collapsed — N/A for the browser
#6340 (pass 1 itself) · #6336 #6325 #6269 (docs) · #6271 #6272 (e2e-tests deps) · #6273 (agent go.mod aws-sdk group — CI-covered) · #5969 (Android signing + Play runbook, mobile-only).

## Groups (sequential — one browser owner at a time)
1. Disk cleanup — #6360, #6346 (highest risk: agent-shipped, Codex-built)
2. Billing + contracts — #6347, #6087
3. Email templates — #6088, #6089
4. DR editor — #6321
5. Auth errors + AI — #6317, #6341, #6345, #6347 AI tools

## Sweep log (append as you go)

### Group 1 — Disk cleanup (#6360, #6346) — browser agent, 2026-09-20 02:05Z

Rig: live agent `qa-agent-pass2` / device `f3bcb55e-f371-475c-8183-6f82143fb1b5`, container `breeze-qa-agent-pass2`, 200 MB planted junk on `/` + 60 MB no-match on `/mnt/data`.

#### #6360 web (Filesystem / "Disk Cleanup" tab) — PASS
- Tab is labelled **Disk Cleanup** (under **More**), route `#filesystem`. `volume-picker` renders 5 `volume-chip`s: `/` (with `volume-os-badge`), `/etc/hostname`, `/etc/hosts`, `/etc/resolv.conf`, `/mnt/data` — the last three are the known bind-mount paper cut from the enroll notes, not re-logged.
- **Per-volume re-key verified.** `/` → Scan Path `/`, Scanned 394.3 MB, 61 candidates, largest dirs `/usr` `/var` `/root/.cache`. `/mnt/data` → Scan Path `/mnt/data`, Scanned 60.0 MB, **0 candidates**, Trash 0 B, own largest-files list. Network shows the panel refetching `GET /filesystem?path=%2F` vs `?path=%2Fmnt%2Fdata` per chip.
- **Rescan from the UI on `/mnt/data`:** `Analyze Now` → `POST /filesystem/scan` 202, command polled, Last Scan **07:47 PM → 07:56 PM** and `Scan Path` stayed `/mnt/data`. Switching back to `/` still showed the untouched 07:47 PM timestamp — state is genuinely per-volume, not shared.
- **"Open File Manager"** navigated to `/remote/files/f3bcb55e-…` (exact path asserted).
- Root chip shows `Capacity unknown` / no `volume-usage-bar` (overlayfs, expected on a container); `/mnt/data` shows `volume-capacity` `103 GB of 1,035 GB used` + bar.

#### #6346 web (execute) — PARTIAL (one real agent-guard defect; cleanup itself works)
du **before**: `/tmp` 60M · `/var/tmp` 30M · `/root/.cache/pip/wheels` 30M · `/var/cache/apt/archives` 31M · `/root/.cache/mozilla` 30M · `/root/.local/share/Trash` 20M.

- `disk-intelligence-toggle` → `disk-preview-button` → `POST /filesystem/cleanup-preview` 200: **61 candidates · 200.0 MB potential**, four categories (temp_files, package_cache, browser_cache, trash) — matches the plant exactly.
- **Only 40 of the 61 candidate rows are rendered** (20 pre-checked, no select-all). Max selectable = `Selected: 40 · 137.0 MB`. The other 21 (firefox cache2 ×10, pip wheels ×10, `junk-20.tmp`) have no UI affordance at all. See paper cut 1.
- `disk-execute-button` → confirm modal (`Cancel` / `Delete Files`) → `POST /filesystem/cleanup-execute` **200** in 22 s:
  `{"bytesReclaimed":122683392,"selectedCount":40,"failedCount":0,"counts":{"completed":39,"partial":0,"failed":0,"skipped_locked":0,"rejected":1,"skipped_budget":0},"rejectedPaths":["/root/.local/share/Trash"],"partial":false}`
  **bytesFreed = 122,683,392 B = 117.0 MB** (39 × 3,145,728).
- `disk-cleanup-result` rendered: *"Cleanup executed: reclaimed 117.0 MB from 40 target(s), 0 failed."* + `disk-cleanup-breakdown` *"0 failed · 0 partial · 1 rejected · 0 locked · 0 not reached"*.
- du **after** (proof the deletes landed): `/tmp` 60M→**3.0M** (only `junk-20.tmp`, never selectable), `/var/tmp` 30M→**0**, `/var/cache/apt/archives` 31M→**1.0M** (only the control), `/root/.cache/pip/wheels` and `/root/.cache/mozilla` untouched at 30M (unrendered), `/root/.local/share/Trash` **20M unchanged** (rejected).
- **Both controls survive**: `/var/cache/apt/archives/partial/skip.deb` and `/opt/keep/important.dat` still present, unmodified.
- **Snapshot persistence after a WS `filesystem_analysis` result — PASS.** Re-scanning `/` from the Disk Cleanup tab moved it 07:47 PM → 08:00 PM, Scanned Data **394.3 MB → 277.4 MB** (−117 MB, exactly the reclaimed amount) and Cleanup Candidates **61 → 22**. `device_filesystem_snapshots` holds 4 rows (`/`, `/mnt/data` ×2, `/home`), i.e. every WS-delivered result persisted.
- **Re-run Preview on the fresh snapshot** → 200, `estimatedBytes 87031808` (83.0 MB) / 22 candidates. Not ~0, and correctly so: 200 − 117 = 83 MB is precisely the 21 unrenderable candidates + the rejected trash dir. The 40-row cap, not the executor, is what left it above zero.

**DEFECT (#6346, agent) — the `trash` category can never be cleaned for root on Linux.**
`{"path":"/root/.local/share/Trash","status":"rejected","reason":"agent_guard","error":"cleanup guard rejected: /root/.local/share/Trash is not inside its anchor /root/.local/share/Trash"}`
`agent/internal/remote/tools/fileops.go:687` rejects when `rel == "."`, i.e. when the target **equals** its confinement anchor. The anchor is the rule pattern's literal prefix (`cleanupRuleAnchorFor` → `concreteAnchor`), so any **fully literal** pattern anchors on itself and is structurally unexecutable. `agent/internal/remote/tools/cleanup_rules.json:123` has exactly one such pattern today: `/root/.local/share/Trash` (granularity `contents`). Its sibling `/home/*/.local/share/Trash` has a wildcard, so its anchor is `/home` and it passes — the bug is root-only, but root's trash is the common case on a server. The preview happily offers the 20 MB and the execute always returns it rejected. Not covered by CI: the Go tests exercise the wildcard form.

#### #6346 api (409 `agent_update_required`) — PASS
- Preview first to pin a run: `POST /filesystem/cleanup-preview {"path":"/"}` → 200, `cleanupRunId c504053e-be79-4fa1-9ef8-4313bd970565`.
- `update devices set agent_version='0.114.0'` (column `agent_version`), then `POST /filesystem/cleanup-execute {"cleanupRunId":"c504053e-…","paths":["/tmp/junk-20.tmp"]}` →
  **409** `{"success":false,"error":"agent_update_required","data":{"minAgentVersion":"0.115.0","agentVersion":"0.114.0"}}`. Guard fires before anything is dispatched, as `filesystem.ts:486` intends.
- **Restored:** left to the agent; the next heartbeat wrote `0.115.0` back within ~40 s — verified in the DB, no manual UPDATE needed.

#### Partial-failure amber rendering — PASS (rejection variant); child-issues variant N/A
- Reproduced amber with a trash-only selection: `disk-cleanup-result` className contains **amber**, not emerald; `disk-cleanup-breakdown` = "0 failed · 0 partial · 1 rejected · 0 locked · 0 not reached"; `disk-error-detail` = "/root/.local/share/Trash — agent_guard". Nothing was deleted (Trash still 20M).
- The true `failedChildren` / `skippedLinkCount` path (`disk-cleanup-child-issues`) is **N/A on this rig**: `chattr +i` is denied in the container (no `CAP_LINUX_IMMUTABLE`), the agent runs as root so DAC permissions cannot block an unlink, and the only directory-granularity Linux rule (trash) is rejected by the anchor bug above before a single child is touched. Covered by unit tests in `FileManager.cleanup.test.tsx`.

**Container/DB changes:** 117 MB of planted junk permanently deleted on the disposable agent container (intended); `devices.agent_version` briefly set to `0.114.0` and restored by heartbeat. No source edits.

### Group 2 — Billing + contracts — browser agent, 2026-09-20 01:50Z

Stack `breeze-wt-qa-sweep-post-v0-114-0-pass2` @ `b4f589a6b3`, baseUrl `http://localhost:32925`, admin@breeze.local (platform admin, partner scope).
Login note: the account/IP login limiter was tripped by another agent's probe; I cleared the `login:ip:*` / `login:socket:*` / `login:account-fail:*` Redis keys on the test stack to proceed. Test-fixture cleanup, not a finding.

#### #6347 (web — Work types card + category default) — PASS

- `/settings/ticketing#categories` renders `work-types-card` with a real empty state ("No work types yet. Add your first work type above.").
- Created **Remote Support** (`cd914052-036a-4473-9a31-be460791b77f`) and **Onsite Visit** (`7c3bcbf2-2dc9-411b-a3d5-6091901aecc6`) via `work-type-new-name` + `work-type-create`. Both produced `work-type-row-<id>` and a `[data-testid="toast"]` reading **"Work type saved."** (captured with a MutationObserver installed before the click).
- Created ticket category **Sweep Cat** (`fff33666-32e9-46d6-8e6b-69d5ad49ad42`); toast "Category "Sweep Cat" created".
- Category editor's **Default work type** (`ticket-category-default-work-type`) is a `<select>` listing `No work type` + both new work types. Set it to Remote Support → toast **"Category updated"**.
- **Persistence confirmed after a full page reload**: both `work-type-row-*` present, and the editor re-opens with `value = cd914052-…` / label `Remote Support`.
- **Settings rule ("one concept, one home") — HOLDS.** `grep` over `apps/web/src` for work-type testids and `WorkTypeSelect`: the only create/rename/archive surface is `settings/WorkTypesCard.tsx` (`work-type-create` / `work-type-save` / `work-type-rename-*` / `work-type-archive-*`). Every other mount — `TicketCategoriesPage.tsx:515`, `TicketTimeBilling.tsx:242,288`, `TimesheetPage.tsx:502` — only *selects* an existing work type. No second editing home.


#### #6347 (web — `WorkTypeSelect` on the time-entry surfaces) — PASS

Setup: ticket **T-2026-0001** on Default Organization, category **Sweep Cat** (default work type = Remote Support).

All three mounts render the two work types created above plus a `No work type` option:

| Surface | testid | Options |
|---|---|---|
| Ticket quick-add time entry | `ticket-billing-quick-add-work-type` | No work type / Onsite Visit / Remote Support |
| Timer widget start | `timer-work-type` | same |
| Timesheet row editor | `timesheet-edit-work-type` | same |

Entries logged and the fallback proven:

- **Explicit**: quick-add 30m, work type = *Onsite Visit* → toast "Time logged".
- **Omitted**: quick-add 15m, work-type select left at `""` → toast "Time logged".
- **Timer**: `timer-work-type` = *Onsite Visit*, Start timer ("Timer started") → `timer-widget-stop` → `timer-stop-submit` ("Time entry saved").

`/timesheet` Work Type column (`timesheet-header-work-type` = "Work Type"):

- `Explicit work type entry` → **Onsite Visit**
- `No work type entry` → **Remote Support** ← the category default was applied server-side, as designed

Confirmed at the DB as well (`time_entries` ⋈ `work_types`): Explicit→Onsite Visit, No-work-type→Remote Support, Timer→Onsite Visit. The timesheet row editor re-opens with `Remote Support` pre-selected on the fallback entry, so the resolved value is persisted on the row and not a render-time fallback.

#### Everyday-workflow check — `/settings/ticketing` — PASS

All seven tabs (`statuses`, `priorities`, `categories`, `templates`, `forms`, `email`, `timeTracking`) render non-empty panels with human copy; no raw `i18n` keys. **Zero app console errors** across the page load and the full tab walk (the one 401 in the log is my own unauthenticated `fetch` probe, not the app).

#### #6347 (ops CLI — `dry-run:labour-pricing`) — PASS

- Run from the repo against the stack DB (`DATABASE_URL=postgresql://breeze:breeze@172.31.2.2:5432/breeze`, the postgres container's IP — the stack does not publish 5432).
- Printed the full report: the "WILL START BILLING (money-moving difference)" section, then per-partner blocks (`Default Partner [fd75aa76-…] (USD)`, default cards, work types to create, name collisions, category rates skipped, dropped non-billable rates, org override cards). All zero on this fresh seed — correct, since the DB had 0 `ticket_categories` / 0 `org_ticket_settings` at the time.
- **Writes nothing — verified two ways.** (a) Source: `apps/api/scripts/labour-pricing-dry-run.ts` opens with `await db.execute(sql\`SET TRANSACTION READ ONLY\`)` inside `withSystemDbAccessContext`, and grep for write verbs over the file returns only the comment and that statement. (b) Counts `ticket_categories` / `org_ticket_settings` / `work_types` identical before and after the run (0 / 0 / 0).
- Caveat worth recording: the report was exercised against an *empty* dataset, so the collision/skip/override rendering paths were not visually exercised here. The owner gate on #4628 is still the real EU+US run.

#### #6087 (web — partner service principal `contracts:write` scope) — PASS

- `/settings/partner-service-principals` loads with a real empty state. Create drawer shows all 12 scopes as `scope-checkbox-<scope>`.
- **`scope-checkbox-contracts:write` is present and UNCHECKED by default.** The eight `*:read` scopes are pre-checked; all four write scopes (`organizations:write`, `sites:write`, `enrollment-keys:write`, `contracts:write`) default to unchecked — matches `DEFAULT_WEAVESTREAM_PARTNER_SERVICE_PRINCIPAL_SCOPES` (read-only).
- Created two principals (toast "Service principal created" on both):
  - **Sweep SP with contracts** — read scopes + `contracts:write`
  - **Sweep SP no contracts** — read scopes only (`contracts:write` confirmed unchecked at save time)
- Issued a key on each; the one-time reveal (`close-key-reveal`, "Copy this key now / This plaintext key is shown once and cannot be retrieved later") shows the full 50-char `brz_sp_…` secret in a `<pre>`.

#### #6087 (api — Partner API contract writes) — PASS

All calls via `curl` against `http://localhost:32925/api/v1`, `X-API-Key: brz_sp_…`. Paths read from `apps/api/src/routes/partnerApi/contracts.ts`, bodies from `createContractSchema` / `contractLineInputSchema` in `packages/shared`.

| # | Call | Status | Body (key part) |
|---|---|---|---|
| 1 | `POST /partner-api/contracts` (with-scope), org `0fa3735d…` | **201** | `{"data":{"id":"34f72145-bad6-424b-b6d5-e1f0751ac9c5","status":"draft","billingTiming":"advance","intervalMonths":12,"startDate":"2026-10-01","currencyCode":"USD","createdBy":null,…}}` |
| 2 | `GET /partner-api/contracts/:id` | **200** | `{"data":{"contract":{…},"lines":[],"periods":[]}}` |
| 3 | `PATCH /partner-api/contracts/:id` `{name, notes}` | **200** | name → `Sweep contract (renamed)`, `notes":"patched by sweep"`, `updatedAt` advanced |
| 4 | `POST /partner-api/contracts/:id/lines` (manual, unitPrice 100.00, manualQuantity 2) | **201** | line `d4862aeb…`, `"manualQuantity":"2.00"` |
| 5 | `PATCH …/lines/:lineId` `{"manualQuantity":"7.5"}` | **200** | `"manualQuantity":"7.50"` |
| 6 | `GET /partner-api/contracts/:id` again | **200** | `lines: [('Sweep manual line', '7.50')]` — the PATCH is reflected on read |
| 7 | `DELETE …/lines/:lineId` | **200** | `{"data":{"ok":true}}` |
| 8 | `GET` after delete | **200** | `lines: []` |
| 9 | `POST /partner-api/contracts` with the **no-scope** key | **403** | `{"error":"Partner API scope required","code":"partner_api_scope_required"}` |
| 10 | `GET /partner-api/contracts/:id` with the **no-scope** key | **403** | same — the GET is behind `contracts:write` too, as the route intends |
| 11 | `POST /api/v1/contracts` (human route) with the **with-scope** key | **401** | `{"error":"Missing or invalid authorization header"}` — the SP key buys nothing on the JWT surface |
| 12 | `GET /api/v1/contracts` (human route) with the with-scope key | **401** | same |
| 13 | `POST /partner-api/contracts` with a **foreign** `orgId` (org of a second partner) | **403** | `{"error":"Organization access denied","code":"ORG_DENIED"}` |

Fixture for #13: a real second partner (`Sweep Foreign Partner`) + org `Sweep Foreign Org` `0230d265-854b-49b9-9b02-3730e6d3241c` inserted under `breeze.scope='system'`, so this is a genuine cross-tenant target, not just an unknown UUID.

DB confirmation: exactly one `contracts` row (`Sweep contract (renamed)`, org `0fa3735d…`, partner `fd75aa76…`, 0 lines after the delete); **zero** rows named `Denied contract` or `Foreign contract` — the 403s wrote nothing.

### API-only rows — curl (main session), 2026-09-20 ~01:55 UTC

#### #6317 stable error codes — PASS (login paths)
- `POST /auth/login` wrong password → `401 {"error":"Invalid email or password","code":"INVALID_CREDENTIALS"}` — prose unchanged, `code` present.
- Login throttle → `429 {"error":"Too many login attempts. Please try again later.","code":"RATE_LIMITED","retryAfter":216}`.
- First login on a fresh client → `428 {"reason":"auth_binding_rotation_required"}` (known noise, retry succeeds).
- `GET /users/<unknown uuid>` → `404 {"error":"User not found","code":"NOT_FOUND"}`.
- Note: the throttle probe locked `login:ip:` / `login:socket:` keys (keyed on the caddy IP locally, so every client) — cleared in Redis on this throwaway stack; not a finding.

#### #6345 `GET /admin/ai/tool-usage` — PASS
- `?days=90` → 200 `{days:90, generatedAt, rows:[], coldTools:[…every registered tool…]}` (no `ai_tool_executions` on a fresh DB → all cold, as expected).
- `?days=0` → 400 `days: Too small: expected number to be >=1`; `?days=400` → 400 `Too big: expected number to be <=365`.

### Live agent for group 1 — enroll agent (curl), 2026-09-20 01:47 UTC
- Disposable container `breeze-qa-agent-pass2` (debian 12 arm64) on the stack network, agent built from main with `-X main.version=0.115.0`, enrolled to Default Organization / Default Site → device `f3bcb55e-f371-475c-8183-6f82143fb1b5` (`qa-agent-pass2`, online, WebSocket connected). Enroll needed `--enrollment-secret` (stack sets `AGENT_ENROLLMENT_SECRET`).
- Planted 200 MB of rule-matching junk (temp_files 90 MB aged 3 d, package_cache 60 MB, browser_cache 30 MB, trash 20 MB) + two controls (`/var/cache/apt/archives/partial/skip.deb`, `/opt/keep/important.dat`) + 60 MB on a second btrfs volume `/mnt/data` with no rule matches.
- #6360 api — **PASS**: `POST /filesystem/scan` on `/` and `/mnt/data` → 202, both completed in ~10 s; `GET /filesystem/volumes` lists `/` (isOsRoot, snapshot estimate 209,715,200) and `/mnt/data` (snapshot estimate 0), each with its own scanState.
- #6346 preview api — `POST /filesystem/cleanup-preview {path:"/"}` → 200, `estimatedBytes 209715200` = exactly the planted set; both controls excluded. Execute left for the browser row.
- **Paper cut (#6360):** Docker single-file bind mounts `/etc/hostname`, `/etc/hosts`, `/etc/resolv.conf` appear as three extra selectable volumes (same btrfs device, capacity duplicated 3×). `listFilesystemVolumes` / the agent disk collector filter by fsType only — real Linux hosts with file bind mounts (containers, snaps) will show the same. Root row has `fsType/totalGb null` on a container (overlay filtered) — expected there.

### Group 3 — Email templates (#6088, #6089) — browser agent, 2026-09-20 02:13Z

#### #6089 web (`/settings/partner#email-templates`) — PASS

- `email-templates-list` renders exactly **6** rows, all six expected ids, each with a status pill:
  `ticket_comment_notification` "Public reply notice" · `ticket_autoresponse` "Ticket received acknowledgement" · `ticket_resolved` "Ticket resolved" · `quote_send` "Quote / proposal" · `invoice_send` "Invoice" · `portal_invite` "Portal invite" — all "Using default" on a fresh partner.
- **Variable chips render human text, not raw `emailTemplates.vars.*` keys**, on all three new ids (testids `email-template-var-<key>`):
  - `quote_send` → Quote number · Company name · Total · Expiry date · Accept URL · Button
  - `invoice_send` → Invoice number · Company name · Total · Due date · Portal URL · Button
  - `portal_invite` → Requester name · Company name · Invite URL · Organization name · Button
- **Sample vars render in `email-template-preview`**: `Q-2026-0001`, `$1,200.00`, `2026-07-01` (quote); `INV-0001`, `2026-09-01` (invoice); `Sample Requester` / `Acme Corp` (invite). No `{{…}}` tokens leak into the preview.
- **Customize → Save → reload → Custom — PASS.** `quote_send`: heading → `Sweep heading Q3`, TipTap body retyped to two paragraphs (real keyboard input, not innerHTML). `email-template-save` → `PATCH /api/v1/orgs/partners/me` **200** + toast **"Email template saved"** (MutationObserver installed before the click). After a full page reload the row reads **"Custom"** and the editor re-opens with heading `Sweep heading Q3` and body `<p>Sweep body paragraph one.</p><p>Second sweep line with {{quote_number}}.</p>`. Confirmed server-side: `GET /orgs/partners/me` → `settings.emailTemplates.quote_send = {heading:"Sweep heading Q3", html:"<p>Sweep body…", subject:null, buttonLabel:null}`.
- **Reset to default — PASS.** `email-template-reset` → fields snap back to the catalog default, server stores `{html:null, heading:null, subject:null, buttonLabel:null}`, and after reload the row reads **"Using default"** again. (Partner left clean.)

#### #6089 injection / sanitization — PASS

`PATCH /orgs/partners/me` with a hostile `quote_send` payload (real admin bearer token, not a mock). Sent: `<script>`, `<a href="javascript:…">`, `<a href="https://evil.example.com/steal">`, `<a href="//evil.example.com/pr">`, `<img src=x onerror=…>`, `<p onclick=…>`, `<iframe>`, `<a href="data:text/html,<script>…">`, plus `<script>` inside the **heading** field.

- Response **200** with an explicit warning: `[{"code":"UNSUPPORTED_HTML_TAGS_REMOVED","field":"emailTemplates.quote_send.html","removedTags":["iframe","img","script"]}]`.
- Stored html came back: `<p>Hello </p><p><a>js link</a></p><p><a href="https://evil.example.com/steal" rel="noopener noreferrer" target="_blank">arbitrary link</a></p><p><a>proto-rel</a></p><p></p><p>handler</p><p><a>data link</a></p>` — **`javascript:`, `data:` and protocol-relative hrefs stripped to a bare `<a>`; `script`/`img`/`iframe`/`onclick` gone.** `apps/api/src/routes/orgs.ts:146` → `sanitizeRichTextHtmlWithReport`; scheme allowlist at `apps/api/src/services/richTextSanitize.ts:30`.
- **`heading` is stored raw** (`Sweep <script>alert(9)</script>heading` — only `blankToNull`, no sanitizer) but is **escaped at render**: `renderLayout` emits `${escapeHtml(heading)}` (`apps/api/src/services/emailLayout.ts:38`).
- **Proved on the real renderer, no mail sent.** Fed the stored hostile template through `renderPartnerEmail({id:'quote_send', ctaUrl:'https://app.example.com/quote/REALTOKEN'})` in a throwaway vitest file (deleted afterwards) and dumped the produced HTML: `<script>` tag present = **false**, `onclick` present = **false**, `<h1>` = `Sweep &lt;script&gt;alert(9)&lt;/script&gt;heading`, and the only two hrefs in the document are the partner's own body link and **the server-built `https://app.example.com/quote/REALTOKEN`** — the CTA URL never comes from partner input (`applyCta` gates on `isSafeHttpUrl(ctaUrl)`, and `stripSentinelFromAttributes` removes the `{{cta_button}}` sentinel from any quoted attribute so it cannot be spliced into an `href`).
- Note (by design, not a finding): a partner **can** put an arbitrary `https://` link in their own body text. That is the same trust level as an email signature — partner-scoped, not cross-tenant — and it is the CTA/accept URL that the PR claims is server-built. Recorded so nobody re-reads it as a hole later.

#### #6089 / #6088 api (send paths) — PARTIAL (no send; proven by renderer + source + unit tests)

Per the sweep's email-safety rule the stack has a live Resend key, so no quote/invoice/invite/ticket mail was actually sent. Proven instead that all four customer-mail builders route through the same renderer with a **server-supplied** `ctaUrl`:

| Path | File | `ctaUrl` source |
|---|---|---|
| quote_send | `apps/api/src/services/quoteEmail.ts:47` | `params.acceptUrl` |
| invoice_send | `apps/api/src/services/email.ts:1239` | `params.portalUrl` |
| portal_invite | `apps/api/src/services/email.ts:1084` | `params.inviteUrl` |
| ticket_* | `apps/api/src/jobs/ticketNotifyWorker.ts:188,403` | portal ticket URL |

`npx vitest run src/services/emailTemplates/renderPartnerEmail.test.ts src/services/emailTemplates/defaults.test.ts src/services/richTextSanitize.test.ts` → **3 files / 57 tests passed**. The "internal `ticket_comments` content never included" claim is asserted in `ticketNotifyWorker`'s own suite and by the tab's own description copy ("The ticket reply notice never includes the reply text"); not re-proven live here.

#### #6088 ticket templates — real layout, not a one-liner — PASS

All three ticket ids now carry a heading, a body and (where appropriate) a CTA:

| id | heading | button label | body |
|---|---|---|---|
| `ticket_comment_notification` | "New reply on your ticket" | "View ticket" | 3 paragraphs + `{{email_only_hint}}` + `{{cta_button}}` |
| `ticket_autoresponse` | "We received your request" | *(none — acknowledgement has no CTA, by catalog design)* | 2 paragraphs incl. `{{ticket_number}}` |
| `ticket_resolved` | "Your ticket has been resolved" | "View ticket" | `{{resolution_note}}` + `{{email_only_hint}}` + `{{cta_button}}` |

Previews render the portal button slot and the sample resolution note; nothing is a bare single line.

#### i18n (switched the console to German) — PASS for the new keys, one gap

`/settings/profile` → `locale-option-de-DE`. Whole editor translated, **zero raw `emailTemplates.*` keys anywhere on the page**:
chips → *Angebotsnummer · Firmenname · Gesamtbetrag · Ablaufdatum · Annahme-URL · Schaltfläche*; labels → *Betreff / Überschrift / Schaltflächenbeschriftung / HTML-Text*; buttons → *Vorlage speichern / Auf Standard zurücksetzen / Zurück zur Liste*; RTE toolbar → *Fett / Kursiv / Unterstrichen / Überschrift / Zwischenüberschrift / Aufzählungsliste / Nummerierte Liste / Tabelle*; status pill → *Standard*.
Checked all 8 shipped locales statically: every one has a complete `emailTemplates.variables` block with all 17 keys and no English fallbacks (the three locales where `total` equals "Total" are fr/es/pt cognates, not misses).
**Gap:** the six template *names* in the list stay English in every locale — see paper cut 2. Switched back to English afterwards.

#### Settings rule — "one concept, one home" — HOLDS

`EmailTemplateEditor` is imported once (`EmailTemplatesTab.tsx:12`) and `EmailTemplatesTab` is mounted once (`PartnerSettingsPage.tsx:755`). No second editor in `apps/web`, `apps/portal` or `apps/mobile`. The Ticketing → Email tab's inbound card only *links* to this tab (`inbound-edit-email-templates` → `/settings/partner#email-templates`), it does not edit content. One caveat recorded as paper cut 5.

### Group 4 — DR editor (#6321) — browser agent, 2026-09-20 02:18Z

Fixture: plan `Sweep DR plan` (`d1def0ef-a39b-4caa-ac7e-331ff4a0b93a`), one group `Rebuild group` (`66d6a89c-d317-4172-81e0-6aed20512f0c`) over the live linux device `qa-agent-pass2` (`f3bcb55e-…`).

#### #6321 web (rebuild step config round-trip) — PASS

- `/dr` → **Create Plan** → **Add group** → `dr-group-step-type` offers all six step types (`VM_RESTORE_FROM_BACKUP`, `VM_INSTANT_BOOT`, `HYPERV_RESTORE`, `MSSQL_RESTORE`, `BMR_RECOVER`, `BARE_METAL_REBUILD`) with human labels.
- Choosing **"Bare-metal rebuild (rebuild engine)"** reveals `dr-group-rebuild-options` with `dr-group-rebuild-output-dir` (default/placeholder `/var/lib/breeze/rebuild/out`), `dr-group-rebuild-wait-timeout` (default `240`) and a single-select **Rebuild host** picker. The picker correctly offers only the Linux device — `qa-agent-pass2 · linux · online` — with the hint "Linux device with qemu-utils that runs rehearsals."
- **The old `restoreConfig: {}` bug is gone.** Captured `POST /api/v1/dr/plans/:id/groups` **201**, request body:
  `{"name":"Rebuild group","sequence":0,"devices":["f3bcb55e-…"],"restoreConfig":{"commandType":"BARE_METAL_REBUILD","snapshotSelection":"latest_restorable","rebuildHostDeviceId":"f3bcb55e-…","outputDir":"/srv/sweep/rebuild-out","waitTimeoutMinutes":77}}`
  The 201 response echoes the same normalised config.
- **Round-trip after a full page reload — PASS.** `/dr` → **Edit** re-opens with `dr-group-step-type` = `BARE_METAL_REBUILD`, `dr-group-rebuild-options` rendered, output dir `/srv/sweep/rebuild-out`, wait timeout `77`, and the rebuild host checkbox checked on `qa-agent-pass2`. Confirmed at the DB: `dr_plan_groups.restore_config` = `{"outputDir":"/srv/sweep/rebuild-out","commandType":"BARE_METAL_REBUILD","snapshotSelection":"latest_restorable","waitTimeoutMinutes":77,"rebuildHostDeviceId":"f3bcb55e-…"}`.

#### #6321 web (other step types + stale-config leakage) — PASS

Cycled the group through **every** step type, saving and re-opening each time. Each reopen loaded the type that was saved previously (`loadedTypeBefore` chained `MSSQL_RESTORE → VM_RESTORE_FROM_BACKUP → VM_INSTANT_BOOT → HYPERV_RESTORE → BMR_RECOVER → BARE_METAL_REBUILD`), so the non-rebuild types round-trip too.

| Saved type | `dr-group-rebuild-options` shown | PATCH `restoreConfig` sent | Status |
|---|---|---|---|
| `MSSQL_RESTORE` | no | `{"commandType":"MSSQL_RESTORE"}` | 200 |
| `VM_RESTORE_FROM_BACKUP` | no | `{"commandType":"VM_RESTORE_FROM_BACKUP"}` | 200 |
| `VM_INSTANT_BOOT` | no | `{"commandType":"VM_INSTANT_BOOT"}` | 200 |
| `HYPERV_RESTORE` | no | `{"commandType":"HYPERV_RESTORE"}` | 200 |
| `BMR_RECOVER` | no | `{"commandType":"BMR_RECOVER"}` | 200 |
| `BARE_METAL_REBUILD` (back) | yes, reset to defaults | `{commandType, snapshotSelection, outputDir:"/var/lib/breeze/rebuild/out", waitTimeoutMinutes:240}` | 200 |

**No stale rebuild keys leak.** Switching away from `BARE_METAL_REBUILD` sends a two-key config with no `outputDir` / `waitTimeoutMinutes` / `rebuildHostDeviceId`; switching back re-renders the panel at the schema defaults rather than resurrecting the previous values (`restoreConfigFromGroup` / `stepFieldsFromRestoreConfig`, `DRPlanEditor.tsx:46-85`).

#### #6321 validation — PASS (no 500s)

| Input | Result |
|---|---|
| Wait timeout `999999` | **Client-side**, no request fired at all: inline **"Bare-metal rebuild wait timeout must be a whole number between 5 and 1440 minutes."**, modal stays open |
| Output dir `relative/not/absolute` | Server **400** `{"error":"restoreConfig.outputDir: absolute path required","details":{"fieldErrors":{"restoreConfig.outputDir":["absolute path required"]}}}` → rendered inline as **"restoreConfig.outputDir: absolute path required"** (readable, but see paper cut 2) |
| Output dir empty | **200**, silently normalised to the placeholder default `/var/lib/breeze/rebuild/out` (schema `.default()`), modal closes |

#### #6321 api (execution) — BLOCKED-locally, but the refusal is clean

Per the brief, no rebuild was executed against the device — no restorable snapshot exists on this stack, so `queuedRecoveries[]` / a `bare_metal_recoveries` row / the timeout→`failed` path cannot be produced here. The trigger *was* exercised once because the refusal is free and side-effect-free:

- **Execute → Rehearsal → Start execution** → `POST /api/v1/dr/plans/:id/execute {"executionType":"rehearsal"}` → **404** `{"error":"resource_not_found"}`. That is the documented authorization outcome when `resolveLatestRestorableSnapshotId` returns null for the group's device (`drBareMetalRebuildStep.ts:62-90`).
- **Nothing was written:** `dr_executions` = 0 rows, `bare_metal_recoveries` = 0 rows after the attempt.
- The UI surfacing of that refusal is bad — see paper cut 1.

The remaining execution-view testids (`dr-device-recovery`, `dr-device-recovery-status`, `dr-device-reissue-code`, `dr-device-cancel-recovery`) are unreachable without an execution and stay **BLOCKED-locally**; they belong to the W05 Task 10 lab run.

**Left on the stack:** one DR plan, now named `Sweep DR plan RENAMED` (see paper cut 3), one group in `BARE_METAL_REBUILD` shape. Draft status, never executed, no recoveries.

### Group 5 — Auth errors + AI (#6317 web, #6341/#6345, #6347 AI tools) — browser agent, 2026-09-20 02:24Z

#### #6317 web login form (a) — PASS

Fresh context: cookies cleared, `localStorage`/`sessionStorage` cleared, reload to `/login`. **One** wrong-password submit only (limiter respected).

- `POST /api/v1/auth/login` → **401** `{"error":"Invalid email or password","code":"INVALID_CREDENTIALS"}`.
- The form renders **"Invalid email or password"** — readable prose, no raw code, not blank, and the machine-readable `code` stays out of the UI. Stayed on `/login`.
- Correct password then logged in normally (first attempt `428 auth_binding_rotation_required` — the documented noise — second attempt **200**, landed on `/`).

#### #6317 users 409 CONFLICT (b) — PASS (message is shown; see paper cut 1 for why it is invisible to testids)

- `/settings/users` → **Invite user** → name `Dupe Admin`, email `admin@breeze.local` (already a user; reserved domain, and the 409 fires before any send path) → **Send Invite**.
- `POST /api/v1/users/invite` → **409** `{"error":"User already exists in this scope","code":"CONFLICT"}` — the expected code.
- The UI **does** show it: a destructive toast reading **"User already exists in this scope"** (`bg-destructive text-destructive-foreground`, `UsersPage.tsx:238` `addToast('error', …)`). It has no `data-testid`, so the standard `[data-testid="toast"]` probe misses it — found by walking the DOM for the text. Modal stays open with the values intact, which is the right behaviour.
- No user row was created.

#### #6341 / #6345 AI registry + harness (c, first half) — PASS

- ANTHROPIC key live on the stack; the chat answered and billed (`$0.20 → $0.41 this month`, 2 msgs).
- **A chat turn routed to a registry tool and executed.** `ai_tool_executions` holds exactly one row: `search_documentation` / `completed`, and the transcript shows "Searched documentation" twice.
- `GET /api/v1/admin/ai/tool-usage?days=90` now returns a real **hot** row instead of the empty case proven in pass 1:
  `{"surface":"chat","toolName":"search_documentation","executions":1,"completed":1,"failed":0,"rejected":0,"distinctSessions":1,"avgDurationMs":55,"lastUsedAt":"2026-09-20T02:21:45.109Z"}` with 238 cold tools. The registry-generated hot/cold report works end-to-end.

#### #6347 AI tools (`list_work_types` / `log_time_entry` with `workType`) — **FAIL (pre-existing structural gap, not a #6347 regression)**

The ticketing tool is **not reachable from the AI chat at all**, so none of the three claims in this row can fire.

Observed (2 chat turns, both with the session on the dashboard, partner scope, platform admin):

1. *"list my work types"* → the model ran `search_documentation` twice and replied **""Work types" isn't a feature within Breeze RMM — it's not a concept I have tools or documentation for in this platform."**
2. *"Use the manage_tickets tool with action list_work_types …"* (naming the tool explicitly) → **"I don't have a manage_tickets tool available in my current toolset."**

Root cause, traced in source (not inferred from the model's answer):

- The chat session is created with `allowedTools: [...BREEZE_MCP_TOOL_NAMES, …]` (`services/streamingSessionManager.ts:1141`).
- `BREEZE_MCP_TOOL_NAMES = Object.keys(TOOL_TIERS).map(n => 'mcp__breeze__' + n)` (`services/aiAgentSdkTools.ts:393`).
- **`manage_tickets` has no `TOOL_TIERS` entry and is never passed to `tool()` in `buildBreezeSdkTools` — the string does not occur anywhere in `aiAgentSdkTools.ts`.** It is registered only in the execution registry (`services/aiToolsTicketing.ts:332`, `registerTicketingTools`).
- This is a **known, frozen gap**: `manage_tickets` is an entry in `KNOWN_MISSING_TOOL_TIERS` in `services/aiAgentSdkTools.registryParity.contract.test.ts` (line ~95), the allowlist whose own docblock says these tools are "registered but have no `TOOL_TIERS` entry, and therefore **invisible to chat**… the AI chat will tell users the capability does not exist." That is verbatim what happened. Frozen as of #3300; the contract test is green because the name is on the baseline.

Consequences for this row:
- `list_work_types` from chat — **unreachable**.
- `log_time_entry` with `workType` creating an entry with the resolved work type — **unreachable**; `time_entries` still holds only the 3 rows group 2 created, none from AI.
- *"log time with work type Nonexistent must fail loudly"* — **unreachable** (the guard at `aiToolsTicketing.ts:1020-1031` exists and is unit-tested, but nothing in chat can call it).
- The external MCP-server surface (`POST /api/v1/mcp`) *does* declare `manage_tickets`, so the tool is not dead everywhere — but that route rejects a normal bearer token (`401 ERR_JOSE_ALG_NOT_ALLOWED`; it wants an MCP OAuth token), so it could not be exercised from this sweep without minting one. Not attempted.

**Recommendation before the cut:** this is not new breakage and should not block the release, but the #6347 release notes must not claim AI-assisted time logging. The fix is one line — a `manage_tickets: 1` entry in `TOOL_TIERS` plus removing it from `KNOWN_MISSING_TOOL_TIERS` — and its guardrail entries already exist (`aiGuardrails.ts:53,293,380,751,1517`), so the approval gating is ready for it. Filing suggested.

**AI spend:** 2 turns, $0.41 total. Well inside the ≤6-turn cap; no credit block was hit (the partner is on the platform key, not a metered plan).

## Paper cuts
- #6360: file bind-mounts (`/etc/hosts` etc.) listed as volumes — see group 1 enroll notes; dedupe by device / skip non-directory mount points.

### Group 1 — Disk cleanup (#6360, #6346)

1. **Cleanup preview renders only 40 of the candidates and has no select-all.** With 61 candidates / 200.0 MB the list stops at 40 rows (137.0 MB) with 20 pre-checked; the remaining 21 — the entire firefox `cache2` and pip-wheels sets — are invisible and unselectable. The header keeps advertising "61 candidates · 200.0 MB potential" that the UI cannot deliver, so a tech runs cleanup, reads "200 MB potential", and gets 117 MB with no explanation. Needs pagination or a select-all plus a visible truncation notice.
2. **Disk Intelligence panel claims the browsed folder's scope but shows — and acts on — the OS root.** The header reads "Fast scan and safe cleanup planning for **/home**" while the snapshot, Preview and Execute below it are all `/` (largest dirs `/usr`, `/var`, `/mnt/data/projects`). Worse, its **Analyze** button really does rescan `/home`: it wrote a `/home` snapshot that the panel then ignored, leaving the timestamp stuck at the old `/` capture — the button looks broken. Either scope the whole panel to the browsed path or label it with the volume it actually plans against.
3. **Preview after a successful Execute re-offers the files that were just deleted.** Clicking Preview Cleanup again (no rescan) returned the stale snapshot's full "61 candidates · 200.0 MB" — and clearing the `disk-cleanup-result` panel in the process, so the only record of what was just freed disappears. Execute should invalidate/refresh the snapshot, or Preview should warn that it is planning against a pre-cleanup scan.
4. **Result-line arithmetic reads wrong when anything is rejected.** Success case: "reclaimed 117.0 MB from **40** target(s), 0 failed" — 39 completed, 1 rejected. Failure case: "Cleanup **failed**: reclaimed 0 B from 1 target(s), **0 failed**." The headline and the counts contradict each other; "target(s)" should be the acted-on count, and rejected should not be narrated as "0 failed".
5. **The Disk Cleanup tab never shows reclaimable bytes.** The summary offers "Cleanup Candidates 61" but no MB figure — the number an operator is actually deciding on — even though the same snapshot yields it. It is only reachable by leaving for the File Manager and running a preview.
6. **`Analyze Now` gives no confirmation.** A UI rescan (7–25 s round trip) shows no toast and no in-flight state; the only feedback is the Last Scan timestamp quietly changing, which is easy to miss on a volume that has never been scanned.

### Group 2 — Billing + contracts (paper cuts, none blocking)

1. **`/settings/ticketing` → Categories: duplicate/stale page heading.** The panel renders its own `<h1 data-testid="ticket-categories-heading">` reading **"Ticketing"**, directly under the page's own `<h1>` "Ticketing Settings". Left over from when `TicketCategoriesPage` was a standalone page; every other tab panel starts with a scoped `<h2>` ("Ticket Statuses", "Priorities & SLAs", …). Two `h1`s on one page is also an a11y nit.
2. **Work type is invisible on the ticket's own Time & Billing entry list.** `TicketTimeBilling` renders each entry as `Breeze Admin — <description> | <minutes>` with no work type, so the tech who just picked "Onsite Visit" gets no confirmation on the surface they picked it from. The Work Type column exists only on `/timesheet`.
3. **Quick-add / Timer work-type selects do not pre-select the ticket category's default.** Both open on `No work type` even when the ticket's category has a default (here, Remote Support). The server does apply the default on save, so the outcome is right — but the control shows a value that is not what will be stored. Pre-selecting the resolved default (or labelling the blank option "Category default — Remote Support") would make the field honest.
4. **Service principal scopes render as raw scope strings.** The create/edit drawer lists `organizations:read`, `backup-configuration:read`, `contracts:write` … as bare identifiers in a 2-column grid with no grouping, no read/write separation, and no description of what `contracts:write` actually grants (it is the Partner API contract surface only — not lifecycle, not the human route). This is the screen where an MSP decides to hand a machine identity contract-write power.
5. **Timer widget's stop popover has no work-type control.** The work type is chosen at *start* (`timer-work-type`) and cannot be corrected in `timer-stop-popover`, which does offer description and billable. A tech who forgot to set it must go to `/timesheet` and edit the row afterwards.

### Group 3 — Email templates (#6088, #6089)

1. **The preview never shows the real button.** `{{cta_button}}` renders as the literal word "Button" in a plain `<p>` (`SAMPLE_VARS.cta_button = 'Button'`, `EmailTemplateEditor.tsx:49`) even though the operator just typed "Review & accept" into `email-template-button-label` two fields above. The preview also omits the heading and the layout chrome entirely, so the one field an MSP is most likely to fuss over is the one field the preview cannot show. Substituting the live `buttonLabel` into a rendered button is a two-line change.
2. **The six template names are hard-coded English.** `EMAIL_TEMPLATE_LABELS` (`packages/shared/src/utils/emailTemplates.ts:91-96`) is a plain string map, so in German the list reads "Public reply notice / Quote / proposal / Invoice / Portal invite" with a translated "Standard" pill beside it. The list is the tab's only navigation, so it is the most visible English text left on an otherwise fully translated screen.
3. **Reset says "saved".** `email-template-reset` toasts **"Email template saved"** — the same string as Save (`settings.json emailTemplates.saved`). Destructive-ish action, indistinguishable confirmation; it needs its own copy ("Reset to default") and arguably a confirm, since one click silently discards a customized template with no undo.
4. **Template selection is not in the URL.** Reloading `/settings/partner#email-templates` reopens whatever template was last selected instead of the list, and browser Back does not leave the editor. The repo rule is `window.location.hash` for exactly this kind of selection state (`DeviceDetails.tsx` pattern); the tab uses the hash for itself but the editor selection is component state only.
5. **Legacy inbound autoresponse text is an invisible override.** `renderPartnerEmail` still falls back to `inboundAutoresponseFallback` (`autoresponseSubject` / `autoresponseBody` on the inbound-email config) whenever `ticket_autoresponse` has no custom subject/html (`renderPartnerEmail.ts:160-172`). Those two fields no longer have any UI — the inbound card only links here — so a tenant that set them before #6088 keeps sending the old one-liner, sees "Using default" on this tab, and has no way to clear it except by customizing the template. Either migrate the legacy values into the template on first read or surface them in this tab.
6. **No "preview as sent" / send-test affordance.** The preview shows the sanitized body only; there is no way to see the full rendered email (header, heading, button, footer) or to send a test to yourself before a real quote goes to a customer.

### Group 4 — DR editor (#6321)

1. **A failed DR execution shows the operator the raw error code and nothing else.** Refusing a rehearsal renders the bare string **`resource_not_found`** as a red line above the Plans tab — no toast, no sentence, no hint that the device has no restorable backup snapshot, and no indication of *which* device or group. This is the highest-stakes button on the page (an MSP clicks it during an actual outage) and the failure message is a machine token. It needs mapped copy ("No restorable backup snapshot for <device> — the bare-metal guard has not marked one restorable yet") plus a toast.
2. **Server validation errors leak the request field path.** The output-dir rejection renders as **"restoreConfig.outputDir: absolute path required"** — accurate but it names an API field the operator has never seen, and it appears at the top of the modal rather than under the **Output directory** input it belongs to. The same 400 body already carries `fieldErrors` keyed well enough to place it inline.
3. **Saving a plan is not atomic, and the list goes stale on a partial failure.** Save fires `PATCH /dr/plans/:id` first and `PATCH …/groups/:groupId` second. Renaming the plan *and* entering a bad output dir in one save produced **plan PATCH 200, group PATCH 400** — the rename was committed (DB: `Sweep DR plan RENAMED`) while the group change was rejected, and after cancelling out the plans table still displayed the **old** name. The operator is told the save failed, is looking at the old name, and the new name is what is actually stored.
4. **Nothing on this screen has a `data-testid` except the three #6321 added.** `Create Plan`, `Add group`, `Save plan`, `Cancel`, `Edit`, `Execute`, the execution-type cards and the whole plans table are addressable only by text — the repo's e2e convention (`e2e-tests/README.md`) is `data-testid` only, so DR cannot be covered by an E2E spec today. #6321 added `dr-group-step-type` / `dr-group-rebuild-options` / `-output-dir` / `-wait-timeout` and stopped there.
5. **No toast on a successful plan save.** Create and update both close the modal silently; the only confirmation is the row appearing/updating in the table behind it. Every other mutation surface in the app toasts (`runAction`).
6. **A `BARE_METAL_REBUILD` group saves happily with no rebuild host.** `rebuildHostDeviceId` is `.optional()` in the schema but is *required at dispatch for a rehearsal* (`drBareMetalRebuildStep.ts:24`), so the editor lets an operator build a plan that cannot rehearse and only find out when they press Execute during an incident. The panel should at least warn inline when the host is empty.
7. **Empty output directory saves as the default with no feedback.** Clearing the field returns a 200 and stores `/var/lib/breeze/rebuild/out`; on reopen the field shows that value as if the operator typed it. Harmless, but the field is the one that decides where multi-GB VHDX images land, so a silent substitution is worth a hint.

### Group 5 — Auth errors + AI (#6317, #6341/#6345, #6347)

1. **`UsersPage` ships its own toast implementation with no `data-testid`.** `apps/web/src/components/settings/UsersPage.tsx:44` defines a local `addToast` and renders its own markup (`relative rounded-lg px-4 py-3 pr-10 shadow-lg bg-destructive …`), instead of the shared host at `apps/web/src/components/shared/Toast.tsx:129-139` which carries `data-testid="toast-container"` / `data-testid="toast"`. Every invite/edit/MFA-reset outcome on this page is therefore invisible to `data-testid`-based e2e and to any toast assertion — it reads exactly like a silent mutation until you walk the DOM for the text. Swap it for the shared toast.
2. **The 409 message is the API's sentence, not the user's.** "User already exists in this scope" leaks the API's tenancy vocabulary ("scope") and does not tell the admin what to do — the user is right there in the table behind the modal. "admin@breeze.local is already a user here" plus focusing the existing row would be one step shorter.
3. **The 428 rotation costs a visible failed login on every fresh client.** The first submit after clearing cookies returns `428 auth_binding_rotation_required` and the form just sits there; the operator has to press Sign in again. Known noise on this stack, but it is the first interaction a new user has with the product, and nothing in the UI says "try again" — worth either auto-retrying once or saying so.
4. **The hot/cold tool report cannot distinguish "unused" from "unreachable".** `GET /admin/ai/tool-usage` lists `manage_tickets` among the 238 cold tools exactly as it lists a tool nobody happened to call, even though `manage_tickets` is structurally impossible to call from chat (`KNOWN_MISSING_TOOL_TIERS`). The report is the instrument that is supposed to surface this class of problem, so it should mark the tools that are not in `TOOL_TIERS` as *unreachable*, not merely cold.
5. **`#1105` tripwire fires on every AI chat message.** The API logs `rateLimiter(ai) ran inside a held withDbAccessContext transaction — it pins a pooled connection idle-in-transaction across slow work (#1105). Move it after the context closes or wrap it in runOutsideDbContext().` **twice per** `POST /ai/sessions/:id/messages`. Not new to this window and not a functional failure here, but the AI chat is the slowest request on the box, which is precisely the shape #1105 warns about.

## Fixes applied

## Issues filed
- #6375 — agent: root `/root/.local/share/Trash` always rejected by the cleanup anchor guard (fully-literal pattern anchors on itself; fails closed). Verified in source, main session.
- #6382 — DR plans: execute failure shows bare `resource_not_found`; plan save not atomic (rename commits when the group PATCH 400s). Browser-observed, not re-verified in source.
- #3300 (comment) — `manage_tickets` has no `TOOL_TIERS` entry, so #6347's AI time-logging with `workType` is unreachable from in-app chat. Pre-existing, verified by grep; MCP-server path not tested.
- PR #6366 (comment) — pointed Disk Cleanup W03 at #6375 / #6376 / the volume-picker bind-mount cut.
- #6376 — web: File Manager cleanup preview renders only 40 candidates (preselects 20), no way to select the rest; carries 5 related paper cuts.

## Summary

Pass 2 base `b4f589a6b3`; 19 rows + 8 collapsed N/A. Oldest PR reached: #5969 (N/A) / #6087 (walked).

| Group | PASS | PARTIAL | FAIL | BLOCKED |
|---|---|---|---|---|
| 1 Disk cleanup (#6360, #6346) — live Linux agent 0.115.0 | 3 | 1 | 0 | 0 |
| 2 Billing + contracts (#6347, #6087) | 5 | 0 | 0 | 0 |
| 3 Email templates (#6088, #6089) | 2 | 2 | 0 | 0 |
| 4 DR editor (#6321) | 1 | 0 | 0 | 1 |
| 5 Auth errors + AI (#6317, #6341, #6345, #6347 AI) | 3 | 0 | 1 | 0 |
| **Total** | **14** | **3** | **1** | **1** |

### Top findings
1. **Disk Cleanup v2 works end to end on a real agent** — execute freed 117 MB, controls survived, the WS-delivered scan now persists a snapshot (the v1 bug), the < 0.115.0 gate returns a clean 409. Two gaps: the File Manager preview caps the list at 40 with no way to select the rest (#6376), and root's Trash can never be cleaned on Linux (#6375, fails closed).
2. **AI time-logging from #6347 is unreachable in in-app chat** — `manage_tickets` was never tiered (#3300). Pre-existing; keep it out of the release notes' AI claims.
3. **DR editor round-trip is fixed** (#6321), but the execute-failure copy and the non-atomic save are operator-hostile on the highest-stakes page (#6382).
4. Systemic paper-cut pattern again: pages shipping their own toast/feedback outside the shared host (`UsersPage.tsx:44`), and result copy whose arithmetic doesn't add up (cleanup result line).

### No fixes applied this pass
Every defect sat on an excluded surface (agent-shipped guard code, AI tool exposure, DR) or is already owned by an open wave (#6366 W03). Filed instead.

### Before the release cut
- **Needs an agent release**: Disk Cleanup execute is gated on agent ≥ 0.115.0 — older agents get 409 `agent_update_required` by design.
- **Send-path email rows are PARTIAL** (#6088/#6089): renderer + sanitizer proven, no real send (stack holds a live Resend key). One real quote/invoice/invite send on staging closes them.
- **BLOCKED → lab rig**: #6321 DR `BARE_METAL_REBUILD` execution (W05 Task 10 lab run), plus the pass-1 live-agent rows.
- **Owner gate unchanged**: #4628 dry-run on EU + US before billing W02.
- Release notes: do not claim AI-assisted time logging in chat (#3300).
